// Engine Sim V0.1.14A
// Ferrari Dino 308 gt4 (F 106 A) 250 @ 7000
// Created by oror 2025

/*
{
  "cam_cfg": {
    "equal_base_radius": true,
    "exhaust_at_lift": 0.508,
    "exhaust_base_mult": 1.75,
    "exhaust_cos": false,
    "exhaust_sigma": 2.0,
    "exhaust_trim": 0.7,
    "exhaust_volume": 0.35,
    "intake_at_lift": 0.508,
    "intake_base_mult": 1.75,
    "intake_cos": false,
    "intake_sigma": 2.0,
    "intake_trim": 0.7,
    "intake_volume": 0.35,
    "lift_significant_fraction": 250.0,
    "ramp_position": 0.0,
    "ramp_steepness": 0.0,
    "resolution": 64,
    "roller_tappet_radius": 0.0
  },
  "flow_cfg": {
    "collector_area_coeff": 0.4,
    "exhaust_flange_dia": null,
    "exhaust_port_dia": 34,
    "exhaust_stem_dia": null,
    "intake_port_dia": 37,
    "intake_runner_dia_mult": 1.02,
    "intake_stem_dia": null,
    "ir_to_er_ratio": 3.0,
    "max_power_rpm": 7000.0,
    "max_torque_rpm": 5000.0,
    "port_to_valve_area": 0.84,
    "power_factor": 1.1,
    "primary_area_coeff": 1.05,
    "resolution": 32,
    "smoothness": 4.0,
    "valve_to_stem_dia": 5.2
  },
  "ign_cfg": {
    "end_deg": 35,
    "end_rpm": 4000,
    "exp_mode": 2,
    "flywheel": 5,
    "resolution": 16,
    "start_deg": 5,
    "start_rpm": 500
  }
}
*/

import "engine_sim.mr"
import "../../fuel/90.mr"

units units()
constants constants()

label bore(81.0)
label stroke(71.0)
label compression_ratio(8.8)
label con_rod(137.0)
label compression_height(33.3 * units.mm) // ?
label intake_valve_diameter(42.4)
label exhaust_valve_diameter(36.7)
label intake_valves(2.0)
label exhaust_valves(2.0)

label cyl(8.0)
label row(2.0)
label cycle(720.0 * units.deg)
label rot90(90.0 * units.deg)
label rot180(180.0 * units.deg)
label rot360(360.0 * units.deg)
label rot(cycle / cyl)
label cyl_base(cyl / row)
label rot_base(cycle / cyl_base)
label vee(rot90)

label redline(7500.0) // RPM

// 308
label IVL(8.949)
label EVL(8.323)
label IVO(34.0 * units.deg) //BTDC
label IVC(46.0 * units.deg) //ABDC
label EVO(36.0 * units.deg) //BBDC
label EVC(38.0 * units.deg) //ATDC

label intake_duration(IVO + IVC + rot180)
label exhaust_duration(EVO + EVC + rot180)

label ILC((rot180 - IVO + IVC) / 2.0 + rot360)
label ELC((rot180 - EVO + EVC) / 2.0 + rot180)

label con_rod_mass(550.0)
label crank_mass(12.0) // kg
label flywheel_mass(4.0) // kg

label exhaust_delay_coeff(1.5)

// standard
public node variant_0 {
    output s1: 0;
    output s3: 1;
    output s4: 2;
    output s2: 3;
}

public node intake_lobe_profile {
    // lobes.py v1.0 | 260.0 254.0 LSA 92.50 ADV -3.50 OVL 72.0
    alias output __out: _lobe_profile;
    function _lobe_profile(2.729 * units.deg)
        _lobe_profile
        .add_sample(-87.315 * units.deg, 0.000 * units.mm)
        .add_sample(-84.586 * units.deg, 0.007 * units.mm)
        .add_sample(-81.858 * units.deg, 0.062 * units.mm)
        .add_sample(-79.129 * units.deg, 0.135 * units.mm)
        .add_sample(-76.401 * units.deg, 0.225 * units.mm)
        .add_sample(-73.672 * units.deg, 0.329 * units.mm)
        .add_sample(-70.943 * units.deg, 0.449 * units.mm)
        .add_sample(-68.215 * units.deg, 0.592 * units.mm)
        .add_sample(-65.486 * units.deg, 0.763 * units.mm)
        .add_sample(-62.758 * units.deg, 0.970 * units.mm)
        .add_sample(-60.029 * units.deg, 1.226 * units.mm)
        .add_sample(-57.300 * units.deg, 1.560 * units.mm)
        .add_sample(-54.572 * units.deg, 2.010 * units.mm)
        .add_sample(-51.843 * units.deg, 2.552 * units.mm)
        .add_sample(-49.115 * units.deg, 3.125 * units.mm)
        .add_sample(-46.386 * units.deg, 3.696 * units.mm)
        .add_sample(-43.658 * units.deg, 4.250 * units.mm)
        .add_sample(-40.929 * units.deg, 4.783 * units.mm)
        .add_sample(-38.200 * units.deg, 5.308 * units.mm)
        .add_sample(-35.472 * units.deg, 5.786 * units.mm)
        .add_sample(-32.743 * units.deg, 6.232 * units.mm)
        .add_sample(-30.015 * units.deg, 6.672 * units.mm)
        .add_sample(-27.286 * units.deg, 7.063 * units.mm)
        .add_sample(-24.557 * units.deg, 7.403 * units.mm)
        .add_sample(-21.829 * units.deg, 7.722 * units.mm)
        .add_sample(-19.100 * units.deg, 8.015 * units.mm)
        .add_sample(-16.372 * units.deg, 8.256 * units.mm)
        .add_sample(-13.643 * units.deg, 8.444 * units.mm)
        .add_sample(-10.914 * units.deg, 8.628 * units.mm)
        .add_sample(-8.186 * units.deg, 8.763 * units.mm)
        .add_sample(-5.457 * units.deg, 8.845 * units.mm)
        .add_sample(-2.729 * units.deg, 8.922 * units.mm)
        .add_sample(0.000 * units.deg, 8.949 * units.mm)
        .add_sample(2.729 * units.deg, 8.922 * units.mm)
        .add_sample(5.457 * units.deg, 8.845 * units.mm)
        .add_sample(8.186 * units.deg, 8.763 * units.mm)
        .add_sample(10.914 * units.deg, 8.628 * units.mm)
        .add_sample(13.643 * units.deg, 8.444 * units.mm)
        .add_sample(16.372 * units.deg, 8.256 * units.mm)
        .add_sample(19.100 * units.deg, 8.015 * units.mm)
        .add_sample(21.829 * units.deg, 7.722 * units.mm)
        .add_sample(24.557 * units.deg, 7.403 * units.mm)
        .add_sample(27.286 * units.deg, 7.063 * units.mm)
        .add_sample(30.015 * units.deg, 6.672 * units.mm)
        .add_sample(32.743 * units.deg, 6.232 * units.mm)
        .add_sample(35.472 * units.deg, 5.786 * units.mm)
        .add_sample(38.200 * units.deg, 5.308 * units.mm)
        .add_sample(40.929 * units.deg, 4.783 * units.mm)
        .add_sample(43.658 * units.deg, 4.250 * units.mm)
        .add_sample(46.386 * units.deg, 3.696 * units.mm)
        .add_sample(49.115 * units.deg, 3.125 * units.mm)
        .add_sample(51.843 * units.deg, 2.552 * units.mm)
        .add_sample(54.572 * units.deg, 2.010 * units.mm)
        .add_sample(57.300 * units.deg, 1.560 * units.mm)
        .add_sample(60.029 * units.deg, 1.226 * units.mm)
        .add_sample(62.758 * units.deg, 0.970 * units.mm)
        .add_sample(65.486 * units.deg, 0.763 * units.mm)
        .add_sample(68.215 * units.deg, 0.592 * units.mm)
        .add_sample(70.943 * units.deg, 0.449 * units.mm)
        .add_sample(73.672 * units.deg, 0.329 * units.mm)
        .add_sample(76.401 * units.deg, 0.225 * units.mm)
        .add_sample(79.129 * units.deg, 0.135 * units.mm)
        .add_sample(81.858 * units.deg, 0.062 * units.mm)
        .add_sample(84.586 * units.deg, 0.007 * units.mm)
        .add_sample(87.315 * units.deg, 0.000 * units.mm)
}

public node exhaust_lobe_profile {
    // lobes.py v1.0 | 260.0 254.0 LSA 92.50 ADV -3.50 OVL 72.0
    alias output __out: _lobe_profile;
    function _lobe_profile(2.703 * units.deg)
        _lobe_profile
        .add_sample(-86.505 * units.deg, 0.000 * units.mm)
        .add_sample(-83.802 * units.deg, 0.006 * units.mm)
        .add_sample(-81.098 * units.deg, 0.054 * units.mm)
        .add_sample(-78.395 * units.deg, 0.122 * units.mm)
        .add_sample(-75.692 * units.deg, 0.201 * units.mm)
        .add_sample(-72.989 * units.deg, 0.298 * units.mm)
        .add_sample(-70.285 * units.deg, 0.406 * units.mm)
        .add_sample(-67.582 * units.deg, 0.537 * units.mm)
        .add_sample(-64.879 * units.deg, 0.688 * units.mm)
        .add_sample(-62.175 * units.deg, 0.871 * units.mm)
        .add_sample(-59.472 * units.deg, 1.099 * units.mm)
        .add_sample(-56.769 * units.deg, 1.386 * units.mm)
        .add_sample(-54.066 * units.deg, 1.770 * units.mm)
        .add_sample(-51.362 * units.deg, 2.264 * units.mm)
        .add_sample(-48.659 * units.deg, 2.799 * units.mm)
        .add_sample(-45.956 * units.deg, 3.332 * units.mm)
        .add_sample(-43.252 * units.deg, 3.864 * units.mm)
        .add_sample(-40.549 * units.deg, 4.361 * units.mm)
        .add_sample(-37.846 * units.deg, 4.864 * units.mm)
        .add_sample(-35.143 * units.deg, 5.323 * units.mm)
        .add_sample(-32.439 * units.deg, 5.738 * units.mm)
        .add_sample(-29.736 * units.deg, 6.159 * units.mm)
        .add_sample(-27.033 * units.deg, 6.533 * units.mm)
        .add_sample(-24.330 * units.deg, 6.859 * units.mm)
        .add_sample(-21.626 * units.deg, 7.156 * units.mm)
        .add_sample(-18.923 * units.deg, 7.437 * units.mm)
        .add_sample(-16.220 * units.deg, 7.667 * units.mm)
        .add_sample(-13.516 * units.deg, 7.847 * units.mm)
        .add_sample(-10.813 * units.deg, 8.018 * units.mm)
        .add_sample(-8.110 * units.deg, 8.147 * units.mm)
        .add_sample(-5.407 * units.deg, 8.225 * units.mm)
        .add_sample(-2.703 * units.deg, 8.297 * units.mm)
        .add_sample(0.000 * units.deg, 8.323 * units.mm)
        .add_sample(2.703 * units.deg, 8.297 * units.mm)
        .add_sample(5.407 * units.deg, 8.225 * units.mm)
        .add_sample(8.110 * units.deg, 8.147 * units.mm)
        .add_sample(10.813 * units.deg, 8.018 * units.mm)
        .add_sample(13.516 * units.deg, 7.847 * units.mm)
        .add_sample(16.220 * units.deg, 7.667 * units.mm)
        .add_sample(18.923 * units.deg, 7.437 * units.mm)
        .add_sample(21.626 * units.deg, 7.156 * units.mm)
        .add_sample(24.330 * units.deg, 6.859 * units.mm)
        .add_sample(27.033 * units.deg, 6.533 * units.mm)
        .add_sample(29.736 * units.deg, 6.159 * units.mm)
        .add_sample(32.439 * units.deg, 5.738 * units.mm)
        .add_sample(35.143 * units.deg, 5.323 * units.mm)
        .add_sample(37.846 * units.deg, 4.864 * units.mm)
        .add_sample(40.549 * units.deg, 4.361 * units.mm)
        .add_sample(43.252 * units.deg, 3.864 * units.mm)
        .add_sample(45.956 * units.deg, 3.332 * units.mm)
        .add_sample(48.659 * units.deg, 2.799 * units.mm)
        .add_sample(51.362 * units.deg, 2.264 * units.mm)
        .add_sample(54.066 * units.deg, 1.770 * units.mm)
        .add_sample(56.769 * units.deg, 1.386 * units.mm)
        .add_sample(59.472 * units.deg, 1.099 * units.mm)
        .add_sample(62.175 * units.deg, 0.871 * units.mm)
        .add_sample(64.879 * units.deg, 0.688 * units.mm)
        .add_sample(67.582 * units.deg, 0.537 * units.mm)
        .add_sample(70.285 * units.deg, 0.406 * units.mm)
        .add_sample(72.989 * units.deg, 0.298 * units.mm)
        .add_sample(75.692 * units.deg, 0.201 * units.mm)
        .add_sample(78.395 * units.deg, 0.122 * units.mm)
        .add_sample(81.098 * units.deg, 0.054 * units.mm)
        .add_sample(83.802 * units.deg, 0.006 * units.mm)
        .add_sample(86.505 * units.deg, 0.000 * units.mm)
}

public node intake_flow {
    alias output __out: _intake_flow;
    function _intake_flow(0.289 * units.mm)
    _intake_flow
        .add_flow_sample(0.000, 0.0)
        .add_flow_sample(0.289, 15.7)
        .add_flow_sample(0.577, 31.1)
        .add_flow_sample(0.866, 46.1)
        .add_flow_sample(1.155, 60.6)
        .add_flow_sample(1.443, 74.5)
        .add_flow_sample(1.732, 87.8)
        .add_flow_sample(2.021, 100.5)
        .add_flow_sample(2.309, 112.5)
        .add_flow_sample(2.598, 124.1)
        .add_flow_sample(2.887, 135.1)
        .add_flow_sample(3.175, 145.7)
        .add_flow_sample(3.464, 156.0)
        .add_flow_sample(3.753, 165.9)
        .add_flow_sample(4.041, 175.5)
        .add_flow_sample(4.330, 184.9)
        .add_flow_sample(4.619, 194.0)
        .add_flow_sample(4.908, 202.9)
        .add_flow_sample(5.196, 211.5)
        .add_flow_sample(5.485, 220.0)
        .add_flow_sample(5.774, 228.3)
        .add_flow_sample(6.062, 236.4)
        .add_flow_sample(6.351, 244.4)
        .add_flow_sample(6.640, 252.1)
        .add_flow_sample(6.928, 259.8)
        .add_flow_sample(7.217, 267.2)
        .add_flow_sample(7.506, 274.6)
        .add_flow_sample(7.794, 281.8)
        .add_flow_sample(8.083, 288.9)
        .add_flow_sample(8.372, 295.9)
        .add_flow_sample(8.660, 302.7)
        .add_flow_sample(8.949, 309.5)
}

public node exhaust_flow {
    alias output __out: _exhaust_flow;
    function _exhaust_flow(0.268 * units.mm)
    _exhaust_flow
        .add_flow_sample(0.000, 0.0)
        .add_flow_sample(0.268, 14.7)
        .add_flow_sample(0.537, 29.1)
        .add_flow_sample(0.805, 43.2)
        .add_flow_sample(1.074, 56.8)
        .add_flow_sample(1.342, 69.9)
        .add_flow_sample(1.611, 82.4)
        .add_flow_sample(1.879, 94.2)
        .add_flow_sample(2.148, 105.5)
        .add_flow_sample(2.416, 116.3)
        .add_flow_sample(2.685, 126.7)
        .add_flow_sample(2.953, 136.6)
        .add_flow_sample(3.222, 146.3)
        .add_flow_sample(3.490, 155.6)
        .add_flow_sample(3.759, 164.6)
        .add_flow_sample(4.027, 173.3)
        .add_flow_sample(4.296, 181.9)
        .add_flow_sample(4.564, 190.2)
        .add_flow_sample(4.833, 198.3)
        .add_flow_sample(5.101, 206.3)
        .add_flow_sample(5.370, 214.0)
        .add_flow_sample(5.638, 221.6)
        .add_flow_sample(5.907, 229.1)
        .add_flow_sample(6.175, 236.3)
        .add_flow_sample(6.444, 243.5)
        .add_flow_sample(6.712, 250.4)
        .add_flow_sample(6.981, 257.3)
        .add_flow_sample(7.249, 264.0)
        .add_flow_sample(7.518, 270.6)
        .add_flow_sample(7.786, 277.0)
        .add_flow_sample(8.055, 283.4)
        .add_flow_sample(8.323, 289.7)
}

intake_lobe_profile intake_lobe_profile()
exhaust_lobe_profile exhaust_lobe_profile()

intake_flow intake_flow()
exhaust_flow exhaust_flow()

private node add_lobes {
    input base;
    input shift: 0.0;
    input var;
    input this;
    alias output __out: this;

    this.add_lobe(base + var.s1 * rot_base + shift)
    this.add_lobe(base + var.s2 * rot_base + shift)
    this.add_lobe(base + var.s3 * rot_base + shift)
    this.add_lobe(base + var.s4 * rot_base + shift)
}

public node bank_builder {
    input var;
    input crankshaft;
    input incline;
    input phasing;
    input ignition;
    input intake;
    input exhaust;
    input primaries;
    input sh;
    input flip: false;

    output bank: _b0;

    ignition_wire _wire1()
    ignition_wire _wire2()
    ignition_wire _wire3()
    ignition_wire _wire4()

    piston_parameters piston_params(
        mass: 550 * units.g,
        blowby: k_28inH2O(0.025),
        compression_height: compression_height,
        wrist_pin_position: 0.0,
        displacement: 0.0
    )

    cylinder_bank_parameters bank_params(
        bore: bore * units.mm,
        deck_height: (con_rod + stroke / 2.0) * units.mm + compression_height
    )

    connecting_rod_parameters cr_params(
        mass: con_rod_mass * units.g,
        moment_of_inertia: rod_moment_of_inertia(
            mass: con_rod_mass * units.g,
            length: con_rod * units.mm
        ),
        center_of_mass: 0.0,
        length: con_rod * units.mm
    )

    camshaft _intake_cam_0(base_radius: 15.1 * units.mm, lobe_profile: intake_lobe_profile)
    camshaft _exhaust_cam_0(base_radius: 15.1 * units.mm, lobe_profile: exhaust_lobe_profile)

    _intake_cam_0.add_lobes(ILC, phasing, var)
    _exhaust_cam_0.add_lobes(ELC, phasing, var)

    ignition.connect_wire(_wire1, rot_base * var.s1 + phasing)
    ignition.connect_wire(_wire2, rot_base * var.s2 + phasing)
    ignition.connect_wire(_wire3, rot_base * var.s3 + phasing)
    ignition.connect_wire(_wire4, rot_base * var.s4 + phasing)

    cylinder_bank _b0(bank_params, angle: incline)

    _b0
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj0,
            intake: intake,
            exhaust_system: exhaust,
            primary_length: primaries.pl0 + sh,
            ignition_wire: _wire1
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj1,
            intake: intake,
            exhaust_system: exhaust,
            primary_length: primaries.pl1 + sh,
            ignition_wire: _wire2
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj2,
            intake: intake,
            exhaust_system: exhaust,
            primary_length: primaries.pl2 + sh,
            ignition_wire: _wire3
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj3,
            intake: intake,
            exhaust_system: exhaust,
            primary_length: primaries.pl3 + sh,
            ignition_wire: _wire4
        )

    _b0.set_cylinder_head(
        head(
            intake_camshaft: _intake_cam_0,
            exhaust_camshaft: _exhaust_cam_0,
            flip_display: flip
        )
    )
}

private node add_flow_sample {
    input lift;
    input flow;
    input this;
    alias output __out: this;

    this.add_sample(lift * units.mm, k_28inH2O(flow))
}

public node head {
    // port_flow.py v1.7
    // intake port area: 20.5 cm²; saturated lift: 8.80 mm
    // exhaust port area: 17.4 cm²; saturated lift: 8.13 mm
    // cylinder volume: 365.9 cm³ (22.33 CI); engine volume: 2.927 L (178.61 CI)
    // chamber volume: 46.9 cm³ (2.86 CI); conrod/stroke 1.930; N.A.C.C. 32.54
    // 8 harmonic intake runner length: 24.8 cm; diameter: 3.9 cm
    // primary length: 18.2 cm, area: 18.2 cm², diameter: 4.8 cm
    // collector diameter: 8.6 cm, area: 58.4 cm²

    input intake_camshaft;
    input exhaust_camshaft;
    input flip_display: false;
    
    alias output __out: head;
    generic_cylinder_head head(
        chamber_volume: 46.905 * units.cc,
        intake_runner_volume: 294.8 * units.cc,
        intake_runner_cross_section_area: 11.9 * units.cm2,
        exhaust_runner_volume: 98.3 * units.cc,
        exhaust_runner_cross_section_area: 18.2 * units.cm2,

        intake_port_flow: intake_flow,
        exhaust_port_flow: exhaust_flow,
        valvetrain: standard_valvetrain(
            intake_camshaft: intake_camshaft,
            exhaust_camshaft: exhaust_camshaft
        ),
        flip_display: flip_display
    )
}

public node crank {
    input phasing;
    input var;

    output c: _c;
    output rj0: _rj0;
    output rj1: _rj1;
    output rj2: _rj2;
    output rj3: _rj3;

    label crank_moment(disk_moment_of_inertia(mass: crank_mass * units.kg, radius: (stroke / 2.0) * units.mm))
    label flywheel_moment(disk_moment_of_inertia(mass: flywheel_mass * units.kg, radius: (stroke * 3.5) * units.mm))
    label other_moment(disk_moment_of_inertia(mass: 1 * units.kg, radius: 1.0 * units.cm))

    crankshaft _c(
        throw: (stroke / 2.0) * units.mm,
        flywheel_mass: flywheel_mass * units.kg,
        mass: crank_mass * units.kg,
        friction_torque: 4.4 * units.Nm,
        moment_of_inertia: crank_moment + flywheel_moment + other_moment,
        tdc: 0.0
    )

    rod_journal _rj0(angle: rot_base * var.s1 + rot90 + vee / 2)
    rod_journal _rj1(angle: rot_base * var.s2 + rot90 + vee / 2)
    rod_journal _rj2(angle: rot_base * var.s3 + rot90 + vee / 2)
    rod_journal _rj3(angle: rot_base * var.s4 + rot90 + vee / 2)

    _c
        .add_rod_journal(_rj0)
        .add_rod_journal(_rj1)
        .add_rod_journal(_rj2)
        .add_rod_journal(_rj3)
}

public node eng {
    alias output __out: engine;

    engine engine(
        name: "Ferrari 308",
        starter_torque: 150 * units.Nm,
        starter_speed: 400 * units.rpm,
        redline: redline * units.rpm,
        fuel: s90(),
        throttle_gamma: 1.5,
        jitter: 1.0,
        noise: 0.1,
        hf_gain: 0.001,
        simulation_frequency: 7500,
        dyno_min_speed: 750 * units.rpm
    )

    variant_0 variant()

    crank c0(vee / 2 + rot90, variant)

    bank_builder bank0(
        var: variant,
        crankshaft: c0,
        incline: vee / 2,
        phasing: 0,
        ignition: ignition,
        intake: intake,
        exhaust: exhaust0,
        primaries: em_0(),
        sh: 0.0 * units.cm,
        flip: true
    )

    bank_builder bank1(
        var: variant,
        crankshaft: c0,
        incline: -vee / 2,
        phasing: vee + rot360,
        ignition: ignition,
        intake: intake,
        exhaust: exhaust1,
        sh: 0.0 * units.cm,
        primaries: em_0()
    )

    ignition_module ignition(timing_curve: timing_curve, rev_limit: redline * 1.05 * units.rpm, limiter_duration: 0.06)

    label collector_cross_section_area(29.2)

    label exhaust_pipe_length_0(190.0) // cm
    label exhaust_volume_0(collector_cross_section_area * exhaust_pipe_length_0 / 1000.0) // Litres
    
    label exhaust_pipe_length_1(120.0) // cm
    label exhaust_volume_1(collector_cross_section_area * exhaust_pipe_length_1 / 1000.0) // Litres

    intake intake(
        plenum_volume: 3.2 * units.L,
        plenum_cross_section_area: 47.6 * units.cm2,
        runner_length: 24.8 * units.cm,
        intake_flow_rate: k_carb(384.2),
        idle_flow_rate: k_carb(0.01),
        idle_throttle_plate_position: 0.9945,
        runner_flow_rate: k_carb(100.8),
        velocity_decay: 0.03
    )
    
    exhaust_system_parameters es_params0(
        outlet_flow_rate: k_carb(328.1),
        collector_cross_section_area: 29.2 * units.cm2,
        length: exhaust_pipe_length_0 * units.cm,
        primary_tube_length: 18.2 * units.cm,
        primary_flow_rate: k_carb(168.2),
        velocity_decay: 1.0,
        volume: (exhaust_volume_0 + 2.0) * units.L
    )

    exhaust_system_parameters es_params1(
        outlet_flow_rate: k_carb(328.1),
        collector_cross_section_area: 29.2 * units.cm2,
        length: exhaust_pipe_length_1 * units.cm,
        primary_tube_length: 18.2 * units.cm,
        primary_flow_rate: k_carb(168.2),
        velocity_decay: 1.0,
        volume: (exhaust_volume_1 + 2.0) * units.L
    )
    
    exhaust_system exhaust0(es_params0, impulse_response: impulse_response(filename: "../../impulse_responses/smooth_56.wav", volume: 0.001))
    exhaust_system exhaust1(es_params1, impulse_response: impulse_response(filename: "../../impulse_responses/smooth_57.wav", volume: 0.001))

    engine
        .add_crankshaft(c0.c)
        .add_cylinder_bank(bank0.bank)
        .add_cylinder_bank(bank1.bank)
        .add_ignition_module(ignition)

    function timing_curve(500.0 * units.rpm)
    timing_curve
        .add_sample(0.0 * units.rpm, 5.0 * units.deg)
        .add_sample(500.0 * units.rpm, 5.0 * units.deg)
        .add_sample(1000.0 * units.rpm, 15.3 * units.deg)
        .add_sample(1500.0 * units.rpm, 22.9 * units.deg)
        .add_sample(2000.0 * units.rpm, 28.6 * units.deg)
        .add_sample(2500.0 * units.rpm, 32.8 * units.deg)
        .add_sample(3000.0 * units.rpm, 35.9 * units.deg)
        .add_sample(3500.0 * units.rpm, 38.3 * units.deg)
        .add_sample(4000.0 * units.rpm, 40.0 * units.deg)
        .add_sample(4500.0 * units.rpm, 40.0 * units.deg)
        .add_sample(5000.0 * units.rpm, 40.0 * units.deg)
        .add_sample(5500.0 * units.rpm, 40.0 * units.deg)
        .add_sample(6000.0 * units.rpm, 40.0 * units.deg)
        .add_sample(6500.0 * units.rpm, 40.0 * units.deg)
        .add_sample(7000.0 * units.rpm, 40.0 * units.deg)
        .add_sample(7500.0 * units.rpm, 40.0 * units.deg)
        .add_sample(8000.0 * units.rpm, -12.5 * units.deg)
        .add_sample(8500.0 * units.rpm, -25.0 * units.deg)
}

label spacing_factor(1.1)
label flange_density(1.5 * exhaust_delay_coeff)
label em_unit(bore * spacing_factor / flange_density)

private node em_0{
    output pl0: (3.4 * em_unit) * units.mm;
    output pl1: (2.4 * em_unit) * units.mm;
    output pl2: (1.5 * em_unit) * units.mm;
    output pl3: (1.0 * em_unit) * units.mm;
}

// 1988 750iL
public node veh {
    alias output __out: vehicle;

    label car_mass(1960) // 1860 empty

    vehicle vehicle(
        mass: car_mass * units.kg,
        drag_coefficient: 0.34,
        cross_sectional_area: (1845  * units.mm) * (1400 * units.mm),
        diff_ratio: 3.15,
        tire_radius: (653.0 / 2) * units.mm, // 225/60 VR 15
        rolling_resistance: 0.012 * car_mass * 9.81
        )
}

// ZF 4HP24E
private node trn {
    alias output __out:
        transmission(
            max_clutch_torque: 675 * units.Nm,
            max_clutch_flex: 30 * units.deg,
            limit_clutch_flex: true,
            clutch_stiffness: 20 * units.Nm / units.deg,
            clutch_damping: 1.0,
            simulate_flex: true
        )
        .add_gear(2.479)
        .add_gear(1.479)
        .add_gear(1.000)
        .add_gear(0.728);
}

run(
    engine: eng(),
    transmission: trn(),
    vehicle: veh()
)
