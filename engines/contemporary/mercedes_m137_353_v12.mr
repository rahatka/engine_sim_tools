// Engine Sim V0.1.14A
// 1998 Mercedes-Benz M 137 362 HP @ 5500
// Created by oror 2023
// 1-12-5-8-3-10-6-7-2-11-4-9

import "engine_sim.mr"
import "../../fuel/90.mr"

units units()
constants constants()

label bore(84.0)
label stroke(87.0)
label compression_ratio(10.0)
label con_rod(138.7)
label compression_height(33.0 * units.mm)
label intake_valve_diameter(35.0) // 129.067
label exhaust_valve_diameter(37.1) // ?? 115 050 00 27
label intake_valves(2.0)
label exhaust_valves(1.0)

label redline(6000.0) // RPM

label cyl(12.0)
label row(2.0)
label cycle(720.0 * units.deg)
label vee(60.0 * units.deg)
label rot(cycle / cyl)
label rot90(90.0 * units.deg)
label rot180(180.0 * units.deg)
label rot360(360.0 * units.deg)
label cyl_base(cyl / row)
label rot_base(cycle / cyl_base)

// Mercedes 5,8 V12 3V ( M137 ) /Performance camshafts
label IVL(11.2)
label EVL(11.2)
label IVO(16.0 * units.deg) //BTDC
label IVC(64.0 * units.deg) //ABDC
label EVO(66.0 * units.deg) //BBDC
label EVC(18.0 * units.deg) //ATDC

label intake_duration(IVO + IVC + rot180)
label exhaust_duration(EVO + EVC + rot180)

label ILC((rot180 - IVO + IVC) / 2.0 + rot360)
label ELC((rot180 - EVO + EVC) / 2.0 + rot180)

label con_rod_mass(480.0) // g
label piston_mass(500.0) // ?
label crank_mass(25.0) // ? kg
label flywheel_mass(7.0) // kg

label exhaust_delay_coeff(1.5)

/*
{
  "cam_cfg": {
    "equal_base_radius": true,
    "exhaust_at_lift": 0.508,
    "exhaust_base_mult": 1.917,
    "exhaust_cos": false,
    "exhaust_sigma": 3.2,
    "exhaust_trim": 0.65,
    "exhaust_volume": 0.4,
    "intake_at_lift": 0.508,
    "intake_base_mult": 1.917,
    "intake_cos": false,
    "intake_sigma": 3.2,
    "intake_trim": 0.65,
    "intake_volume": 0.4,
    "lift_significant_fraction": 250.0,
    "ramp_position": 0.0,
    "ramp_steepness": 5.0,
    "resolution": 64,
    "roller_tappet_radius": 0.5
  },
  "flow_cfg": {
    "collector_area_coeff": 0.3,
    "exhaust_flange_dia": null,
    "exhaust_port_dia": null,
    "exhaust_stem_dia": null,
    "intake_port_dia": null,
    "intake_runner_dia_mult": 1.04,
    "intake_stem_dia": null,
    "ir_to_er_ratio": 3.3,
    "max_power_rpm": 5500.0,
    "max_torque_rpm": 3700.0,
    "port_to_valve_area": 0.86,
    "power_factor": 1.0,
    "primary_area_coeff": 1.1,
    "resolution": 32,
    "smoothness": 2.0,
    "valve_to_stem_dia": 5.3
  },
  "ign_cfg": {
    "end_deg": 32,
    "end_rpm": 3000,
    "exp_mode": 2,
    "flywheel": 4,
    "resolution": 16,
    "start_deg": 0,
    "start_rpm": 300
  }
}
*/

public node intake_lobe_profile {
    // lobes.py v1.0 | 260.0 264.0 LSA 114.00 ADV 0.00 OVL 34.0
    alias output __out: _lobe_profile;
    function _lobe_profile(2.419 * units.deg)
        _lobe_profile
        .add_sample(-77.415 * units.deg, 0.000 * units.mm)
        .add_sample(-74.996 * units.deg, 0.016 * units.mm)
        .add_sample(-72.576 * units.deg, 0.146 * units.mm)
        .add_sample(-70.157 * units.deg, 0.288 * units.mm)
        .add_sample(-67.738 * units.deg, 0.429 * units.mm)
        .add_sample(-65.319 * units.deg, 0.574 * units.mm)
        .add_sample(-62.899 * units.deg, 0.733 * units.mm)
        .add_sample(-60.480 * units.deg, 0.916 * units.mm)
        .add_sample(-58.061 * units.deg, 1.128 * units.mm)
        .add_sample(-55.642 * units.deg, 1.361 * units.mm)
        .add_sample(-53.223 * units.deg, 1.619 * units.mm)
        .add_sample(-50.803 * units.deg, 1.902 * units.mm)
        .add_sample(-48.384 * units.deg, 2.215 * units.mm)
        .add_sample(-45.965 * units.deg, 2.569 * units.mm)
        .add_sample(-43.546 * units.deg, 2.975 * units.mm)
        .add_sample(-41.127 * units.deg, 3.421 * units.mm)
        .add_sample(-38.707 * units.deg, 3.910 * units.mm)
        .add_sample(-36.288 * units.deg, 4.443 * units.mm)
        .add_sample(-33.869 * units.deg, 5.019 * units.mm)
        .add_sample(-31.450 * units.deg, 5.633 * units.mm)
        .add_sample(-29.031 * units.deg, 6.276 * units.mm)
        .add_sample(-26.611 * units.deg, 6.930 * units.mm)
        .add_sample(-24.192 * units.deg, 7.575 * units.mm)
        .add_sample(-21.773 * units.deg, 8.184 * units.mm)
        .add_sample(-19.354 * units.deg, 8.732 * units.mm)
        .add_sample(-16.934 * units.deg, 9.293 * units.mm)
        .add_sample(-14.515 * units.deg, 9.777 * units.mm)
        .add_sample(-12.096 * units.deg, 10.163 * units.mm)
        .add_sample(-9.677 * units.deg, 10.510 * units.mm)
        .add_sample(-7.258 * units.deg, 10.799 * units.mm)
        .add_sample(-4.838 * units.deg, 10.988 * units.mm)
        .add_sample(-2.419 * units.deg, 11.106 * units.mm)
        .add_sample(0.000 * units.deg, 11.200 * units.mm)
        .add_sample(2.419 * units.deg, 11.106 * units.mm)
        .add_sample(4.838 * units.deg, 10.988 * units.mm)
        .add_sample(7.258 * units.deg, 10.799 * units.mm)
        .add_sample(9.677 * units.deg, 10.510 * units.mm)
        .add_sample(12.096 * units.deg, 10.163 * units.mm)
        .add_sample(14.515 * units.deg, 9.777 * units.mm)
        .add_sample(16.934 * units.deg, 9.293 * units.mm)
        .add_sample(19.354 * units.deg, 8.732 * units.mm)
        .add_sample(21.773 * units.deg, 8.184 * units.mm)
        .add_sample(24.192 * units.deg, 7.575 * units.mm)
        .add_sample(26.611 * units.deg, 6.930 * units.mm)
        .add_sample(29.031 * units.deg, 6.276 * units.mm)
        .add_sample(31.450 * units.deg, 5.633 * units.mm)
        .add_sample(33.869 * units.deg, 5.019 * units.mm)
        .add_sample(36.288 * units.deg, 4.443 * units.mm)
        .add_sample(38.707 * units.deg, 3.910 * units.mm)
        .add_sample(41.127 * units.deg, 3.421 * units.mm)
        .add_sample(43.546 * units.deg, 2.975 * units.mm)
        .add_sample(45.965 * units.deg, 2.569 * units.mm)
        .add_sample(48.384 * units.deg, 2.215 * units.mm)
        .add_sample(50.803 * units.deg, 1.902 * units.mm)
        .add_sample(53.223 * units.deg, 1.619 * units.mm)
        .add_sample(55.642 * units.deg, 1.361 * units.mm)
        .add_sample(58.061 * units.deg, 1.128 * units.mm)
        .add_sample(60.480 * units.deg, 0.916 * units.mm)
        .add_sample(62.899 * units.deg, 0.733 * units.mm)
        .add_sample(65.319 * units.deg, 0.574 * units.mm)
        .add_sample(67.738 * units.deg, 0.429 * units.mm)
        .add_sample(70.157 * units.deg, 0.288 * units.mm)
        .add_sample(72.576 * units.deg, 0.146 * units.mm)
        .add_sample(74.996 * units.deg, 0.016 * units.mm)
        .add_sample(77.415 * units.deg, 0.000 * units.mm)
}

public node exhaust_lobe_profile {
    // lobes.py v1.0 | 260.0 264.0 LSA 114.00 ADV 0.00 OVL 34.0
    alias output __out: _lobe_profile;
    function _lobe_profile(2.456 * units.deg)
        _lobe_profile
        .add_sample(-78.606 * units.deg, 0.000 * units.mm)
        .add_sample(-76.149 * units.deg, 0.014 * units.mm)
        .add_sample(-73.693 * units.deg, 0.144 * units.mm)
        .add_sample(-71.236 * units.deg, 0.286 * units.mm)
        .add_sample(-68.780 * units.deg, 0.427 * units.mm)
        .add_sample(-66.324 * units.deg, 0.572 * units.mm)
        .add_sample(-63.867 * units.deg, 0.731 * units.mm)
        .add_sample(-61.411 * units.deg, 0.908 * units.mm)
        .add_sample(-58.954 * units.deg, 1.120 * units.mm)
        .add_sample(-56.498 * units.deg, 1.353 * units.mm)
        .add_sample(-54.041 * units.deg, 1.610 * units.mm)
        .add_sample(-51.585 * units.deg, 1.894 * units.mm)
        .add_sample(-49.129 * units.deg, 2.206 * units.mm)
        .add_sample(-46.672 * units.deg, 2.550 * units.mm)
        .add_sample(-44.216 * units.deg, 2.953 * units.mm)
        .add_sample(-41.759 * units.deg, 3.399 * units.mm)
        .add_sample(-39.303 * units.deg, 3.887 * units.mm)
        .add_sample(-36.846 * units.deg, 4.419 * units.mm)
        .add_sample(-34.390 * units.deg, 4.994 * units.mm)
        .add_sample(-31.934 * units.deg, 5.607 * units.mm)
        .add_sample(-29.477 * units.deg, 6.249 * units.mm)
        .add_sample(-27.021 * units.deg, 6.902 * units.mm)
        .add_sample(-24.564 * units.deg, 7.545 * units.mm)
        .add_sample(-22.108 * units.deg, 8.153 * units.mm)
        .add_sample(-19.651 * units.deg, 8.713 * units.mm)
        .add_sample(-17.195 * units.deg, 9.279 * units.mm)
        .add_sample(-14.739 * units.deg, 9.762 * units.mm)
        .add_sample(-12.282 * units.deg, 10.148 * units.mm)
        .add_sample(-9.826 * units.deg, 10.506 * units.mm)
        .add_sample(-7.369 * units.deg, 10.796 * units.mm)
        .add_sample(-4.913 * units.deg, 10.985 * units.mm)
        .add_sample(-2.456 * units.deg, 11.106 * units.mm)
        .add_sample(0.000 * units.deg, 11.200 * units.mm)
        .add_sample(2.456 * units.deg, 11.106 * units.mm)
        .add_sample(4.913 * units.deg, 10.985 * units.mm)
        .add_sample(7.369 * units.deg, 10.796 * units.mm)
        .add_sample(9.826 * units.deg, 10.506 * units.mm)
        .add_sample(12.282 * units.deg, 10.148 * units.mm)
        .add_sample(14.739 * units.deg, 9.762 * units.mm)
        .add_sample(17.195 * units.deg, 9.279 * units.mm)
        .add_sample(19.651 * units.deg, 8.713 * units.mm)
        .add_sample(22.108 * units.deg, 8.153 * units.mm)
        .add_sample(24.564 * units.deg, 7.545 * units.mm)
        .add_sample(27.021 * units.deg, 6.902 * units.mm)
        .add_sample(29.477 * units.deg, 6.249 * units.mm)
        .add_sample(31.934 * units.deg, 5.607 * units.mm)
        .add_sample(34.390 * units.deg, 4.994 * units.mm)
        .add_sample(36.846 * units.deg, 4.419 * units.mm)
        .add_sample(39.303 * units.deg, 3.887 * units.mm)
        .add_sample(41.759 * units.deg, 3.399 * units.mm)
        .add_sample(44.216 * units.deg, 2.953 * units.mm)
        .add_sample(46.672 * units.deg, 2.550 * units.mm)
        .add_sample(49.129 * units.deg, 2.206 * units.mm)
        .add_sample(51.585 * units.deg, 1.894 * units.mm)
        .add_sample(54.041 * units.deg, 1.610 * units.mm)
        .add_sample(56.498 * units.deg, 1.353 * units.mm)
        .add_sample(58.954 * units.deg, 1.120 * units.mm)
        .add_sample(61.411 * units.deg, 0.908 * units.mm)
        .add_sample(63.867 * units.deg, 0.731 * units.mm)
        .add_sample(66.324 * units.deg, 0.572 * units.mm)
        .add_sample(68.780 * units.deg, 0.427 * units.mm)
        .add_sample(71.236 * units.deg, 0.286 * units.mm)
        .add_sample(73.693 * units.deg, 0.144 * units.mm)
        .add_sample(76.149 * units.deg, 0.014 * units.mm)
        .add_sample(78.606 * units.deg, 0.000 * units.mm)
}

public node intake_flow {
    alias output __out: _intake_flow;
    function _intake_flow(0.361 * units.mm)
    _intake_flow
        .add_flow_sample(0.000, 0.0)
        .add_flow_sample(0.361, 17.4)
        .add_flow_sample(0.723, 34.1)
        .add_flow_sample(1.084, 49.5)
        .add_flow_sample(1.445, 63.7)
        .add_flow_sample(1.806, 76.7)
        .add_flow_sample(2.168, 88.8)
        .add_flow_sample(2.529, 100.2)
        .add_flow_sample(2.890, 111.1)
        .add_flow_sample(3.252, 121.5)
        .add_flow_sample(3.613, 131.4)
        .add_flow_sample(3.974, 141.1)
        .add_flow_sample(4.335, 150.3)
        .add_flow_sample(4.697, 159.3)
        .add_flow_sample(5.058, 168.0)
        .add_flow_sample(5.419, 176.4)
        .add_flow_sample(5.781, 184.6)
        .add_flow_sample(6.142, 192.6)
        .add_flow_sample(6.503, 200.2)
        .add_flow_sample(6.865, 207.5)
        .add_flow_sample(7.226, 214.1)
        .add_flow_sample(7.587, 219.8)
        .add_flow_sample(7.948, 224.2)
        .add_flow_sample(8.310, 227.3)
        .add_flow_sample(8.671, 229.2)
        .add_flow_sample(9.032, 230.2)
        .add_flow_sample(9.394, 230.8)
        .add_flow_sample(9.755, 231.2)
        .add_flow_sample(10.116, 231.5)
        .add_flow_sample(10.477, 231.7)
        .add_flow_sample(10.839, 232.0)
        .add_flow_sample(11.200, 232.2)
}

public node exhaust_flow {
    alias output __out: _exhaust_flow;
    function _exhaust_flow(0.361 * units.mm)
    _exhaust_flow
        .add_flow_sample(0.000, 0.0)
        .add_flow_sample(0.361, 10.9)
        .add_flow_sample(0.723, 21.3)
        .add_flow_sample(1.084, 31.0)
        .add_flow_sample(1.445, 39.9)
        .add_flow_sample(1.806, 48.0)
        .add_flow_sample(2.168, 55.7)
        .add_flow_sample(2.529, 62.9)
        .add_flow_sample(2.890, 69.7)
        .add_flow_sample(3.252, 76.2)
        .add_flow_sample(3.613, 82.5)
        .add_flow_sample(3.974, 88.6)
        .add_flow_sample(4.335, 94.5)
        .add_flow_sample(4.697, 100.1)
        .add_flow_sample(5.058, 105.6)
        .add_flow_sample(5.419, 111.0)
        .add_flow_sample(5.781, 116.2)
        .add_flow_sample(6.142, 121.2)
        .add_flow_sample(6.503, 126.2)
        .add_flow_sample(6.865, 130.9)
        .add_flow_sample(7.226, 135.5)
        .add_flow_sample(7.587, 139.7)
        .add_flow_sample(7.948, 143.3)
        .add_flow_sample(8.310, 146.3)
        .add_flow_sample(8.671, 148.4)
        .add_flow_sample(9.032, 149.7)
        .add_flow_sample(9.394, 150.6)
        .add_flow_sample(9.755, 151.0)
        .add_flow_sample(10.116, 151.3)
        .add_flow_sample(10.477, 151.5)
        .add_flow_sample(10.839, 151.7)
        .add_flow_sample(11.200, 151.9)
}

intake_lobe_profile intake_lobe_profile()
exhaust_lobe_profile exhaust_lobe_profile()

intake_flow intake_flow()
exhaust_flow exhaust_flow()

private node add_lobes {
    input base;
    input shift: 0.0;
    input this;
    alias output __out: this;

    this.add_lobe(base + 0 * rot * row + shift)
    this.add_lobe(base + 4 * rot * row + shift)
    this.add_lobe(base + 2 * rot * row + shift)
    this.add_lobe(base + 5 * rot * row + shift)
    this.add_lobe(base + 1 * rot * row + shift)
    this.add_lobe(base + 3 * rot * row + shift)
}

public node bank_builder {
    input angle;
    input phasing;
    input ignition;
    input intake;
    input exhaust;
    input pls;

    input rj0;
    input rj1;
    input rj2;
    input rj3;
    input rj4;
    input rj5;

    input flip: false;

    output bank: _b0;

    ignition_wire _wire1()
    ignition_wire _wire2()
    ignition_wire _wire3()
    ignition_wire _wire4()
    ignition_wire _wire5()
    ignition_wire _wire6()

    piston_parameters piston_params(
        mass: piston_mass * units.g,
        blowby: k_28inH2O(0.026),
        compression_height: compression_height,
        wrist_pin_position: 0.0,
        displacement: 0.0
    )

    connecting_rod_parameters cr_params(
        mass: con_rod_mass * units.g,
        moment_of_inertia: rod_moment_of_inertia(
            mass: con_rod_mass * units.g,
            length: con_rod * units.mm
        ),
        center_of_mass: 0.0,
        length: con_rod * units.mm
    )

    cylinder_bank_parameters bank_params(
        bore: bore * units.mm,
        deck_height: (con_rod + stroke / 2.0) * units.mm + compression_height
    )

    camshaft _intake_cam_0(base_radius: 21.5 * units.mm, lobe_profile: intake_lobe_profile)
    camshaft _exhaust_cam_0(base_radius: 21.5 * units.mm, lobe_profile: exhaust_lobe_profile)

    _intake_cam_0.add_lobes(ILC, phasing)
    _exhaust_cam_0.add_lobes(ELC, phasing)

    ignition.connect_wire(_wire1, rot_base * 0 + phasing)
    ignition.connect_wire(_wire5, rot_base * 1 + phasing)
    ignition.connect_wire(_wire3, rot_base * 2 + phasing)
    ignition.connect_wire(_wire6, rot_base * 3 + phasing)
    ignition.connect_wire(_wire2, rot_base * 4 + phasing)
    ignition.connect_wire(_wire4, rot_base * 5 + phasing)

    cylinder_bank _b0(bank_params, angle: angle)

    _b0
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj0,
            intake: intake,
            exhaust_system: exhaust,
            primary_length: pls.pl0,
            ignition_wire: _wire1
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj1,
            intake: intake,
            exhaust_system: exhaust,
            primary_length: pls.pl1,
            ignition_wire: _wire2
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj2,
            intake: intake,
            exhaust_system: exhaust,
            primary_length: pls.pl2,
            ignition_wire: _wire3
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj3,
            intake: intake,
            exhaust_system: exhaust,
            primary_length: pls.pl3,
            ignition_wire: _wire4
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj4,
            intake: intake,
            exhaust_system: exhaust,
            primary_length: pls.pl4,
            ignition_wire: _wire5
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj5,
            intake: intake,
            exhaust_system: exhaust,
            primary_length: pls.pl5,
            ignition_wire: _wire6
        )
    .set_cylinder_head(
        head(
            intake_camshaft: _intake_cam_0,
            exhaust_camshaft: _exhaust_cam_0,
            flip_display: flip
        )
    )
}

private node add_flow_sample {
    input lift;
    input flow;
    input this;
    alias output __out: this;

    this.add_sample(lift * units.mm, k_28inH2O(flow))
}

public node head {
    // port_flow.py v1.5
    // intake port area: 15.9 cm²; saturated lift: 7.78 mm
    // exhaust port area: 8.9 cm²; saturated lift: 8.25 mm
    // cylinder volume: 482.1 cm³ (29.4 CI); engine volume: 5.786 L (353.1 CI)
    // chamber volume: 53.6 cm³ (3.3 CI)
    // 16 harmonic intake runner length: 15.8 cm; diameter: 4.0 cm
    // primary length: 26.9 cm, area: 9.8 cm², diameter: 3.5 cm
    // collector diameter: 6.7 cm, area: 35.3 cm²

    input intake_camshaft;
    input exhaust_camshaft;
    input flip_display: false;
    
    alias output __out: head;
    generic_cylinder_head head(
        chamber_volume: 53.570 * units.cc,
        intake_runner_volume: 201.7 * units.cc,
        intake_runner_cross_section_area: 12.8 * units.cm2,
        exhaust_runner_volume: 61.1 * units.cc,
        exhaust_runner_cross_section_area: 9.8 * units.cm2,

        intake_port_flow: intake_flow,
        exhaust_port_flow: exhaust_flow,
        valvetrain: standard_valvetrain(
            intake_camshaft: intake_camshaft,
            exhaust_camshaft: exhaust_camshaft
        ),
        flip_display: flip_display
    )
}

public node eng {
    alias output __out: engine;

    engine engine(
        name: "Mercedes-Benz M 137",
        starter_torque: 150 * units.Nm,
        starter_speed: 650 * units.rpm,
        redline: redline * units.rpm,
        fuel: s90(),
        throttle_gamma: 1.5,
        jitter: 0.5,
        noise: 0.1,
        hf_gain: 0.007,
        simulation_frequency: 8000
    )

    label crank_moment(disk_moment_of_inertia(mass: crank_mass * units.kg, radius: (stroke / 2.0) * units.mm))
    label flywheel_moment(disk_moment_of_inertia(mass: flywheel_mass * units.kg, radius: (stroke * 3.5) * units.mm))
    label other_moment(disk_moment_of_inertia(mass: 1 * units.kg, radius: 1.0 * units.cm))

    crankshaft c0(
        throw: (stroke / 2.0) * units.mm,
        flywheel_mass: flywheel_mass * units.kg,
        mass: crank_mass * units.kg,
        friction_torque: 8.7 * units.Nm,
        moment_of_inertia: crank_moment + flywheel_moment + other_moment,
        position_x: 0.0,
        position_y: 0.0,
        tdc: 0.0
    )

    ignition_module ignition(timing_curve: timing_curve, rev_limit: 6000 * units.rpm, limiter_duration: 0.07)

    exh_setup_0 exh()

    bank_builder bank0(
        angle: vee / 2.0,
        phasing: 0,
        ignition: ignition,
        intake: intake,
        exhaust: exh.ex0,
        pls: exh,
        rj0: rj0, rj1: rj1, rj2: rj2, rj3: rj3, rj4: rj4, rj5: rj5,
        flip: true
    )

    bank_builder bank1(
        angle: -vee / 2.0,
        phasing: vee + rot360,
        ignition: ignition,
        intake: intake,
        exhaust: exh.ex1,
        pls: exh,
        rj0: rj0, rj1: rj1, rj2: rj2, rj3: rj3, rj4: rj4, rj5: rj5
    )

    rod_journal rj0(angle: rot * 0 + rot90 + vee / 2)
    rod_journal rj1(angle: rot * 8 + rot90 + vee / 2)
    rod_journal rj2(angle: rot * 4 + rot90 + vee / 2)
    rod_journal rj3(angle: rot * 10 + rot90 + vee / 2)
    rod_journal rj4(angle: rot * 2 + rot90 + vee / 2)
    rod_journal rj5(angle: rot * 6 + rot90 + vee / 2)
    
    c0
        .add_rod_journal(rj0)
        .add_rod_journal(rj1)
        .add_rod_journal(rj2)
        .add_rod_journal(rj3)
        .add_rod_journal(rj4)
        .add_rod_journal(rj5)

    intake intake(
        plenum_volume: 4.8 * units.L,
        plenum_cross_section_area: 76.8 * units.cm2,
        runner_length: 15.8 * units.cm,
        intake_flow_rate: k_carb(578.6),
        idle_flow_rate: k_carb(0.03),
        idle_throttle_plate_position: 0.9965,
        runner_flow_rate: k_carb(96.4),
        velocity_decay: 0.05
    )
 
    engine
        .add_crankshaft(c0)
        .add_cylinder_bank(bank0.bank)
        .add_cylinder_bank(bank1.bank)
        .add_ignition_module(ignition)

    function timing_curve(400.0 * units.rpm)
    timing_curve
        .add_sample(0.0 * units.rpm, 4.0 * units.deg)
        .add_sample(400.0 * units.rpm, 6.8 * units.deg)
        .add_sample(800.0 * units.rpm, 16.0 * units.deg)
        .add_sample(1200.0 * units.rpm, 22.7 * units.deg)
        .add_sample(1600.0 * units.rpm, 27.5 * units.deg)
        .add_sample(2000.0 * units.rpm, 31.0 * units.deg)
        .add_sample(2400.0 * units.rpm, 33.5 * units.deg)
        .add_sample(2800.0 * units.rpm, 35.3 * units.deg)
        .add_sample(3200.0 * units.rpm, 36.0 * units.deg)
        .add_sample(3600.0 * units.rpm, 36.0 * units.deg)
        .add_sample(4000.0 * units.rpm, 36.0 * units.deg)
        .add_sample(4400.0 * units.rpm, 36.0 * units.deg)
        .add_sample(4800.0 * units.rpm, 36.0 * units.deg)
        .add_sample(5200.0 * units.rpm, 36.0 * units.deg)
        .add_sample(5600.0 * units.rpm, 36.0 * units.deg)
        .add_sample(6000.0 * units.rpm, 36.0 * units.deg)
        .add_sample(6400.0 * units.rpm, -15.0 * units.deg)
        .add_sample(6800.0 * units.rpm, -60.0 * units.deg)
}

private node exh_setup_0 {
    output ex0: _ex0;
    output ex1: _ex1;

    output pl0: _pl0;
    output pl1: _pl1;
    output pl2: _pl2;
    output pl3: _pl3;
    output pl4: _pl4;
    output pl5: _pl5;

    label spacing_factor(1.1)
    label flange_density(1.0 * exhaust_delay_coeff)
    label collector_cross_section_area(17.6)

    label exhaust_pipe_length_0(405.0) // cm
    label exhaust_volume_0(collector_cross_section_area * exhaust_pipe_length_0 / 100.0) // Litres

    label exhaust_pipe_length_1(400.0) // cm
    label exhaust_volume_1(collector_cross_section_area * exhaust_pipe_length_0 / 100.0) // Litres

    float _pl0 ((3.5 * bore * spacing_factor / flange_density) * units.mm)
    float _pl1 ((2.5 * bore * spacing_factor / flange_density) * units.mm)
    float _pl2 ((1.5 * bore * spacing_factor / flange_density) * units.mm)
    float _pl3 ((1.0 * bore * spacing_factor / flange_density) * units.mm)
    float _pl4 ((1.4 * bore * spacing_factor / flange_density) * units.mm)
    float _pl5 ((2.3 * bore * spacing_factor / flange_density) * units.mm)

    exhaust_system_parameters es_params0(
        outlet_flow_rate: k_carb(482.1),
        collector_cross_section_area: 17.6 * units.cm2,
        length: exhaust_pipe_length_0 * units.cm,
        primary_tube_length: 26.9 * units.cm,
        primary_flow_rate: k_carb(160.7),
        velocity_decay: 1.0,
        volume: (exhaust_volume_0 + 10.0) * units.L
    )

    exhaust_system_parameters es_params1(
        outlet_flow_rate: k_carb(482.1),
        collector_cross_section_area: 17.6 * units.cm2,
        length: exhaust_pipe_length_1 * units.cm,
        primary_tube_length: 26.9 * units.cm,
        primary_flow_rate: k_carb(160.7),
        velocity_decay: 1.0,
        volume: (exhaust_volume_1 + 10.0) * units.L
    )

    exhaust_system _ex0(es_params0, impulse_response: impulse_response(filename: "../../impulse_responses/smooth_42.wav", volume: 0.001))
    exhaust_system _ex1(es_params1, impulse_response: impulse_response(filename: "../../impulse_responses/smooth_42.wav", volume: 0.001))
}

// 2000 CL600
public node veh {
    alias output __out: vehicle;

    label car_mass(2100) // 1955

    vehicle vehicle(
        mass: car_mass * units.kg,
        drag_coefficient: 0.28,
        cross_sectional_area: (1857 * units.mm) * (1398 * units.mm),
        diff_ratio: 2.82,
        tire_radius: (679.0 / 2) * units.mm, // 225/55 R 17 W
        rolling_resistance: 0.012 * car_mass * 9.81
        )
}

// 2000 CL600
private node trn {
    alias output __out:
        transmission(
            max_clutch_torque: 600 * units.Nm,
            max_clutch_flex: 15 * units.deg,
            limit_clutch_flex: true,
            clutch_stiffness: 20 * units.Nm / units.deg,
            clutch_damping: 1.0,
            simulate_flex: true
        )
        .add_gear(3.590)
        .add_gear(2.186)
        .add_gear(1.405)
        .add_gear(1.000)
        .add_gear(0.831);
}

run(
    engine: eng(),
    transmission: trn(),
    vehicle: veh()
)
