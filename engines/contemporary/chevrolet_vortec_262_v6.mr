// Engine Sim V0.1.14A
// Chevrolet Vortec 262 195 @ 4600
// Created by oror 2024

/*
{
  "cam_cfg": {
    "equal_base_radius": true,
    "es_version": "0.1.14a",
    "exhaust_at_lift": 1.016,
    "exhaust_base_mult": 1.75,
    "exhaust_cos": false,
    "exhaust_sigma": 3.0,
    "exhaust_trim": 0.65,
    "exhaust_volume": 0.5,
    "intake_at_lift": 1.016,
    "intake_base_mult": 1.75,
    "intake_cos": false,
    "intake_sigma": 3.0,
    "intake_trim": 0.65,
    "intake_volume": 0.5,
    "lift_significant_fraction": 250.0,
    "ramp_position": 0.0,
    "ramp_steepness": 0.0,
    "resolution": 64,
    "roller_tappet_radius": 0.6
  },
  "flow_cfg": {
    "collector_area_coeff": 0.3,
    "exhaust_flange_dia": null,
    "exhaust_port_dia": null,
    "exhaust_stem_dia": null,
    "intake_port_dia": null,
    "intake_runner_dia_mult": 1.0,
    "intake_stem_dia": null,
    "ir_to_er_ratio": 3.0,
    "max_power_rpm": 4600.0,
    "max_torque_rpm": 2800.0,
    "port_to_valve_area": 0.84,
    "power_factor": 1.0,
    "primary_area_coeff": 1.1,
    "resolution": 32,
    "smoothness": 3.0,
    "valve_to_stem_dia": 5.3
  },
  "ign_cfg": {
    "end_deg": 30,
    "end_rpm": 4000,
    "exp_mode": 2,
    "flywheel": 5,
    "resolution": 16,
    "start_deg": 5,
    "start_rpm": 500
  }
}
*/

import "engine_sim.mr"
import "_fuel_90.mr"

units units()
constants constants()

label bore(101.6) // 4
label stroke(88.392) // 3.48
label compression_ratio(9.2)
label con_rod(144.78) // 5.7
label compression_height(39.37 * units.mm) // 1.55
label intake_valve_diameter(49.276) // 1.94 ; 0.342 stem
label exhaust_valve_diameter(38.1) // 1.5 ; 0.342 stem
label intake_valves(1.0)
label exhaust_valves(1.0)

label cyl(6.0)
label row(2.0)
label cycle(720.0 * units.deg)
label rot90(90.0 * units.deg)
label rot180(180.0 * units.deg)
label rot360(360.0 * units.deg)
label rot(cycle / cyl)
label cyl_base(cyl / row)
label rot_base(cycle / cyl_base)
label vee(90.0 * units.deg)

label comp(vee - rot)

label redline(5000.0) // RPM

// 036584780694
label IVL(10.5156) // .414
label EVL(10.8712) // .428
label IVO(-3.0 * units.deg) //BTDC
label IVC(29.0 * units.deg) //ABDC
label EVO(37.0 * units.deg) //BBDC
label EVC(11.0 * units.deg) //ATDC

label intake_duration(IVO + IVC + rot180)
label exhaust_duration(EVO + EVC + rot180)

label ILC((rot180 - IVO + IVC) / 2.0 + rot360)
label ELC((rot180 - EVO + EVC) / 2.0 + rot180)

label con_rod_mass(675.0) // g
label crank_mass(20.41) // kg
label flywheel_mass(8.0) // kg

label exhaust_delay_coeff(1.5)

// standard
public node variant_0 {
    output s1: 0;
    output s3: 1;
    output s2: 2;
}

public node intake_lobe_profile {
    // lobes.py v0.9f
    alias output __out: _lobe_profile;
    function _lobe_profile(2.292 * units.deg)
        _lobe_profile
        .add_sample(-71.063 * units.deg, 0.000 * units.mm)
        .add_sample(-68.771 * units.deg, 0.051 * units.mm)
        .add_sample(-66.478 * units.deg, 0.137 * units.mm)
        .add_sample(-64.186 * units.deg, 0.250 * units.mm)
        .add_sample(-61.894 * units.deg, 0.395 * units.mm)
        .add_sample(-59.601 * units.deg, 0.558 * units.mm)
        .add_sample(-57.309 * units.deg, 0.738 * units.mm)
        .add_sample(-55.017 * units.deg, 0.937 * units.mm)
        .add_sample(-52.724 * units.deg, 1.168 * units.mm)
        .add_sample(-50.432 * units.deg, 1.428 * units.mm)
        .add_sample(-48.140 * units.deg, 1.715 * units.mm)
        .add_sample(-45.847 * units.deg, 2.031 * units.mm)
        .add_sample(-43.555 * units.deg, 2.393 * units.mm)
        .add_sample(-41.262 * units.deg, 2.801 * units.mm)
        .add_sample(-38.970 * units.deg, 3.250 * units.mm)
        .add_sample(-36.678 * units.deg, 3.741 * units.mm)
        .add_sample(-34.385 * units.deg, 4.276 * units.mm)
        .add_sample(-32.093 * units.deg, 4.853 * units.mm)
        .add_sample(-29.801 * units.deg, 5.464 * units.mm)
        .add_sample(-27.508 * units.deg, 6.095 * units.mm)
        .add_sample(-25.216 * units.deg, 6.724 * units.mm)
        .add_sample(-22.924 * units.deg, 7.321 * units.mm)
        .add_sample(-20.631 * units.deg, 7.862 * units.mm)
        .add_sample(-18.339 * units.deg, 8.420 * units.mm)
        .add_sample(-16.047 * units.deg, 8.887 * units.mm)
        .add_sample(-13.754 * units.deg, 9.294 * units.mm)
        .add_sample(-11.462 * units.deg, 9.668 * units.mm)
        .add_sample(-9.169 * units.deg, 9.933 * units.mm)
        .add_sample(-6.877 * units.deg, 10.184 * units.mm)
        .add_sample(-4.585 * units.deg, 10.352 * units.mm)
        .add_sample(-2.292 * units.deg, 10.436 * units.mm)
        .add_sample(0.000 * units.deg, 10.516 * units.mm)
        .add_sample(2.292 * units.deg, 10.436 * units.mm)
        .add_sample(4.585 * units.deg, 10.352 * units.mm)
        .add_sample(6.877 * units.deg, 10.184 * units.mm)
        .add_sample(9.169 * units.deg, 9.933 * units.mm)
        .add_sample(11.462 * units.deg, 9.668 * units.mm)
        .add_sample(13.754 * units.deg, 9.294 * units.mm)
        .add_sample(16.047 * units.deg, 8.887 * units.mm)
        .add_sample(18.339 * units.deg, 8.420 * units.mm)
        .add_sample(20.631 * units.deg, 7.862 * units.mm)
        .add_sample(22.924 * units.deg, 7.321 * units.mm)
        .add_sample(25.216 * units.deg, 6.724 * units.mm)
        .add_sample(27.508 * units.deg, 6.095 * units.mm)
        .add_sample(29.801 * units.deg, 5.464 * units.mm)
        .add_sample(32.093 * units.deg, 4.853 * units.mm)
        .add_sample(34.385 * units.deg, 4.276 * units.mm)
        .add_sample(36.678 * units.deg, 3.741 * units.mm)
        .add_sample(38.970 * units.deg, 3.250 * units.mm)
        .add_sample(41.262 * units.deg, 2.801 * units.mm)
        .add_sample(43.555 * units.deg, 2.393 * units.mm)
        .add_sample(45.847 * units.deg, 2.031 * units.mm)
        .add_sample(48.140 * units.deg, 1.715 * units.mm)
        .add_sample(50.432 * units.deg, 1.428 * units.mm)
        .add_sample(52.724 * units.deg, 1.168 * units.mm)
        .add_sample(55.017 * units.deg, 0.937 * units.mm)
        .add_sample(57.309 * units.deg, 0.738 * units.mm)
        .add_sample(59.601 * units.deg, 0.558 * units.mm)
        .add_sample(61.894 * units.deg, 0.395 * units.mm)
        .add_sample(64.186 * units.deg, 0.250 * units.mm)
        .add_sample(66.478 * units.deg, 0.137 * units.mm)
        .add_sample(68.771 * units.deg, 0.051 * units.mm)
        .add_sample(71.063 * units.deg, 0.000 * units.mm)
}

public node exhaust_lobe_profile {
    // lobes.py v0.9f
    alias output __out: _lobe_profile;
    function _lobe_profile(2.519 * units.deg)
        _lobe_profile
        .add_sample(-78.077 * units.deg, 0.000 * units.mm)
        .add_sample(-75.558 * units.deg, 0.046 * units.mm)
        .add_sample(-73.040 * units.deg, 0.135 * units.mm)
        .add_sample(-70.521 * units.deg, 0.247 * units.mm)
        .add_sample(-68.002 * units.deg, 0.380 * units.mm)
        .add_sample(-65.484 * units.deg, 0.547 * units.mm)
        .add_sample(-62.965 * units.deg, 0.732 * units.mm)
        .add_sample(-60.447 * units.deg, 0.937 * units.mm)
        .add_sample(-57.928 * units.deg, 1.163 * units.mm)
        .add_sample(-55.409 * units.deg, 1.412 * units.mm)
        .add_sample(-52.891 * units.deg, 1.695 * units.mm)
        .add_sample(-50.372 * units.deg, 2.019 * units.mm)
        .add_sample(-47.853 * units.deg, 2.376 * units.mm)
        .add_sample(-45.335 * units.deg, 2.769 * units.mm)
        .add_sample(-42.816 * units.deg, 3.201 * units.mm)
        .add_sample(-40.298 * units.deg, 3.689 * units.mm)
        .add_sample(-37.779 * units.deg, 4.233 * units.mm)
        .add_sample(-35.260 * units.deg, 4.818 * units.mm)
        .add_sample(-32.742 * units.deg, 5.439 * units.mm)
        .add_sample(-30.223 * units.deg, 6.079 * units.mm)
        .add_sample(-27.705 * units.deg, 6.716 * units.mm)
        .add_sample(-25.186 * units.deg, 7.358 * units.mm)
        .add_sample(-22.667 * units.deg, 7.993 * units.mm)
        .add_sample(-20.149 * units.deg, 8.563 * units.mm)
        .add_sample(-17.630 * units.deg, 9.051 * units.mm)
        .add_sample(-15.112 * units.deg, 9.545 * units.mm)
        .add_sample(-12.593 * units.deg, 9.929 * units.mm)
        .add_sample(-10.074 * units.deg, 10.234 * units.mm)
        .add_sample(-7.556 * units.deg, 10.512 * units.mm)
        .add_sample(-5.037 * units.deg, 10.685 * units.mm)
        .add_sample(-2.519 * units.deg, 10.789 * units.mm)
        .add_sample(0.000 * units.deg, 10.871 * units.mm)
        .add_sample(2.519 * units.deg, 10.789 * units.mm)
        .add_sample(5.037 * units.deg, 10.685 * units.mm)
        .add_sample(7.556 * units.deg, 10.512 * units.mm)
        .add_sample(10.074 * units.deg, 10.234 * units.mm)
        .add_sample(12.593 * units.deg, 9.929 * units.mm)
        .add_sample(15.112 * units.deg, 9.545 * units.mm)
        .add_sample(17.630 * units.deg, 9.051 * units.mm)
        .add_sample(20.149 * units.deg, 8.563 * units.mm)
        .add_sample(22.667 * units.deg, 7.993 * units.mm)
        .add_sample(25.186 * units.deg, 7.358 * units.mm)
        .add_sample(27.705 * units.deg, 6.716 * units.mm)
        .add_sample(30.223 * units.deg, 6.079 * units.mm)
        .add_sample(32.742 * units.deg, 5.439 * units.mm)
        .add_sample(35.260 * units.deg, 4.818 * units.mm)
        .add_sample(37.779 * units.deg, 4.233 * units.mm)
        .add_sample(40.298 * units.deg, 3.689 * units.mm)
        .add_sample(42.816 * units.deg, 3.201 * units.mm)
        .add_sample(45.335 * units.deg, 2.769 * units.mm)
        .add_sample(47.853 * units.deg, 2.376 * units.mm)
        .add_sample(50.372 * units.deg, 2.019 * units.mm)
        .add_sample(52.891 * units.deg, 1.695 * units.mm)
        .add_sample(55.409 * units.deg, 1.412 * units.mm)
        .add_sample(57.928 * units.deg, 1.163 * units.mm)
        .add_sample(60.447 * units.deg, 0.937 * units.mm)
        .add_sample(62.965 * units.deg, 0.732 * units.mm)
        .add_sample(65.484 * units.deg, 0.547 * units.mm)
        .add_sample(68.002 * units.deg, 0.380 * units.mm)
        .add_sample(70.521 * units.deg, 0.247 * units.mm)
        .add_sample(73.040 * units.deg, 0.135 * units.mm)
        .add_sample(75.558 * units.deg, 0.046 * units.mm)
        .add_sample(78.077 * units.deg, 0.000 * units.mm)
}

public node intake_flow {
    alias output __out: _intake_flow;
    function _intake_flow(0.339 * units.mm)
    _intake_flow
        .add_flow_sample(0.000, 0.0)
        .add_flow_sample(0.339, 11.4)
        .add_flow_sample(0.678, 22.6)
        .add_flow_sample(1.018, 33.3)
        .add_flow_sample(1.357, 43.5)
        .add_flow_sample(1.696, 53.1)
        .add_flow_sample(2.035, 62.2)
        .add_flow_sample(2.374, 70.8)
        .add_flow_sample(2.714, 78.9)
        .add_flow_sample(3.053, 86.7)
        .add_flow_sample(3.392, 94.2)
        .add_flow_sample(3.731, 101.5)
        .add_flow_sample(4.071, 108.5)
        .add_flow_sample(4.410, 115.3)
        .add_flow_sample(4.749, 121.9)
        .add_flow_sample(5.088, 128.3)
        .add_flow_sample(5.427, 134.6)
        .add_flow_sample(5.767, 140.8)
        .add_flow_sample(6.106, 146.7)
        .add_flow_sample(6.445, 152.6)
        .add_flow_sample(6.784, 158.3)
        .add_flow_sample(7.123, 164.0)
        .add_flow_sample(7.463, 169.5)
        .add_flow_sample(7.802, 174.9)
        .add_flow_sample(8.141, 180.2)
        .add_flow_sample(8.480, 185.4)
        .add_flow_sample(8.820, 190.5)
        .add_flow_sample(9.159, 195.6)
        .add_flow_sample(9.498, 200.5)
        .add_flow_sample(9.837, 205.4)
        .add_flow_sample(10.176, 210.3)
        .add_flow_sample(10.516, 215.2)
}

public node exhaust_flow {
    alias output __out: _exhaust_flow;
    function _exhaust_flow(0.351 * units.mm)
    _exhaust_flow
        .add_flow_sample(0.000, 0.0)
        .add_flow_sample(0.351, 10.0)
        .add_flow_sample(0.701, 19.8)
        .add_flow_sample(1.052, 29.2)
        .add_flow_sample(1.403, 38.1)
        .add_flow_sample(1.753, 46.4)
        .add_flow_sample(2.104, 54.3)
        .add_flow_sample(2.455, 61.6)
        .add_flow_sample(2.805, 68.6)
        .add_flow_sample(3.156, 75.3)
        .add_flow_sample(3.507, 81.7)
        .add_flow_sample(3.858, 87.8)
        .add_flow_sample(4.208, 93.7)
        .add_flow_sample(4.559, 99.4)
        .add_flow_sample(4.910, 105.0)
        .add_flow_sample(5.260, 110.4)
        .add_flow_sample(5.611, 115.6)
        .add_flow_sample(5.962, 120.7)
        .add_flow_sample(6.312, 125.6)
        .add_flow_sample(6.663, 130.3)
        .add_flow_sample(7.014, 134.8)
        .add_flow_sample(7.364, 139.0)
        .add_flow_sample(7.715, 142.8)
        .add_flow_sample(8.066, 146.1)
        .add_flow_sample(8.416, 148.9)
        .add_flow_sample(8.767, 151.1)
        .add_flow_sample(9.118, 152.7)
        .add_flow_sample(9.468, 153.9)
        .add_flow_sample(9.819, 154.7)
        .add_flow_sample(10.170, 155.3)
        .add_flow_sample(10.521, 155.6)
        .add_flow_sample(10.871, 155.9)
}

intake_lobe_profile intake_lobe_profile()
exhaust_lobe_profile exhaust_lobe_profile()

intake_flow intake_flow()
exhaust_flow exhaust_flow()

private node add_lobes {
    input base;
    input shift: 0.0;
    input var;
    input this;
    alias output __out: this;

    this.add_lobe(base + var.s1 * rot_base + shift)
    this.add_lobe(base + var.s2 * rot_base + shift)
    this.add_lobe(base + var.s3 * rot_base + shift)
}

public node bank_builder {
    input var;
    input crankshaft;
    input incline;
    input phasing;
    input ignition;
    input intake;
    input exhaust;
    input primaries;
    input flip: false;

    output bank: _b0;

    ignition_wire _wire1()
    ignition_wire _wire2()
    ignition_wire _wire3()

    piston_parameters piston_params(
        mass: 545 * units.g,
        blowby: k_28inH2O(0.032),
        compression_height: compression_height,
        wrist_pin_position: 0.0,
        displacement: 0.0
    )

    cylinder_bank_parameters bank_params(
        bore: bore * units.mm,
        deck_height: (con_rod + stroke / 2.0) * units.mm + compression_height
    )

    connecting_rod_parameters cr_params(
        mass: con_rod_mass * units.g,
        moment_of_inertia: rod_moment_of_inertia(
            mass: con_rod_mass * units.g,
            length: con_rod * units.mm
        ),
        center_of_mass: 0.0,
        length: con_rod * units.mm
    )

    camshaft _intake_cam_0(base_radius: 18.7 * units.mm, lobe_profile: intake_lobe_profile)
    camshaft _exhaust_cam_0(base_radius: 18.7 * units.mm, lobe_profile: exhaust_lobe_profile)

    _intake_cam_0.add_lobes(ILC, phasing, var)
    _exhaust_cam_0.add_lobes(ELC, phasing, var)

    ignition.connect_wire(_wire1, rot_base * var.s1 + phasing)
    ignition.connect_wire(_wire2, rot_base * var.s2 + phasing)
    ignition.connect_wire(_wire3, rot_base * var.s3 + phasing)

    cylinder_bank _b0(bank_params, angle: incline)

    _b0
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj0,
            intake: intake,
            exhaust_system: exhaust,
            primary_length: primaries.pl0,
            ignition_wire: _wire1
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj1,
            intake: intake,
            exhaust_system: exhaust,
            primary_length: primaries.pl1,
            ignition_wire: _wire2
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj2,
            intake: intake,
            exhaust_system: exhaust,
            primary_length: primaries.pl2,
            ignition_wire: _wire3
        )

    _b0.set_cylinder_head(
        head(
            intake_camshaft: _intake_cam_0,
            exhaust_camshaft: _exhaust_cam_0,
            flip_display: flip
        )
    )
}

private node add_flow_sample {
    input lift;
    input flow;
    input this;
    alias output __out: this;

    this.add_sample(lift * units.mm, k_28inH2O(flow))
}

public node head {
    // port_flow.py v1.5
    // intake port area: 15.3 cm²; saturated lift: 10.81 mm
    // exhaust port area: 9.2 cm²; saturated lift: 8.36 mm
    // cylinder volume: 716.6 cm³ (43.7 CI); engine volume: 4.300 L (262.4 CI)
    // chamber volume: 87.4 cm³ (5.3 CI)
    // 16 harmonic intake runner length: 21.0 cm; diameter: 4.3 cm
    // primary length: 36.0 cm, area: 10.1 cm², diameter: 3.6 cm
    // collector diameter: 4.8 cm, area: 18.2 cm²

    input intake_camshaft;
    input exhaust_camshaft;
    input flip_display: false;
    
    alias output __out: head;
    generic_cylinder_head head(
        chamber_volume: 87.393 * units.cc,
        intake_runner_volume: 309.5 * units.cc,
        intake_runner_cross_section_area: 14.7 * units.cm2,
        exhaust_runner_volume: 103.2 * units.cc,
        exhaust_runner_cross_section_area: 10.1 * units.cm2,

        intake_port_flow: intake_flow,
        exhaust_port_flow: exhaust_flow,
        valvetrain: standard_valvetrain(
            intake_camshaft: intake_camshaft,
            exhaust_camshaft: exhaust_camshaft
        ),
        flip_display: flip_display
    )
}

public node crank {
    input phasing;
    input var;

    output c: _c;
    output rj0: _rj0;
    output rj1: _rj1;
    output rj2: _rj2;
    output rj0_1: _rj0_1;
    output rj1_1: _rj1_1;
    output rj2_1: _rj2_1;

    label crank_moment(disk_moment_of_inertia(mass: crank_mass * units.kg, radius: (stroke / 2.0) * units.mm))
    label flywheel_moment(disk_moment_of_inertia(mass: flywheel_mass * units.kg, radius: (stroke * 3.5) * units.mm))
    label other_moment(disk_moment_of_inertia(mass: 1 * units.kg, radius: 1.0 * units.cm))

    crankshaft _c(
        throw: (stroke / 2.0) * units.mm,
        flywheel_mass: flywheel_mass * units.kg,
        mass: crank_mass * units.kg,
        friction_torque: 6.4 * units.Nm,
        moment_of_inertia: crank_moment + flywheel_moment + other_moment,
        tdc: 0.0
    )

    rod_journal _rj0(angle: rot_base * var.s1 + rot90 + vee / 2)
    rod_journal _rj1(angle: rot_base * var.s2 + rot90 + vee / 2)
    rod_journal _rj2(angle: rot_base * var.s3 + rot90 + vee / 2)

    rod_journal _rj0_1(angle: rot_base * var.s1 + rot90 + vee / 2 - comp)
    rod_journal _rj1_1(angle: rot_base * var.s2 + rot90 + vee / 2 - comp)
    rod_journal _rj2_1(angle: rot_base * var.s3 + rot90 + vee / 2 - comp)

    _c
        .add_rod_journal(_rj0)
        .add_rod_journal(_rj0_1)
        .add_rod_journal(_rj1)
        .add_rod_journal(_rj1_1)
        .add_rod_journal(_rj2)
        .add_rod_journal(_rj2_1)
}

public node c0_1 {
    input crank;
    output rj0: crank.rj0;
    output rj1: crank.rj1;
    output rj2: crank.rj2;
}

public node c1_1 {
    input crank;
    output rj0: crank.rj0_1;
    output rj1: crank.rj1_1;
    output rj2: crank.rj2_1;
}

public node eng {
    alias output __out: engine;

    engine engine(
        name: "Chevrolet Vortec 262",
        starter_torque: 150 * units.Nm,
        starter_speed: 200 * units.rpm,
        redline: redline * units.rpm,
        fuel: s90(),
        throttle_gamma: 1.5,
        jitter: 1.0,
        noise: 0.1,
        hf_gain: 0.01,
        simulation_frequency: 8500,
        dyno_min_speed: 750 * units.rpm
    )

    variant_0 variant()

    crank c0(vee / 2 + rot90, variant)

    bank_builder bank0(
        var: variant,
        crankshaft: c0_1(c0),
        incline: vee / 2,
        phasing: 0,
        ignition: ignition,
        intake: intake,
        exhaust: exhaust0,
        primaries: em_0(),
        flip: true
    )

    bank_builder bank1(
        var: variant,
        crankshaft: c1_1(c0),
        incline: -vee / 2,
        phasing: vee - comp,
        ignition: ignition,
        intake: intake,
        exhaust: exhaust0,
        primaries: em_1()
    )

    ignition_module ignition(timing_curve: timing_curve, rev_limit: redline * 1.05 * units.rpm, limiter_duration: 0.13)

    label collector_cross_section_area(18.2)

    label exhaust_pipe_length_0(380.0) // cm
    label exhaust_volume_0(collector_cross_section_area * exhaust_pipe_length_0 / 100.0) // Litres

    intake intake(
        plenum_volume: 3.0 * units.L,
        plenum_cross_section_area: 44.1 * units.cm2,
        runner_length: 21.0 * units.cm,
        intake_flow_rate: k_carb(358.3),
        idle_flow_rate: k_carb(0.01),
        idle_throttle_plate_position: 0.998,
        runner_flow_rate: k_carb(119.4),
        velocity_decay: 0.03
    )
    
    exhaust_system_parameters es_params0(
        outlet_flow_rate: k_carb(597.2),
        collector_cross_section_area: 18.2 * units.cm2,
        length: exhaust_pipe_length_0 * units.cm,
        primary_tube_length: 36.0 * units.cm,
        primary_flow_rate: k_carb(199.1),
        velocity_decay: 1.0,
        volume: (exhaust_volume_0 + 2.0) * units.L
    )
    
    exhaust_system exhaust0(es_params0, impulse_response: impulse_response(filename: "../../impulse_responses/smooth_58.wav", volume: 0.001))

    engine
        .add_crankshaft(c0.c)
        .add_cylinder_bank(bank0.bank)
        .add_cylinder_bank(bank1.bank)
        .add_ignition_module(ignition)

    function timing_curve(333.3 * units.rpm)
    timing_curve
        .add_sample(0.0 * units.rpm, 5.0 * units.deg)
        .add_sample(333.3 * units.rpm, 5.0 * units.deg)
        .add_sample(666.7 * units.rpm, 8.1 * units.deg)
        .add_sample(1000.0 * units.rpm, 13.5 * units.deg)
        .add_sample(1333.3 * units.rpm, 18.0 * units.deg)
        .add_sample(1666.7 * units.rpm, 21.7 * units.deg)
        .add_sample(2000.0 * units.rpm, 24.8 * units.deg)
        .add_sample(2333.3 * units.rpm, 27.4 * units.deg)
        .add_sample(2666.7 * units.rpm, 29.5 * units.deg)
        .add_sample(3000.0 * units.rpm, 31.3 * units.deg)
        .add_sample(3333.3 * units.rpm, 32.8 * units.deg)
        .add_sample(3666.7 * units.rpm, 34.0 * units.deg)
        .add_sample(4000.0 * units.rpm, 35.0 * units.deg)
        .add_sample(4333.3 * units.rpm, 35.0 * units.deg)
        .add_sample(4666.7 * units.rpm, 35.0 * units.deg)
        .add_sample(5000.0 * units.rpm, 35.0 * units.deg)
        .add_sample(5333.3 * units.rpm, -15.0 * units.deg)
        .add_sample(5666.7 * units.rpm, -45.0 * units.deg)
}

label spacing_factor(1.15)
label flange_density(1.0 * exhaust_delay_coeff)
label em_unit(bore * spacing_factor / flange_density)
label sh(30.0 * units.cm)

private node em_0{
    output pl0: (3.2 * em_unit) * units.mm;
    output pl1: (2.5 * em_unit) * units.mm;
    output pl2: (1.2 * em_unit) * units.mm;
}

private node em_1{
    output pl0: (3.2 * em_unit) * units.mm + sh;
    output pl1: (1.5 * em_unit) * units.mm + sh;
    output pl2: (1.0 * em_unit) * units.mm + sh;
}

// 1988 750iL
public node veh {
    alias output __out: vehicle;

    label car_mass(1960) // 1860 empty

    vehicle vehicle(
        mass: car_mass * units.kg,
        drag_coefficient: 0.34,
        cross_sectional_area: (1845  * units.mm) * (1400 * units.mm),
        diff_ratio: 3.15,
        tire_radius: (653.0 / 2) * units.mm, // 225/60 VR 15
        rolling_resistance: 0.012 * car_mass * 9.81
        )
}

// ZF 4HP24E
private node trn {
    alias output __out:
        transmission(
            max_clutch_torque: 675 * units.Nm,
            max_clutch_flex: 30 * units.deg,
            limit_clutch_flex: true,
            clutch_stiffness: 20 * units.Nm / units.deg,
            clutch_damping: 1.0,
            simulate_flex: true
        )
        .add_gear(2.479)
        .add_gear(1.479)
        .add_gear(1.000)
        .add_gear(0.728);
}

run(
    engine: eng(),
    transmission: trn(),
    vehicle: veh()
)
