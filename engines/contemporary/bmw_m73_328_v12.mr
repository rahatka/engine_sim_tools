// Engine Sim V0.1.14A
// M73B54 322 @ 5000 ; 450 Nm @ 4100
// Created by oror 2024

/*
{
  "cam_cfg": {
    "equal_base_radius": false,
    "exhaust_at_lift": 0.508,
    "exhaust_base_mult": 1.75,
    "exhaust_cos": false,
    "exhaust_sigma": 3.0,
    "exhaust_trim": 0.65,
    "exhaust_volume": 0.5,
    "intake_at_lift": 0.508,
    "intake_base_mult": 1.75,
    "intake_cos": false,
    "intake_sigma": 3.0,
    "intake_trim": 0.65,
    "intake_volume": 0.5,
    "lift_significant_fraction": 250.0,
    "ramp_position": 0.0,
    "ramp_steepness": 0.0,
    "resolution": 64,
    "roller_tappet_radius": 0.7
  },
  "flow_cfg": {
    "collector_area_coeff": 0.35,
    "exhaust_flange_dia": null,
    "exhaust_port_dia": null,
    "exhaust_stem_dia": null,
    "intake_port_dia": null,
    "intake_runner_dia_mult": 1.04,
    "intake_stem_dia": null,
    "ir_to_er_ratio": 3.0,
    "max_power_rpm": 5000.0,
    "max_torque_rpm": 4100.0,
    "port_to_valve_area": 0.85,
    "power_factor": 0.9,
    "primary_area_coeff": 1.1,
    "resolution": 32,
    "smoothness": 3.0,
    "valve_to_stem_dia": 5.3
  },
  "ign_cfg": {
    "end_deg": 35,
    "end_rpm": 3500,
    "exp_mode": 2,
    "flywheel": 5,
    "resolution": 16,
    "start_deg": 5,
    "start_rpm": 500
  }
}
*/

import "engine_sim.mr"
import "../../fuel/90.mr"

units units()
constants constants()

label bore(85.0)
label stroke(79.0)
label compression_ratio(10.0)
label con_rod(135.0)
label compression_height(34.15 * units.mm)
label intake_valve_diameter(42.0) // 6.94 stem
label exhaust_valve_diameter(36.0) // 6.94 stem
label intake_valves(1.0)
label exhaust_valves(1.0)

label cyl(12.0)
label row(2.0)
label cycle(720.0 * units.deg)
label rot90(90.0 * units.deg)
label rot180(180.0 * units.deg)
label rot360(360.0 * units.deg)
label rot(cycle / cyl)
label cyl_base(cyl / row)
label rot_base(cycle / cyl_base)
label vee(60.0 * units.deg)

label redline(6000.0) // RPM

// stock M70 240 / 240 110
label IVL(10.3)
label EVL(10.3)
label IVO(14.0 * units.deg) //BTDC
label IVC(46.0 * units.deg) //ABDC
label EVO(54.0 * units.deg) //BBDC
label EVC(6.0 * units.deg) //ATDC

label intake_duration(IVO + IVC + rot180)
label exhaust_duration(EVO + EVC + rot180)

label ILC((rot180 - IVO + IVC) / 2.0 + rot360)
label ELC((rot180 - EVO + EVC) / 2.0 + rot180)

label con_rod_mass(499.0) // g
label crank_mass(25.0) // kg
label flywheel_mass(8.0) // kg

label exhaust_delay_coeff(1.5)

// standard
public node variant_0 {
    output s1: 0;
    output s5: 1;
    output s3: 2;
    output s6: 3;
    output s2: 4;
    output s4: 5;
}

public node intake_lobe_profile {
    // lobes.py v1.0 | 240.0 240.0 LSA 110.00 ADV 4.00 OVL 20.0
    alias output __out: _lobe_profile;
    function _lobe_profile(2.372 * units.deg)
        _lobe_profile
        .add_sample(-73.544 * units.deg, 0.000 * units.mm)
        .add_sample(-71.172 * units.deg, 0.052 * units.mm)
        .add_sample(-68.799 * units.deg, 0.136 * units.mm)
        .add_sample(-66.427 * units.deg, 0.252 * units.mm)
        .add_sample(-64.055 * units.deg, 0.394 * units.mm)
        .add_sample(-61.682 * units.deg, 0.553 * units.mm)
        .add_sample(-59.310 * units.deg, 0.730 * units.mm)
        .add_sample(-56.938 * units.deg, 0.929 * units.mm)
        .add_sample(-54.565 * units.deg, 1.161 * units.mm)
        .add_sample(-52.193 * units.deg, 1.417 * units.mm)
        .add_sample(-49.820 * units.deg, 1.699 * units.mm)
        .add_sample(-47.448 * units.deg, 2.017 * units.mm)
        .add_sample(-45.076 * units.deg, 2.382 * units.mm)
        .add_sample(-42.703 * units.deg, 2.784 * units.mm)
        .add_sample(-40.331 * units.deg, 3.225 * units.mm)
        .add_sample(-37.958 * units.deg, 3.716 * units.mm)
        .add_sample(-35.586 * units.deg, 4.260 * units.mm)
        .add_sample(-33.214 * units.deg, 4.835 * units.mm)
        .add_sample(-30.841 * units.deg, 5.428 * units.mm)
        .add_sample(-28.469 * units.deg, 6.026 * units.mm)
        .add_sample(-26.096 * units.deg, 6.645 * units.mm)
        .add_sample(-23.724 * units.deg, 7.233 * units.mm)
        .add_sample(-21.352 * units.deg, 7.761 * units.mm)
        .add_sample(-18.979 * units.deg, 8.281 * units.mm)
        .add_sample(-16.607 * units.deg, 8.740 * units.mm)
        .add_sample(-14.234 * units.deg, 9.119 * units.mm)
        .add_sample(-11.862 * units.deg, 9.485 * units.mm)
        .add_sample(-9.490 * units.deg, 9.745 * units.mm)
        .add_sample(-7.117 * units.deg, 9.979 * units.mm)
        .add_sample(-4.745 * units.deg, 10.143 * units.mm)
        .add_sample(-2.372 * units.deg, 10.222 * units.mm)
        .add_sample(0.000 * units.deg, 10.300 * units.mm)
        .add_sample(2.372 * units.deg, 10.222 * units.mm)
        .add_sample(4.745 * units.deg, 10.143 * units.mm)
        .add_sample(7.117 * units.deg, 9.979 * units.mm)
        .add_sample(9.490 * units.deg, 9.745 * units.mm)
        .add_sample(11.862 * units.deg, 9.485 * units.mm)
        .add_sample(14.234 * units.deg, 9.119 * units.mm)
        .add_sample(16.607 * units.deg, 8.740 * units.mm)
        .add_sample(18.979 * units.deg, 8.281 * units.mm)
        .add_sample(21.352 * units.deg, 7.761 * units.mm)
        .add_sample(23.724 * units.deg, 7.233 * units.mm)
        .add_sample(26.096 * units.deg, 6.645 * units.mm)
        .add_sample(28.469 * units.deg, 6.026 * units.mm)
        .add_sample(30.841 * units.deg, 5.428 * units.mm)
        .add_sample(33.214 * units.deg, 4.835 * units.mm)
        .add_sample(35.586 * units.deg, 4.260 * units.mm)
        .add_sample(37.958 * units.deg, 3.716 * units.mm)
        .add_sample(40.331 * units.deg, 3.225 * units.mm)
        .add_sample(42.703 * units.deg, 2.784 * units.mm)
        .add_sample(45.076 * units.deg, 2.382 * units.mm)
        .add_sample(47.448 * units.deg, 2.017 * units.mm)
        .add_sample(49.820 * units.deg, 1.699 * units.mm)
        .add_sample(52.193 * units.deg, 1.417 * units.mm)
        .add_sample(54.565 * units.deg, 1.161 * units.mm)
        .add_sample(56.938 * units.deg, 0.929 * units.mm)
        .add_sample(59.310 * units.deg, 0.730 * units.mm)
        .add_sample(61.682 * units.deg, 0.553 * units.mm)
        .add_sample(64.055 * units.deg, 0.394 * units.mm)
        .add_sample(66.427 * units.deg, 0.252 * units.mm)
        .add_sample(68.799 * units.deg, 0.136 * units.mm)
        .add_sample(71.172 * units.deg, 0.052 * units.mm)
        .add_sample(73.544 * units.deg, 0.000 * units.mm)
}

public node exhaust_lobe_profile {
    // lobes.py v1.0 | 240.0 240.0 LSA 110.00 ADV 4.00 OVL 20.0
    alias output __out: _lobe_profile;
    function _lobe_profile(2.372 * units.deg)
        _lobe_profile
        .add_sample(-73.544 * units.deg, 0.000 * units.mm)
        .add_sample(-71.172 * units.deg, 0.052 * units.mm)
        .add_sample(-68.799 * units.deg, 0.136 * units.mm)
        .add_sample(-66.427 * units.deg, 0.252 * units.mm)
        .add_sample(-64.055 * units.deg, 0.394 * units.mm)
        .add_sample(-61.682 * units.deg, 0.553 * units.mm)
        .add_sample(-59.310 * units.deg, 0.730 * units.mm)
        .add_sample(-56.938 * units.deg, 0.929 * units.mm)
        .add_sample(-54.565 * units.deg, 1.161 * units.mm)
        .add_sample(-52.193 * units.deg, 1.417 * units.mm)
        .add_sample(-49.820 * units.deg, 1.699 * units.mm)
        .add_sample(-47.448 * units.deg, 2.017 * units.mm)
        .add_sample(-45.076 * units.deg, 2.382 * units.mm)
        .add_sample(-42.703 * units.deg, 2.784 * units.mm)
        .add_sample(-40.331 * units.deg, 3.225 * units.mm)
        .add_sample(-37.958 * units.deg, 3.716 * units.mm)
        .add_sample(-35.586 * units.deg, 4.260 * units.mm)
        .add_sample(-33.214 * units.deg, 4.835 * units.mm)
        .add_sample(-30.841 * units.deg, 5.428 * units.mm)
        .add_sample(-28.469 * units.deg, 6.026 * units.mm)
        .add_sample(-26.096 * units.deg, 6.645 * units.mm)
        .add_sample(-23.724 * units.deg, 7.233 * units.mm)
        .add_sample(-21.352 * units.deg, 7.761 * units.mm)
        .add_sample(-18.979 * units.deg, 8.281 * units.mm)
        .add_sample(-16.607 * units.deg, 8.740 * units.mm)
        .add_sample(-14.234 * units.deg, 9.119 * units.mm)
        .add_sample(-11.862 * units.deg, 9.485 * units.mm)
        .add_sample(-9.490 * units.deg, 9.745 * units.mm)
        .add_sample(-7.117 * units.deg, 9.979 * units.mm)
        .add_sample(-4.745 * units.deg, 10.143 * units.mm)
        .add_sample(-2.372 * units.deg, 10.222 * units.mm)
        .add_sample(0.000 * units.deg, 10.300 * units.mm)
        .add_sample(2.372 * units.deg, 10.222 * units.mm)
        .add_sample(4.745 * units.deg, 10.143 * units.mm)
        .add_sample(7.117 * units.deg, 9.979 * units.mm)
        .add_sample(9.490 * units.deg, 9.745 * units.mm)
        .add_sample(11.862 * units.deg, 9.485 * units.mm)
        .add_sample(14.234 * units.deg, 9.119 * units.mm)
        .add_sample(16.607 * units.deg, 8.740 * units.mm)
        .add_sample(18.979 * units.deg, 8.281 * units.mm)
        .add_sample(21.352 * units.deg, 7.761 * units.mm)
        .add_sample(23.724 * units.deg, 7.233 * units.mm)
        .add_sample(26.096 * units.deg, 6.645 * units.mm)
        .add_sample(28.469 * units.deg, 6.026 * units.mm)
        .add_sample(30.841 * units.deg, 5.428 * units.mm)
        .add_sample(33.214 * units.deg, 4.835 * units.mm)
        .add_sample(35.586 * units.deg, 4.260 * units.mm)
        .add_sample(37.958 * units.deg, 3.716 * units.mm)
        .add_sample(40.331 * units.deg, 3.225 * units.mm)
        .add_sample(42.703 * units.deg, 2.784 * units.mm)
        .add_sample(45.076 * units.deg, 2.382 * units.mm)
        .add_sample(47.448 * units.deg, 2.017 * units.mm)
        .add_sample(49.820 * units.deg, 1.699 * units.mm)
        .add_sample(52.193 * units.deg, 1.417 * units.mm)
        .add_sample(54.565 * units.deg, 1.161 * units.mm)
        .add_sample(56.938 * units.deg, 0.929 * units.mm)
        .add_sample(59.310 * units.deg, 0.730 * units.mm)
        .add_sample(61.682 * units.deg, 0.553 * units.mm)
        .add_sample(64.055 * units.deg, 0.394 * units.mm)
        .add_sample(66.427 * units.deg, 0.252 * units.mm)
        .add_sample(68.799 * units.deg, 0.136 * units.mm)
        .add_sample(71.172 * units.deg, 0.052 * units.mm)
        .add_sample(73.544 * units.deg, 0.000 * units.mm)
}

public node intake_flow {
    alias output __out: _intake_flow;
    function _intake_flow(0.332 * units.mm)
    _intake_flow
        .add_flow_sample(0.000, 0.0)
        .add_flow_sample(0.332, 9.3)
        .add_flow_sample(0.665, 18.5)
        .add_flow_sample(0.997, 27.2)
        .add_flow_sample(1.329, 35.5)
        .add_flow_sample(1.661, 43.3)
        .add_flow_sample(1.994, 50.7)
        .add_flow_sample(2.326, 57.6)
        .add_flow_sample(2.658, 64.2)
        .add_flow_sample(2.990, 70.5)
        .add_flow_sample(3.323, 76.6)
        .add_flow_sample(3.655, 82.4)
        .add_flow_sample(3.987, 88.0)
        .add_flow_sample(4.319, 93.5)
        .add_flow_sample(4.652, 98.8)
        .add_flow_sample(4.984, 104.0)
        .add_flow_sample(5.316, 109.0)
        .add_flow_sample(5.648, 113.9)
        .add_flow_sample(5.981, 118.6)
        .add_flow_sample(6.313, 123.3)
        .add_flow_sample(6.645, 127.9)
        .add_flow_sample(6.977, 132.3)
        .add_flow_sample(7.310, 136.6)
        .add_flow_sample(7.642, 140.8)
        .add_flow_sample(7.974, 144.8)
        .add_flow_sample(8.306, 148.6)
        .add_flow_sample(8.639, 152.1)
        .add_flow_sample(8.971, 155.2)
        .add_flow_sample(9.303, 157.9)
        .add_flow_sample(9.635, 160.3)
        .add_flow_sample(9.968, 162.3)
        .add_flow_sample(10.300, 164.0)
}

public node exhaust_flow {
    alias output __out: _exhaust_flow;
    function _exhaust_flow(0.332 * units.mm)
    _exhaust_flow
        .add_flow_sample(0.000, 0.0)
        .add_flow_sample(0.332, 9.0)
        .add_flow_sample(0.665, 17.9)
        .add_flow_sample(0.997, 26.3)
        .add_flow_sample(1.329, 34.3)
        .add_flow_sample(1.661, 41.9)
        .add_flow_sample(1.994, 48.9)
        .add_flow_sample(2.326, 55.6)
        .add_flow_sample(2.658, 61.9)
        .add_flow_sample(2.990, 67.9)
        .add_flow_sample(3.323, 73.6)
        .add_flow_sample(3.655, 79.1)
        .add_flow_sample(3.987, 84.5)
        .add_flow_sample(4.319, 89.6)
        .add_flow_sample(4.652, 94.6)
        .add_flow_sample(4.984, 99.5)
        .add_flow_sample(5.316, 104.2)
        .add_flow_sample(5.648, 108.8)
        .add_flow_sample(5.981, 113.2)
        .add_flow_sample(6.313, 117.5)
        .add_flow_sample(6.645, 121.6)
        .add_flow_sample(6.977, 125.4)
        .add_flow_sample(7.310, 128.8)
        .add_flow_sample(7.642, 131.8)
        .add_flow_sample(7.974, 134.3)
        .add_flow_sample(8.306, 136.3)
        .add_flow_sample(8.639, 137.9)
        .add_flow_sample(8.971, 138.9)
        .add_flow_sample(9.303, 139.7)
        .add_flow_sample(9.635, 140.2)
        .add_flow_sample(9.968, 140.5)
        .add_flow_sample(10.300, 140.7)
}

intake_lobe_profile intake_lobe_profile()
exhaust_lobe_profile exhaust_lobe_profile()

intake_flow intake_flow()
exhaust_flow exhaust_flow()

private node add_lobes {
    input base;
    input shift: 0.0;
    input var;
    input this;
    alias output __out: this;

    this.add_lobe(base + var.s1 * rot_base + shift)
    this.add_lobe(base + var.s2 * rot_base + shift)
    this.add_lobe(base + var.s3 * rot_base + shift)
    this.add_lobe(base + var.s4 * rot_base + shift)
    this.add_lobe(base + var.s5 * rot_base + shift)
    this.add_lobe(base + var.s6 * rot_base + shift)
}

public node bank_builder {
    input var;
    input crankshaft;
    input incline;
    input phasing;
    input ignition;
    input intake;
    input exhaust;
    input primaries;
    input sh;
    input flip: false;

    output bank: _b0;

    ignition_wire _wire1()
    ignition_wire _wire2()
    ignition_wire _wire3()
    ignition_wire _wire4()
    ignition_wire _wire5()
    ignition_wire _wire6()

    piston_parameters piston_params(
        mass: 550 * units.g,
        blowby: k_28inH2O(0.027),
        compression_height: compression_height,
        wrist_pin_position: 0.0,
        displacement: 0.0
    )

    cylinder_bank_parameters bank_params(
        bore: bore * units.mm,
        deck_height: (con_rod + stroke / 2.0) * units.mm + compression_height
    )

    connecting_rod_parameters cr_params(
        mass: con_rod_mass * units.g,
        moment_of_inertia: rod_moment_of_inertia(
            mass: con_rod_mass * units.g,
            length: con_rod * units.mm
        ),
        center_of_mass: 0.0,
        length: con_rod * units.mm
    )

    camshaft _intake_cam_0(base_radius: 18.0 * units.mm, lobe_profile: intake_lobe_profile)
    camshaft _exhaust_cam_0(base_radius: 18.0 * units.mm, lobe_profile: exhaust_lobe_profile)

    _intake_cam_0.add_lobes(ILC, phasing, var)
    _exhaust_cam_0.add_lobes(ELC, phasing, var)

    ignition.connect_wire(_wire1, rot_base * var.s1 + phasing)
    ignition.connect_wire(_wire2, rot_base * var.s2 + phasing)
    ignition.connect_wire(_wire3, rot_base * var.s3 + phasing)
    ignition.connect_wire(_wire4, rot_base * var.s4 + phasing)
    ignition.connect_wire(_wire5, rot_base * var.s5 + phasing)
    ignition.connect_wire(_wire6, rot_base * var.s6 + phasing)

    cylinder_bank _b0(bank_params, angle: incline)

    _b0
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj0,
            intake: intake,
            exhaust_system: exhaust,
            primary_length: primaries.pl0 + sh,
            ignition_wire: _wire1
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj1,
            intake: intake,
            exhaust_system: exhaust,
            primary_length: primaries.pl1 + sh,
            ignition_wire: _wire2
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj2,
            intake: intake,
            exhaust_system: exhaust,
            primary_length: primaries.pl2 + sh,
            ignition_wire: _wire3
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj3,
            intake: intake,
            exhaust_system: exhaust,
            primary_length: primaries.pl3 + sh,
            ignition_wire: _wire4
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj4,
            intake: intake,
            exhaust_system: exhaust,
            primary_length: primaries.pl4 + sh,
            ignition_wire: _wire5
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj5,
            intake: intake,
            exhaust_system: exhaust,
            primary_length: primaries.pl5 + sh,
            ignition_wire: _wire6
        )

    _b0.set_cylinder_head(
        head(
            intake_camshaft: _intake_cam_0,
            exhaust_camshaft: _exhaust_cam_0,
            flip_display: flip
        )
    )
}

private node add_flow_sample {
    input lift;
    input flow;
    input this;
    alias output __out: this;

    this.add_sample(lift * units.mm, k_28inH2O(flow))
}

public node head {
    // port_flow.py v1.5
    // intake port area: 11.3 cm²; saturated lift: 9.28 mm
    // exhaust port area: 8.3 cm²; saturated lift: 7.95 mm
    // cylinder volume: 448.3 cm³ (27.36 CI); engine volume: 5.379 L (328.27 CI)
    // chamber volume: 49.8 cm³ (3.04 CI)
    // 16 harmonic intake runner length: 18.1 cm; diameter: 3.7 cm
    // primary length: 24.5 cm, area: 9.1 cm², diameter: 3.4 cm
    // collector diameter: 7.0 cm, area: 38.3 cm²

    input intake_camshaft;
    input exhaust_camshaft;
    input flip_display: false;
    
    alias output __out: head;
    generic_cylinder_head head(
        chamber_volume: 49.810 * units.cc,
        intake_runner_volume: 195.5 * units.cc,
        intake_runner_cross_section_area: 10.8 * units.cm2,
        exhaust_runner_volume: 65.2 * units.cc,
        exhaust_runner_cross_section_area: 9.1 * units.cm2,

        intake_port_flow: intake_flow,
        exhaust_port_flow: exhaust_flow,
        valvetrain: standard_valvetrain(
            intake_camshaft: intake_camshaft,
            exhaust_camshaft: exhaust_camshaft
        ),
        flip_display: flip_display
    )
}

public node crank {
    input phasing;
    input var;

    output c: _c;
    output rj0: _rj0;
    output rj1: _rj1;
    output rj2: _rj2;
    output rj3: _rj3;
    output rj4: _rj4;
    output rj5: _rj5;

    label crank_moment(disk_moment_of_inertia(mass: crank_mass * units.kg, radius: (stroke / 2.0) * units.mm))
    label flywheel_moment(disk_moment_of_inertia(mass: flywheel_mass * units.kg, radius: (stroke * 3.5) * units.mm))
    label other_moment(disk_moment_of_inertia(mass: 1 * units.kg, radius: 1.0 * units.cm))

    crankshaft _c(
        throw: (stroke / 2.0) * units.mm,
        flywheel_mass: flywheel_mass * units.kg,
        mass: crank_mass * units.kg,
        friction_torque: 8.1 * units.Nm,
        moment_of_inertia: crank_moment + flywheel_moment + other_moment,
        tdc: 0.0
    )

    rod_journal _rj0(angle: rot_base * var.s1 + rot90 + vee / 2)
    rod_journal _rj1(angle: rot_base * var.s2 + rot90 + vee / 2)
    rod_journal _rj2(angle: rot_base * var.s3 + rot90 + vee / 2)
    rod_journal _rj3(angle: rot_base * var.s4 + rot90 + vee / 2)
    rod_journal _rj4(angle: rot_base * var.s5 + rot90 + vee / 2)
    rod_journal _rj5(angle: rot_base * var.s6 + rot90 + vee / 2)

    _c
        .add_rod_journal(_rj0)
        .add_rod_journal(_rj1)
        .add_rod_journal(_rj2)
        .add_rod_journal(_rj3)
        .add_rod_journal(_rj4)
        .add_rod_journal(_rj5)
}

public node eng {
    alias output __out: engine;

    engine engine(
        name: "M73B54",
        starter_torque: 150 * units.Nm,
        starter_speed: 200 * units.rpm,
        redline: redline * units.rpm,
        fuel: s90(),
        throttle_gamma: 1.5,
        jitter: 1.0,
        noise: 0.1,
        hf_gain: 0.01,
        simulation_frequency: 7500,
        dyno_min_speed: 750 * units.rpm
    )

    variant_0 variant()

    crank c0(vee / 2 + rot90, variant)

    bank_builder bank0(
        var: variant,
        crankshaft: c0,
        incline: vee / 2,
        phasing: 0,
        ignition: ignition,
        intake: intake,
        exhaust: exhaust0,
        primaries: em_0(),
        sh: 0.0 * units.cm,
        flip: true
    )

    bank_builder bank1(
        var: variant,
        crankshaft: c0,
        incline: -vee / 2,
        phasing: vee,
        ignition: ignition,
        intake: intake,
        exhaust: exhaust1,
        sh: 0.0 * units.cm,
        primaries: em_1()
    )

    ignition_module ignition(timing_curve: timing_curve, rev_limit: redline * 1.05 * units.rpm, limiter_duration: 0.06)

    label collector_cross_section_area(19.1)

    label exhaust_pipe_length_0(380.0) // cm
    label exhaust_volume_0(collector_cross_section_area * exhaust_pipe_length_0 / 1000.0) // Litres
    
    label exhaust_pipe_length_1(384.0) // cm
    label exhaust_volume_1(collector_cross_section_area * exhaust_pipe_length_1 / 1000.0) // Litres

    intake intake(
        plenum_volume: 3.8 * units.L,
        plenum_cross_section_area: 64.9 * units.cm2,
        runner_length: 18.1 * units.cm,
        intake_flow_rate: k_carb(511.0),
        idle_flow_rate: k_carb(0.01),
        idle_throttle_plate_position: 0.998,
        runner_flow_rate: k_carb(85.2),
        velocity_decay: 0.03
    )
    
    exhaust_system_parameters es_params0(
        outlet_flow_rate: k_carb(415.2),
        collector_cross_section_area: 19.1 * units.cm2,
        length: exhaust_pipe_length_0 * units.cm,
        primary_tube_length: 24.5 * units.cm,
        primary_flow_rate: k_carb(138.4),
        velocity_decay: 1.0,
        volume: (exhaust_volume_0 + 4.0) * units.L
    )

    exhaust_system_parameters es_params1(
        outlet_flow_rate: k_carb(415.2),
        collector_cross_section_area: 19.1 * units.cm2,
        length: exhaust_pipe_length_1 * units.cm,
        primary_tube_length: 24.5 * units.cm,
        primary_flow_rate: k_carb(138.4),
        velocity_decay: 1.0,
        volume: (exhaust_volume_1 + 4.0) * units.L
    )
    
    exhaust_system exhaust0(es_params0, impulse_response: impulse_response(filename: "../../impulse_responses/smooth_42.wav", volume: 0.001))
    exhaust_system exhaust1(es_params1, impulse_response: impulse_response(filename: "../../impulse_responses/smooth_58.wav", volume: 0.001))

    engine
        .add_crankshaft(c0.c)
        .add_cylinder_bank(bank0.bank)
        .add_cylinder_bank(bank1.bank)
        .add_ignition_module(ignition)

    function timing_curve(400.0 * units.rpm)
    timing_curve
        .add_sample(0.0 * units.rpm, 10.0 * units.deg)
        .add_sample(400.0 * units.rpm, 10.0 * units.deg)
        .add_sample(800.0 * units.rpm, 15.2 * units.deg)
        .add_sample(1200.0 * units.rpm, 21.1 * units.deg)
        .add_sample(1600.0 * units.rpm, 25.9 * units.deg)
        .add_sample(2000.0 * units.rpm, 30.0 * units.deg)
        .add_sample(2400.0 * units.rpm, 33.4 * units.deg)
        .add_sample(2800.0 * units.rpm, 36.2 * units.deg)
        .add_sample(3200.0 * units.rpm, 38.5 * units.deg)
        .add_sample(3600.0 * units.rpm, 40.0 * units.deg)
        .add_sample(4000.0 * units.rpm, 40.0 * units.deg)
        .add_sample(4400.0 * units.rpm, 40.0 * units.deg)
        .add_sample(4800.0 * units.rpm, 40.0 * units.deg)
        .add_sample(5200.0 * units.rpm, 40.0 * units.deg)
        .add_sample(5600.0 * units.rpm, 40.0 * units.deg)
        .add_sample(6000.0 * units.rpm, 40.0 * units.deg)
        .add_sample(6400.0 * units.rpm, -12.5 * units.deg)
        .add_sample(6800.0 * units.rpm, -25.0 * units.deg)
}

label spacing_factor(1.13)
label flange_density(1.0 * exhaust_delay_coeff)
label em_unit(bore * spacing_factor / flange_density)

private node em_0{
    output pl0: (6.4 * em_unit) * units.mm;
    output pl1: (5.5 * em_unit) * units.mm;
    output pl2: (5.0 * em_unit) * units.mm;
    output pl3: (1.0 * em_unit) * units.mm;
    output pl4: (2.0 * em_unit) * units.mm;
    output pl5: (3.0 * em_unit) * units.mm;
}

private node em_1{
    output pl0: (7.0 * em_unit) * units.mm;
    output pl1: (6.0 * em_unit) * units.mm;
    output pl2: (5.0 * em_unit) * units.mm;
    output pl3: (1.0 * em_unit) * units.mm;
    output pl4: (1.5 * em_unit) * units.mm;
    output pl5: (2.4 * em_unit) * units.mm;
}

// 1988 750iL
public node veh {
    alias output __out: vehicle;

    label car_mass(1960) // 1860 empty

    vehicle vehicle(
        mass: car_mass * units.kg,
        drag_coefficient: 0.34,
        cross_sectional_area: (1845  * units.mm) * (1400 * units.mm),
        diff_ratio: 3.15,
        tire_radius: (653.0 / 2) * units.mm, // 225/60 VR 15
        rolling_resistance: 0.012 * car_mass * 9.81
        )
}

// ZF 4HP24E
private node trn {
    alias output __out:
        transmission(
            max_clutch_torque: 675 * units.Nm,
            max_clutch_flex: 30 * units.deg,
            limit_clutch_flex: true,
            clutch_stiffness: 20 * units.Nm / units.deg,
            clutch_damping: 1.0,
            simulate_flex: true
        )
        .add_gear(2.479)
        .add_gear(1.479)
        .add_gear(1.000)
        .add_gear(0.728);
}

run(
    engine: eng(),
    transmission: trn(),
    vehicle: veh()
)
