// Engine Sim V0.1.14A
// 2000 Jaguar AJ30 245 @ 6100
// Created by oror 2025
// 1-5-4-2-6-3-7-8

/*
{
  "cam_cfg": {
    "equal_base_radius": true,
    "exhaust_at_lift": 0.381,
    "exhaust_base_mult": 1.667,
    "exhaust_cos": false,
    "exhaust_sigma": 1.5,
    "exhaust_trim": 0.85,
    "exhaust_volume": 0.5,
    "intake_at_lift": 0.381,
    "intake_base_mult": 1.667,
    "intake_cos": false,
    "intake_sigma": 1.5,
    "intake_trim": 0.85,
    "intake_volume": 0.5,
    "lift_significant_fraction": 250.0,
    "ramp_position": 0.0,
    "ramp_steepness": 0.0,
    "resolution": 64,
    "roller_tappet_radius": 0.0
  },
  "flow_cfg": {
    "collector_area_coeff": 0.3,
    "exhaust_flange_dia": null,
    "exhaust_port_dia": null,
    "exhaust_stem_dia": 5,
    "intake_port_dia": null,
    "intake_runner_dia_mult": 1.02,
    "intake_stem_dia": 5,
    "ir_to_er_ratio": 3.3,
    "max_power_rpm": 6100.0,
    "max_torque_rpm": 3700.0,
    "port_to_valve_area": 0.86,
    "power_factor": 0.9,
    "primary_area_coeff": 1.1,
    "resolution": 32,
    "smoothness": 2.0,
    "valve_to_stem_dia": 5.3
  },
  "ign_cfg": {
    "end_deg": 32,
    "exp_mode": 0,
    "flywheel": 4,
    "resolution": 16,
    "start_deg": 0,
    "start_rpm": 300
  }
}
*/

import "engine_sim.mr"
import "../../fuel/90.mr"

units units()
constants constants()

label bore(86.0)
label stroke(85.0)
label compression_ratio(10.75)
label con_rod(155.321) // 6.115
label compression_height(30.76 * units.mm) // 1.211
label intake_valve_diameter(34.9)
label exhaust_valve_diameter(30.9)
label intake_valves(2.0)
label exhaust_valves(2.0)

label redline(6500.0)

label cyl(8.0)
label row(2.0)
label cycle(720.0 * units.deg)
label vee(90.0 * units.deg)
label rot(cycle / cyl)
label rot90(90.0 * units.deg)
label rot180(180.0 * units.deg)
label rot360(360.0 * units.deg)

// 5-25 65-35 50 10
label IVL(9.0)
label EVL(9.0)
label IVO(15.0 * units.deg) //BTDC
label IVC(50.0 * units.deg) //ABDC
label EVO(50.0 * units.deg) //BBDC
label EVC(10.0 * units.deg) //ATDC

label intake_duration(IVO + IVC + rot180)
label exhaust_duration(EVO + EVC + rot180)

label ILC((rot180 - IVO + IVC) / 2.0 + rot360)
label ELC((rot180 - EVO + EVC) / 2.0 + rot180)

label con_rod_mass(450.0) // ? g
label crank_mass(15.0) // ? kg
label flywheel_mass(3.0) // kg

label exhaust_delay_coeff(1.5)

public node eng_distributor {
    input wires;
    input timing_curve;
    input rev_limit: 7250 * units.rpm;
    input limiter_duration: 0.03;
    alias output __out:
        ignition_module(timing_curve: timing_curve, rev_limit: rev_limit, limiter_duration: limiter_duration)
            .connect_wire(wires.wire1, rot * 0)
            .connect_wire(wires.wire5, rot * 1)
            .connect_wire(wires.wire4, rot * 2)
            .connect_wire(wires.wire2, rot * 3)
            .connect_wire(wires.wire6, rot * 4)
            .connect_wire(wires.wire3, rot * 5)
            .connect_wire(wires.wire7, rot * 6)
            .connect_wire(wires.wire8, rot * 7);
}

private node wires {
    output wire1: ignition_wire();
    output wire2: ignition_wire();
    output wire3: ignition_wire();
    output wire4: ignition_wire();
    output wire5: ignition_wire();
    output wire6: ignition_wire();
    output wire7: ignition_wire();
    output wire8: ignition_wire();
}

public node intake_lobe_profile {
    // lobes.py v1.0 | 245.0 240.0 LSA 108.75 ADV 1.25 OVL 25.0
    alias output __out: _lobe_profile;
    function _lobe_profile(2.421 * units.deg)
        _lobe_profile
        .add_sample(-77.483 * units.deg, 0.000 * units.mm)
        .add_sample(-75.062 * units.deg, 0.031 * units.mm)
        .add_sample(-72.640 * units.deg, 0.108 * units.mm)
        .add_sample(-70.219 * units.deg, 0.193 * units.mm)
        .add_sample(-67.798 * units.deg, 0.293 * units.mm)
        .add_sample(-65.376 * units.deg, 0.406 * units.mm)
        .add_sample(-62.955 * units.deg, 0.540 * units.mm)
        .add_sample(-60.534 * units.deg, 0.697 * units.mm)
        .add_sample(-58.112 * units.deg, 0.885 * units.mm)
        .add_sample(-55.691 * units.deg, 1.113 * units.mm)
        .add_sample(-53.270 * units.deg, 1.404 * units.mm)
        .add_sample(-50.848 * units.deg, 1.789 * units.mm)
        .add_sample(-48.427 * units.deg, 2.293 * units.mm)
        .add_sample(-46.006 * units.deg, 2.848 * units.mm)
        .add_sample(-43.584 * units.deg, 3.396 * units.mm)
        .add_sample(-41.163 * units.deg, 3.951 * units.mm)
        .add_sample(-38.742 * units.deg, 4.472 * units.mm)
        .add_sample(-36.320 * units.deg, 4.969 * units.mm)
        .add_sample(-33.899 * units.deg, 5.468 * units.mm)
        .add_sample(-31.478 * units.deg, 5.930 * units.mm)
        .add_sample(-29.056 * units.deg, 6.355 * units.mm)
        .add_sample(-26.635 * units.deg, 6.741 * units.mm)
        .add_sample(-24.213 * units.deg, 7.110 * units.mm)
        .add_sample(-21.792 * units.deg, 7.464 * units.mm)
        .add_sample(-19.371 * units.deg, 7.778 * units.mm)
        .add_sample(-16.949 * units.deg, 8.051 * units.mm)
        .add_sample(-14.528 * units.deg, 8.282 * units.mm)
        .add_sample(-12.107 * units.deg, 8.472 * units.mm)
        .add_sample(-9.685 * units.deg, 8.658 * units.mm)
        .add_sample(-7.264 * units.deg, 8.807 * units.mm)
        .add_sample(-4.843 * units.deg, 8.914 * units.mm)
        .add_sample(-2.421 * units.deg, 8.979 * units.mm)
        .add_sample(0.000 * units.deg, 9.000 * units.mm)
        .add_sample(2.421 * units.deg, 8.979 * units.mm)
        .add_sample(4.843 * units.deg, 8.914 * units.mm)
        .add_sample(7.264 * units.deg, 8.807 * units.mm)
        .add_sample(9.685 * units.deg, 8.658 * units.mm)
        .add_sample(12.107 * units.deg, 8.472 * units.mm)
        .add_sample(14.528 * units.deg, 8.282 * units.mm)
        .add_sample(16.949 * units.deg, 8.051 * units.mm)
        .add_sample(19.371 * units.deg, 7.778 * units.mm)
        .add_sample(21.792 * units.deg, 7.464 * units.mm)
        .add_sample(24.213 * units.deg, 7.110 * units.mm)
        .add_sample(26.635 * units.deg, 6.741 * units.mm)
        .add_sample(29.056 * units.deg, 6.355 * units.mm)
        .add_sample(31.478 * units.deg, 5.930 * units.mm)
        .add_sample(33.899 * units.deg, 5.468 * units.mm)
        .add_sample(36.320 * units.deg, 4.969 * units.mm)
        .add_sample(38.742 * units.deg, 4.472 * units.mm)
        .add_sample(41.163 * units.deg, 3.951 * units.mm)
        .add_sample(43.584 * units.deg, 3.396 * units.mm)
        .add_sample(46.006 * units.deg, 2.848 * units.mm)
        .add_sample(48.427 * units.deg, 2.293 * units.mm)
        .add_sample(50.848 * units.deg, 1.789 * units.mm)
        .add_sample(53.270 * units.deg, 1.404 * units.mm)
        .add_sample(55.691 * units.deg, 1.113 * units.mm)
        .add_sample(58.112 * units.deg, 0.885 * units.mm)
        .add_sample(60.534 * units.deg, 0.697 * units.mm)
        .add_sample(62.955 * units.deg, 0.540 * units.mm)
        .add_sample(65.376 * units.deg, 0.406 * units.mm)
        .add_sample(67.798 * units.deg, 0.293 * units.mm)
        .add_sample(70.219 * units.deg, 0.193 * units.mm)
        .add_sample(72.640 * units.deg, 0.108 * units.mm)
        .add_sample(75.062 * units.deg, 0.031 * units.mm)
        .add_sample(77.483 * units.deg, 0.000 * units.mm)
}

public node exhaust_lobe_profile {
    // lobes.py v1.0 | 245.0 240.0 LSA 108.75 ADV 1.25 OVL 25.0
    alias output __out: _lobe_profile;
    function _lobe_profile(2.372 * units.deg)
        _lobe_profile
        .add_sample(-75.902 * units.deg, 0.000 * units.mm)
        .add_sample(-73.530 * units.deg, 0.036 * units.mm)
        .add_sample(-71.158 * units.deg, 0.112 * units.mm)
        .add_sample(-68.786 * units.deg, 0.199 * units.mm)
        .add_sample(-66.414 * units.deg, 0.302 * units.mm)
        .add_sample(-64.042 * units.deg, 0.418 * units.mm)
        .add_sample(-61.670 * units.deg, 0.555 * units.mm)
        .add_sample(-59.298 * units.deg, 0.718 * units.mm)
        .add_sample(-56.926 * units.deg, 0.917 * units.mm)
        .add_sample(-54.554 * units.deg, 1.163 * units.mm)
        .add_sample(-52.183 * units.deg, 1.487 * units.mm)
        .add_sample(-49.811 * units.deg, 1.929 * units.mm)
        .add_sample(-47.439 * units.deg, 2.467 * units.mm)
        .add_sample(-45.067 * units.deg, 3.016 * units.mm)
        .add_sample(-42.695 * units.deg, 3.570 * units.mm)
        .add_sample(-40.323 * units.deg, 4.104 * units.mm)
        .add_sample(-37.951 * units.deg, 4.605 * units.mm)
        .add_sample(-35.579 * units.deg, 5.105 * units.mm)
        .add_sample(-33.207 * units.deg, 5.584 * units.mm)
        .add_sample(-30.835 * units.deg, 6.029 * units.mm)
        .add_sample(-28.463 * units.deg, 6.437 * units.mm)
        .add_sample(-26.091 * units.deg, 6.808 * units.mm)
        .add_sample(-23.719 * units.deg, 7.178 * units.mm)
        .add_sample(-21.347 * units.deg, 7.518 * units.mm)
        .add_sample(-18.975 * units.deg, 7.819 * units.mm)
        .add_sample(-16.604 * units.deg, 8.081 * units.mm)
        .add_sample(-14.232 * units.deg, 8.304 * units.mm)
        .add_sample(-11.860 * units.deg, 8.488 * units.mm)
        .add_sample(-9.488 * units.deg, 8.672 * units.mm)
        .add_sample(-7.116 * units.deg, 8.815 * units.mm)
        .add_sample(-4.744 * units.deg, 8.918 * units.mm)
        .add_sample(-2.372 * units.deg, 8.979 * units.mm)
        .add_sample(0.000 * units.deg, 9.000 * units.mm)
        .add_sample(2.372 * units.deg, 8.979 * units.mm)
        .add_sample(4.744 * units.deg, 8.918 * units.mm)
        .add_sample(7.116 * units.deg, 8.815 * units.mm)
        .add_sample(9.488 * units.deg, 8.672 * units.mm)
        .add_sample(11.860 * units.deg, 8.488 * units.mm)
        .add_sample(14.232 * units.deg, 8.304 * units.mm)
        .add_sample(16.604 * units.deg, 8.081 * units.mm)
        .add_sample(18.975 * units.deg, 7.819 * units.mm)
        .add_sample(21.347 * units.deg, 7.518 * units.mm)
        .add_sample(23.719 * units.deg, 7.178 * units.mm)
        .add_sample(26.091 * units.deg, 6.808 * units.mm)
        .add_sample(28.463 * units.deg, 6.437 * units.mm)
        .add_sample(30.835 * units.deg, 6.029 * units.mm)
        .add_sample(33.207 * units.deg, 5.584 * units.mm)
        .add_sample(35.579 * units.deg, 5.105 * units.mm)
        .add_sample(37.951 * units.deg, 4.605 * units.mm)
        .add_sample(40.323 * units.deg, 4.104 * units.mm)
        .add_sample(42.695 * units.deg, 3.570 * units.mm)
        .add_sample(45.067 * units.deg, 3.016 * units.mm)
        .add_sample(47.439 * units.deg, 2.467 * units.mm)
        .add_sample(49.811 * units.deg, 1.929 * units.mm)
        .add_sample(52.183 * units.deg, 1.487 * units.mm)
        .add_sample(54.554 * units.deg, 1.163 * units.mm)
        .add_sample(56.926 * units.deg, 0.917 * units.mm)
        .add_sample(59.298 * units.deg, 0.718 * units.mm)
        .add_sample(61.670 * units.deg, 0.555 * units.mm)
        .add_sample(64.042 * units.deg, 0.418 * units.mm)
        .add_sample(66.414 * units.deg, 0.302 * units.mm)
        .add_sample(68.786 * units.deg, 0.199 * units.mm)
        .add_sample(71.158 * units.deg, 0.112 * units.mm)
        .add_sample(73.530 * units.deg, 0.036 * units.mm)
        .add_sample(75.902 * units.deg, 0.000 * units.mm)
}

public node intake_flow {
    alias output __out: _intake_flow;
    function _intake_flow(0.290 * units.mm)
    _intake_flow
        .add_flow_sample(0.000, 0.0)
        .add_flow_sample(0.290, 14.6)
        .add_flow_sample(0.581, 28.6)
        .add_flow_sample(0.871, 41.7)
        .add_flow_sample(1.161, 53.6)
        .add_flow_sample(1.452, 64.7)
        .add_flow_sample(1.742, 75.1)
        .add_flow_sample(2.032, 84.9)
        .add_flow_sample(2.323, 94.3)
        .add_flow_sample(2.613, 103.3)
        .add_flow_sample(2.903, 111.9)
        .add_flow_sample(3.194, 120.3)
        .add_flow_sample(3.484, 128.3)
        .add_flow_sample(3.774, 136.2)
        .add_flow_sample(4.065, 143.8)
        .add_flow_sample(4.355, 151.2)
        .add_flow_sample(4.645, 158.5)
        .add_flow_sample(4.935, 165.5)
        .add_flow_sample(5.226, 172.4)
        .add_flow_sample(5.516, 179.1)
        .add_flow_sample(5.806, 185.7)
        .add_flow_sample(6.097, 192.1)
        .add_flow_sample(6.387, 198.4)
        .add_flow_sample(6.677, 204.6)
        .add_flow_sample(6.968, 210.5)
        .add_flow_sample(7.258, 216.1)
        .add_flow_sample(7.548, 221.2)
        .add_flow_sample(7.839, 225.5)
        .add_flow_sample(8.129, 228.8)
        .add_flow_sample(8.419, 231.1)
        .add_flow_sample(8.710, 232.6)
        .add_flow_sample(9.000, 233.8)
}

public node exhaust_flow {
    alias output __out: _exhaust_flow;
    function _exhaust_flow(0.290 * units.mm)
    _exhaust_flow
        .add_flow_sample(0.000, 0.0)
        .add_flow_sample(0.290, 14.7)
        .add_flow_sample(0.581, 28.8)
        .add_flow_sample(0.871, 41.9)
        .add_flow_sample(1.161, 53.9)
        .add_flow_sample(1.452, 65.0)
        .add_flow_sample(1.742, 75.3)
        .add_flow_sample(2.032, 85.1)
        .add_flow_sample(2.323, 94.4)
        .add_flow_sample(2.613, 103.3)
        .add_flow_sample(2.903, 111.9)
        .add_flow_sample(3.194, 120.1)
        .add_flow_sample(3.484, 128.1)
        .add_flow_sample(3.774, 135.8)
        .add_flow_sample(4.065, 143.3)
        .add_flow_sample(4.355, 150.6)
        .add_flow_sample(4.645, 157.7)
        .add_flow_sample(4.935, 164.6)
        .add_flow_sample(5.226, 171.4)
        .add_flow_sample(5.516, 177.9)
        .add_flow_sample(5.806, 184.3)
        .add_flow_sample(6.097, 190.4)
        .add_flow_sample(6.387, 196.0)
        .add_flow_sample(6.677, 200.9)
        .add_flow_sample(6.968, 204.9)
        .add_flow_sample(7.258, 207.7)
        .add_flow_sample(7.548, 209.5)
        .add_flow_sample(7.839, 210.5)
        .add_flow_sample(8.129, 211.1)
        .add_flow_sample(8.419, 211.4)
        .add_flow_sample(8.710, 211.7)
        .add_flow_sample(9.000, 211.9)
}

intake_lobe_profile intake_lobe_profile()
exhaust_lobe_profile exhaust_lobe_profile()

intake_flow intake_flow()
exhaust_flow exhaust_flow()

public node camshaft_builder {
    output intake_cam_0: _intake_cam_0;
    output exhaust_cam_0: _exhaust_cam_0;

    output intake_cam_1: _intake_cam_1;
    output exhaust_cam_1: _exhaust_cam_1;

    camshaft _intake_cam_0(base_radius: 15.0 * units.mm, lobe_profile: intake_lobe_profile)
    camshaft _exhaust_cam_0(base_radius: 15.0 * units.mm, lobe_profile: exhaust_lobe_profile)
    
    camshaft _intake_cam_1(base_radius: 15.0 * units.mm, lobe_profile: intake_lobe_profile)
    camshaft _exhaust_cam_1(base_radius: 15.0 * units.mm, lobe_profile: exhaust_lobe_profile)

    _intake_cam_0
        .add_lobe(ILC + 0 * rot)
        .add_lobe(ILC + 3 * rot)
        .add_lobe(ILC + 5 * rot)
        .add_lobe(ILC + 2 * rot)
    _exhaust_cam_0
        .add_lobe(ELC + 0 * rot)
        .add_lobe(ELC + 3 * rot)
        .add_lobe(ELC + 5 * rot)
        .add_lobe(ELC + 2 * rot)
        
    _intake_cam_1
        .add_lobe(ILC + 1 * rot)
        .add_lobe(ILC + 4 * rot)
        .add_lobe(ILC + 6 * rot)
        .add_lobe(ILC + 7 * rot)
    _exhaust_cam_1
        .add_lobe(ELC + 1 * rot)
        .add_lobe(ELC + 4 * rot)
        .add_lobe(ELC + 6 * rot)
        .add_lobe(ELC + 7 * rot)
}

private node add_flow_sample {
    input lift;
    input flow;
    input this;
    alias output __out: this;

    this.add_sample(lift * units.mm, k_28inH2O(flow))
}

public node head {
    // port_flow.py v1.7
    // intake port area: 16.1 cm²; saturated lift: 7.90 mm
    // exhaust port area: 12.5 cm²; saturated lift: 6.95 mm
    // cylinder volume: 493.7 cm³ (30.13 CI); engine volume: 3.950 L (241.04 CI)
    // chamber volume: 50.6 cm³ (3.09 CI); conrod/stroke 1.827; N.A.C.C. 36.68
    // 8 harmonic intake runner length: 29.4 cm; diameter: 4.2 cm
    // primary length: 20.1 cm, area: 13.8 cm², diameter: 4.2 cm
    // collector diameter: 6.5 cm, area: 33.0 cm²

    input intake_camshaft;
    input exhaust_camshaft;
    input flip_display: false;
    
    alias output __out: head;
    generic_cylinder_head head(
        chamber_volume: 50.641 * units.cc,
        intake_runner_volume: 410.7 * units.cc,
        intake_runner_cross_section_area: 14.0 * units.cm2,
        exhaust_runner_volume: 124.4 * units.cc,
        exhaust_runner_cross_section_area: 13.8 * units.cm2,

        intake_port_flow: intake_flow,
        exhaust_port_flow: exhaust_flow,
        valvetrain: standard_valvetrain(
            intake_camshaft: intake_camshaft,
            exhaust_camshaft: exhaust_camshaft
        ),
        flip_display: flip_display
    )
}

public node eng {
    alias output __out: engine;

    engine engine(
        name: "Lincoln LS",
        starter_torque: 100 * units.Nm,
        starter_speed: 400 * units.rpm,
        redline: redline * units.rpm,
        fuel: s90(),
        throttle_gamma: 1.5,
        jitter: 0.7,
        noise: 0.2,
        hf_gain: 0.005,
        simulation_frequency: 10000
    )

    wires wires()

    connecting_rod_parameters cr_params(
        mass: con_rod_mass * units.g,
        moment_of_inertia: rod_moment_of_inertia(
            mass: con_rod_mass * units.g,
            length: con_rod * units.mm
        ),
        center_of_mass: 0.0,
        length: con_rod * units.mm
    )

    label crank_moment(disk_moment_of_inertia(mass: crank_mass * units.kg, radius: (stroke / 2.0) * units.mm))
    label flywheel_moment(disk_moment_of_inertia(mass: flywheel_mass * units.kg, radius: (stroke * 3.5) * units.mm))
    label other_moment(disk_moment_of_inertia(mass: 1 * units.kg, radius: 1.0 * units.cm))

    crankshaft c0(
        throw: (stroke / 2.0) * units.mm,
        flywheel_mass: flywheel_mass * units.kg,
        mass: crank_mass * units.kg,
        friction_torque: 5.9 * units.Nm,
        moment_of_inertia: crank_moment + flywheel_moment + other_moment,
        tdc: 0.0
    )

    rod_journal rj0(angle: rot * 0 + rot90 + vee / 2)
    rod_journal rj1(angle: rot * 3 + rot90 + vee / 2)
    rod_journal rj2(angle: rot * 5 + rot90 + vee / 2)
    rod_journal rj3(angle: rot * 2 + rot90 + vee / 2)
    
    c0
        .add_rod_journal(rj0)
        .add_rod_journal(rj1)
        .add_rod_journal(rj2)
        .add_rod_journal(rj3)

    piston_parameters piston_params(
        mass: 500.0 * units.g,
        blowby: k_28inH2O(0.027),
        compression_height: compression_height,
        wrist_pin_position: 0.0,
        displacement: 0.0
    )

    cylinder_bank_parameters bank_params(
        bore: bore * units.mm,
        deck_height: (con_rod + stroke / 2.0) * units.mm + compression_height
    )

    label collector_cross_section_area(16.5)

    label exhaust_pipe_length_0(283.0) // cm
    label exhaust_volume_0(collector_cross_section_area * exhaust_pipe_length_0 / 1000.0) // Litres
    
    label exhaust_pipe_length_1(280.0) // cm
    label exhaust_volume_1(collector_cross_section_area * exhaust_pipe_length_1 / 1000.0) // Litres
    
    label spacing_factor(1.07)
    label flange_density(1.0 * exhaust_delay_coeff)

    intake intake(
        plenum_volume: 3.4 * units.L,
        plenum_cross_section_area: 55.9 * units.cm2,
        runner_length: 29.4 * units.cm,
        intake_flow_rate: k_carb(406.5),
        idle_flow_rate: k_carb(0.02),
        idle_throttle_plate_position: 0.9975,
        runner_flow_rate: k_carb(101.6),
        velocity_decay: 0.05
    )
    
    exhaust_system_parameters es_params0(
        outlet_flow_rate: k_carb(330.3),
        collector_cross_section_area: 16.5 * units.cm2,
        length: exhaust_pipe_length_0 * units.cm,
        primary_tube_length: 20.1 * units.cm,
        primary_flow_rate: k_carb(165.1),
        velocity_decay: 0.9,
        volume: (exhaust_volume_0 + 3.0) * units.L
    )

    exhaust_system_parameters es_params1(
        outlet_flow_rate: k_carb(330.3),
        collector_cross_section_area: 16.5 * units.cm2,
        length: exhaust_pipe_length_1 * units.cm,
        primary_tube_length: 20.1 * units.cm,
        primary_flow_rate: k_carb(165.1),
        velocity_decay: 0.9,
        volume: (exhaust_volume_1 + 3.0) * units.L
    )

    exhaust_system exhaust0(es_params0, impulse_response: impulse_response(filename: "../../impulse_responses/smooth_56.wav", volume: 0.00025))
    exhaust_system exhaust1(es_params1, impulse_response: impulse_response(filename: "../../impulse_responses/smooth_56.wav", volume: 0.00023))
 
    cylinder_bank b0(bank_params, angle: vee / 2)
    cylinder_bank b1(bank_params, angle: -vee / 2)
    
    label pl0 ((4.0 * bore * spacing_factor / flange_density) * units.mm)
    label pl1 ((3.0 * bore * spacing_factor / flange_density) * units.mm)
    label pl2 ((2.0 * bore * spacing_factor / flange_density) * units.mm)
    label pl3 ((1.0 * bore * spacing_factor / flange_density) * units.mm)

    b0
        .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj0,
            intake: intake,
            exhaust_system: exhaust0,
            primary_length: pl0,
            ignition_wire: wires.wire1
        )
        .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj1,
            intake: intake,
            exhaust_system: exhaust0,
            primary_length: pl1,
            ignition_wire: wires.wire2
        )
        .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj2,
            intake: intake,
            exhaust_system: exhaust0,
            primary_length: pl2,
            ignition_wire: wires.wire3
        )
        .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj3,
            intake: intake,
            exhaust_system: exhaust0,
            primary_length: pl3,
            ignition_wire: wires.wire4
        )
        
    b1
        .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj0,
            intake: intake,
            exhaust_system: exhaust1,
            primary_length: pl3,
            ignition_wire: wires.wire5
        )
        .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj1,
            intake: intake,
            exhaust_system: exhaust1,
            primary_length: pl2,
            ignition_wire: wires.wire6
        )
        .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj2,
            intake: intake,
            exhaust_system: exhaust1,
            primary_length: pl1,
            ignition_wire: wires.wire7
        )
        .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj3,
            intake: intake,
            exhaust_system: exhaust1,
            primary_length: pl0,
            ignition_wire: wires.wire8
        )

    engine
        .add_cylinder_bank(b0)
        .add_cylinder_bank(b1)

    engine.add_crankshaft(c0)

    camshaft_builder camshaft()

    b0.set_cylinder_head (
        head(
            intake_camshaft: camshaft.intake_cam_0,
            exhaust_camshaft: camshaft.exhaust_cam_0,
            flip_display: true
        )
    )
    b1.set_cylinder_head (
        head(
            intake_camshaft: camshaft.intake_cam_1,
            exhaust_camshaft: camshaft.exhaust_cam_1
        )
    )

    function timing_curve(466.7 * units.rpm)
    timing_curve
        .add_sample(0.0 * units.rpm, 4.0 * units.deg)
        .add_sample(466.7 * units.rpm, 8.6 * units.deg)
        .add_sample(933.3 * units.rpm, 18.5 * units.deg)
        .add_sample(1400.0 * units.rpm, 25.3 * units.deg)
        .add_sample(1866.7 * units.rpm, 29.9 * units.deg)
        .add_sample(2333.3 * units.rpm, 33.1 * units.deg)
        .add_sample(2800.0 * units.rpm, 35.3 * units.deg)
        .add_sample(3266.7 * units.rpm, 36.0 * units.deg)
        .add_sample(3733.3 * units.rpm, 36.0 * units.deg)
        .add_sample(4200.0 * units.rpm, 36.0 * units.deg)
        .add_sample(4666.7 * units.rpm, 36.0 * units.deg)
        .add_sample(5133.3 * units.rpm, 36.0 * units.deg)
        .add_sample(5600.0 * units.rpm, 36.0 * units.deg)
        .add_sample(6066.7 * units.rpm, 36.0 * units.deg)
        .add_sample(6533.3 * units.rpm, 36.0 * units.deg)
        .add_sample(7000.0 * units.rpm, 36.0 * units.deg)
        .add_sample(7466.7 * units.rpm, -12.5 * units.deg)
        .add_sample(7933.3 * units.rpm, -25.0 * units.deg)

    engine.add_ignition_module(
        eng_distributor(
            wires: wires,
            timing_curve: timing_curve
        )
    )
}

// AMG GT R
public node veh {
    alias output __out: vehicle;

    label car_mass(1630)

    vehicle vehicle(
        mass: car_mass * units.kg,
        drag_coefficient: 0.33,
        cross_sectional_area: (2007 * units.mm) * (1287 * units.mm),
        diff_ratio: 3.88,
        tire_radius: (670.5 / 2) * units.mm, // 8.25x20
        rolling_resistance: 0.012 * car_mass * 9.81
        )
}

// AMG GT R
private node trn {
    alias output __out:
        transmission(
            max_clutch_torque: 800 * units.Nm,
            max_clutch_flex: 10 * units.deg,
            limit_clutch_flex: true,
            clutch_stiffness: 20 * units.Nm / units.deg,
            clutch_damping: 2.0,
            simulate_flex: true
        )
        .add_gear(3.08)
        .add_gear(2.19)
        .add_gear(1.63)
        .add_gear(1.29)
        .add_gear(1.03)
        .add_gear(0.84)
        .add_gear(0.69);
}

run(
    engine: eng(),
    transmission: trn(),
    vehicle: veh()
)
