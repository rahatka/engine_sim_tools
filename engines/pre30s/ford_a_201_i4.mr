// Engine Sim V0.1.14A
// 1929 Ford A 40 @ 2200
// Created by oror 2024

/*
{
  "cam_cfg": {
    "equal_base_radius": true,
    "es_version": "0.1.14a",
    "exhaust_at_lift": 0.508,
    "exhaust_base_mult": 1.917,
    "exhaust_cos": false,
    "exhaust_sigma": 1.0,
    "exhaust_trim": 0.9,
    "exhaust_volume": 0.5,
    "intake_at_lift": 0.508,
    "intake_base_mult": 1.917,
    "intake_cos": false,
    "intake_sigma": 1.0,
    "intake_trim": 0.9,
    "intake_volume": 0.5,
    "lift_significant_fraction": 250.0,
    "ramp_position": 0.0,
    "ramp_steepness": 2.0,
    "resolution": 64,
    "roller_tappet_radius": 0.0
  },
  "flow_cfg": {
    "collector_area_coeff": 0.3,
    "exhaust_flange_dia": null,
    "exhaust_port_dia": null,
    "exhaust_stem_dia": null,
    "intake_port_dia": null,
    "intake_runner_dia_mult": 1.02,
    "intake_stem_dia": null,
    "ir_to_er_ratio": 3.0,
    "max_power_rpm": 2200.0,
    "max_torque_rpm": 1000.0,
    "port_to_valve_area": 0.8,
    "power_factor": 1.1,
    "primary_area_coeff": 1.1,
    "resolution": 32,
    "smoothness": 3.0,
    "valve_to_stem_dia": 4.7
  },
  "ign_cfg": {
    "end_deg": 20,
    "end_rpm": 2200,
    "exp_mode": 2,
    "flywheel": 5,
    "resolution": 16,
    "start_rpm": 400
  }
}
*/

import "engine_sim.mr"
import "../../fuel/10.mr"

units units()
constants constants()

label bore(98.425) // 3 7/8
label stroke(107.95) // 4 1/4
label compression_ratio(4.2)
label con_rod(190.5) // 7 1/2
label compression_height(99.2187 / 2 * units.mm) // 3 29/32 len
label intake_valve_diameter(38.1) // 1 1/2
label exhaust_valve_diameter(38.1) // 1 1/2
label intake_valves(1.0)
label exhaust_valves(1.0)

label cyl(4.0)
label row(1.0)
label cycle(720.0 * units.deg)
label rot(cycle / cyl)
label cyl_base(cyl / row)
label rot_base(cycle / cyl_base)
label rot90(90.0 * units.deg)
label rot180(180.0 * units.deg)
label rot360(360.0 * units.deg)

label redline(2400.0) // RPM

// 1929 Ford A
label IVL(7.2898) // .287
label EVL(7.2898) // .287
label IVO(7.0 * units.deg) //BTDC
label IVC(48.0 * units.deg) //ABDC
label EVO(51.0 * units.deg) //BBDC
label EVC(1.0 * units.deg) //ATDC

label intake_duration(IVO + IVC + rot180)
label exhaust_duration(EVO + EVC + rot180)

label ILC((rot180 - IVO + IVC) / 2.0 + rot360)
label ELC((rot180 - EVO + EVC) / 2.0 + rot180)

label con_rod_mass(701.651) // 24 3/4
label piston_mass(725.7478) // 25.6
label crank_mass(18.0) // ? kg
label flywheel_mass(4.0) // kg

label exhaust_delay_coeff(1.5)

public node variant_0 {
    output s1: 0;
    output s2: 1;
    output s4: 2;
    output s3: 3;
}

public node intake_lobe_profile {
    // lobes.py v1.0 | 235.0 232.0 LSA 112.8 ADV 2.2 OVL 8.0
    alias output __out: _lobe_profile;
    function _lobe_profile(2.413 * units.deg)
        _lobe_profile
        .add_sample(-77.230 * units.deg, 0.000 * units.mm)
        .add_sample(-74.817 * units.deg, 0.024 * units.mm)
        .add_sample(-72.404 * units.deg, 0.096 * units.mm)
        .add_sample(-69.990 * units.deg, 0.205 * units.mm)
        .add_sample(-67.577 * units.deg, 0.320 * units.mm)
        .add_sample(-65.163 * units.deg, 0.428 * units.mm)
        .add_sample(-62.750 * units.deg, 0.525 * units.mm)
        .add_sample(-60.336 * units.deg, 0.638 * units.mm)
        .add_sample(-57.923 * units.deg, 0.769 * units.mm)
        .add_sample(-55.509 * units.deg, 0.921 * units.mm)
        .add_sample(-53.096 * units.deg, 1.098 * units.mm)
        .add_sample(-50.683 * units.deg, 1.309 * units.mm)
        .add_sample(-48.269 * units.deg, 1.563 * units.mm)
        .add_sample(-45.856 * units.deg, 1.876 * units.mm)
        .add_sample(-43.442 * units.deg, 2.270 * units.mm)
        .add_sample(-41.029 * units.deg, 2.737 * units.mm)
        .add_sample(-38.615 * units.deg, 3.199 * units.mm)
        .add_sample(-36.202 * units.deg, 3.667 * units.mm)
        .add_sample(-33.788 * units.deg, 4.105 * units.mm)
        .add_sample(-31.375 * units.deg, 4.510 * units.mm)
        .add_sample(-28.961 * units.deg, 4.886 * units.mm)
        .add_sample(-26.548 * units.deg, 5.266 * units.mm)
        .add_sample(-24.135 * units.deg, 5.612 * units.mm)
        .add_sample(-21.721 * units.deg, 5.924 * units.mm)
        .add_sample(-19.308 * units.deg, 6.199 * units.mm)
        .add_sample(-16.894 * units.deg, 6.440 * units.mm)
        .add_sample(-14.481 * units.deg, 6.643 * units.mm)
        .add_sample(-12.067 * units.deg, 6.820 * units.mm)
        .add_sample(-9.654 * units.deg, 6.989 * units.mm)
        .add_sample(-7.240 * units.deg, 7.120 * units.mm)
        .add_sample(-4.827 * units.deg, 7.214 * units.mm)
        .add_sample(-2.413 * units.deg, 7.271 * units.mm)
        .add_sample(0.000 * units.deg, 7.290 * units.mm)
        .add_sample(2.413 * units.deg, 7.271 * units.mm)
        .add_sample(4.827 * units.deg, 7.214 * units.mm)
        .add_sample(7.240 * units.deg, 7.120 * units.mm)
        .add_sample(9.654 * units.deg, 6.989 * units.mm)
        .add_sample(12.067 * units.deg, 6.820 * units.mm)
        .add_sample(14.481 * units.deg, 6.643 * units.mm)
        .add_sample(16.894 * units.deg, 6.440 * units.mm)
        .add_sample(19.308 * units.deg, 6.199 * units.mm)
        .add_sample(21.721 * units.deg, 5.924 * units.mm)
        .add_sample(24.135 * units.deg, 5.612 * units.mm)
        .add_sample(26.548 * units.deg, 5.266 * units.mm)
        .add_sample(28.961 * units.deg, 4.886 * units.mm)
        .add_sample(31.375 * units.deg, 4.510 * units.mm)
        .add_sample(33.788 * units.deg, 4.105 * units.mm)
        .add_sample(36.202 * units.deg, 3.667 * units.mm)
        .add_sample(38.615 * units.deg, 3.199 * units.mm)
        .add_sample(41.029 * units.deg, 2.737 * units.mm)
        .add_sample(43.442 * units.deg, 2.270 * units.mm)
        .add_sample(45.856 * units.deg, 1.876 * units.mm)
        .add_sample(48.269 * units.deg, 1.563 * units.mm)
        .add_sample(50.683 * units.deg, 1.309 * units.mm)
        .add_sample(53.096 * units.deg, 1.098 * units.mm)
        .add_sample(55.509 * units.deg, 0.921 * units.mm)
        .add_sample(57.923 * units.deg, 0.769 * units.mm)
        .add_sample(60.336 * units.deg, 0.638 * units.mm)
        .add_sample(62.750 * units.deg, 0.525 * units.mm)
        .add_sample(65.163 * units.deg, 0.428 * units.mm)
        .add_sample(67.577 * units.deg, 0.320 * units.mm)
        .add_sample(69.990 * units.deg, 0.205 * units.mm)
        .add_sample(72.404 * units.deg, 0.096 * units.mm)
        .add_sample(74.817 * units.deg, 0.024 * units.mm)
        .add_sample(77.230 * units.deg, 0.000 * units.mm)
}

public node exhaust_lobe_profile {
    // lobes.py v1.0 | 235.0 232.0 LSA 112.8 ADV 2.2 OVL 8.0
    alias output __out: _lobe_profile;
    function _lobe_profile(2.383 * units.deg)
        _lobe_profile
        .add_sample(-76.245 * units.deg, 0.000 * units.mm)
        .add_sample(-73.862 * units.deg, 0.025 * units.mm)
        .add_sample(-71.479 * units.deg, 0.101 * units.mm)
        .add_sample(-69.097 * units.deg, 0.213 * units.mm)
        .add_sample(-66.714 * units.deg, 0.325 * units.mm)
        .add_sample(-64.331 * units.deg, 0.433 * units.mm)
        .add_sample(-61.949 * units.deg, 0.530 * units.mm)
        .add_sample(-59.566 * units.deg, 0.646 * units.mm)
        .add_sample(-57.183 * units.deg, 0.778 * units.mm)
        .add_sample(-54.801 * units.deg, 0.933 * units.mm)
        .add_sample(-52.418 * units.deg, 1.115 * units.mm)
        .add_sample(-50.035 * units.deg, 1.334 * units.mm)
        .add_sample(-47.653 * units.deg, 1.600 * units.mm)
        .add_sample(-45.270 * units.deg, 1.932 * units.mm)
        .add_sample(-42.888 * units.deg, 2.351 * units.mm)
        .add_sample(-40.505 * units.deg, 2.821 * units.mm)
        .add_sample(-38.122 * units.deg, 3.285 * units.mm)
        .add_sample(-35.740 * units.deg, 3.742 * units.mm)
        .add_sample(-33.357 * units.deg, 4.169 * units.mm)
        .add_sample(-30.974 * units.deg, 4.564 * units.mm)
        .add_sample(-28.592 * units.deg, 4.942 * units.mm)
        .add_sample(-26.209 * units.deg, 5.312 * units.mm)
        .add_sample(-23.826 * units.deg, 5.650 * units.mm)
        .add_sample(-21.444 * units.deg, 5.953 * units.mm)
        .add_sample(-19.061 * units.deg, 6.222 * units.mm)
        .add_sample(-16.678 * units.deg, 6.456 * units.mm)
        .add_sample(-14.296 * units.deg, 6.655 * units.mm)
        .add_sample(-11.913 * units.deg, 6.832 * units.mm)
        .add_sample(-9.531 * units.deg, 6.996 * units.mm)
        .add_sample(-7.148 * units.deg, 7.125 * units.mm)
        .add_sample(-4.765 * units.deg, 7.216 * units.mm)
        .add_sample(-2.383 * units.deg, 7.271 * units.mm)
        .add_sample(0.000 * units.deg, 7.290 * units.mm)
        .add_sample(2.383 * units.deg, 7.271 * units.mm)
        .add_sample(4.765 * units.deg, 7.216 * units.mm)
        .add_sample(7.148 * units.deg, 7.125 * units.mm)
        .add_sample(9.531 * units.deg, 6.996 * units.mm)
        .add_sample(11.913 * units.deg, 6.832 * units.mm)
        .add_sample(14.296 * units.deg, 6.655 * units.mm)
        .add_sample(16.678 * units.deg, 6.456 * units.mm)
        .add_sample(19.061 * units.deg, 6.222 * units.mm)
        .add_sample(21.444 * units.deg, 5.953 * units.mm)
        .add_sample(23.826 * units.deg, 5.650 * units.mm)
        .add_sample(26.209 * units.deg, 5.312 * units.mm)
        .add_sample(28.592 * units.deg, 4.942 * units.mm)
        .add_sample(30.974 * units.deg, 4.564 * units.mm)
        .add_sample(33.357 * units.deg, 4.169 * units.mm)
        .add_sample(35.740 * units.deg, 3.742 * units.mm)
        .add_sample(38.122 * units.deg, 3.285 * units.mm)
        .add_sample(40.505 * units.deg, 2.821 * units.mm)
        .add_sample(42.888 * units.deg, 2.351 * units.mm)
        .add_sample(45.270 * units.deg, 1.932 * units.mm)
        .add_sample(47.653 * units.deg, 1.600 * units.mm)
        .add_sample(50.035 * units.deg, 1.334 * units.mm)
        .add_sample(52.418 * units.deg, 1.115 * units.mm)
        .add_sample(54.801 * units.deg, 0.933 * units.mm)
        .add_sample(57.183 * units.deg, 0.778 * units.mm)
        .add_sample(59.566 * units.deg, 0.646 * units.mm)
        .add_sample(61.949 * units.deg, 0.530 * units.mm)
        .add_sample(64.331 * units.deg, 0.433 * units.mm)
        .add_sample(66.714 * units.deg, 0.325 * units.mm)
        .add_sample(69.097 * units.deg, 0.213 * units.mm)
        .add_sample(71.479 * units.deg, 0.101 * units.mm)
        .add_sample(73.862 * units.deg, 0.025 * units.mm)
        .add_sample(76.245 * units.deg, 0.000 * units.mm)
}

public node intake_flow {
    alias output __out: _intake_flow;
    function _intake_flow(0.235 * units.mm)
    _intake_flow
        .add_flow_sample(0.000, 0.0)
        .add_flow_sample(0.235, 6.1)
        .add_flow_sample(0.470, 12.0)
        .add_flow_sample(0.705, 17.7)
        .add_flow_sample(0.941, 23.1)
        .add_flow_sample(1.176, 28.2)
        .add_flow_sample(1.411, 33.0)
        .add_flow_sample(1.646, 37.6)
        .add_flow_sample(1.881, 42.0)
        .add_flow_sample(2.116, 46.1)
        .add_flow_sample(2.352, 50.1)
        .add_flow_sample(2.587, 54.0)
        .add_flow_sample(2.822, 57.7)
        .add_flow_sample(3.057, 61.4)
        .add_flow_sample(3.292, 64.9)
        .add_flow_sample(3.527, 68.4)
        .add_flow_sample(3.762, 71.8)
        .add_flow_sample(3.998, 75.1)
        .add_flow_sample(4.233, 78.3)
        .add_flow_sample(4.468, 81.4)
        .add_flow_sample(4.703, 84.5)
        .add_flow_sample(4.938, 87.5)
        .add_flow_sample(5.173, 90.5)
        .add_flow_sample(5.409, 93.4)
        .add_flow_sample(5.644, 96.3)
        .add_flow_sample(5.879, 99.1)
        .add_flow_sample(6.114, 101.8)
        .add_flow_sample(6.349, 104.6)
        .add_flow_sample(6.584, 107.2)
        .add_flow_sample(6.819, 109.9)
        .add_flow_sample(7.055, 112.5)
        .add_flow_sample(7.290, 115.1)
}

public node exhaust_flow {
    alias output __out: _exhaust_flow;
    function _exhaust_flow(0.235 * units.mm)
    _exhaust_flow
        .add_flow_sample(0.000, 0.0)
        .add_flow_sample(0.235, 7.1)
        .add_flow_sample(0.470, 14.0)
        .add_flow_sample(0.705, 20.6)
        .add_flow_sample(0.941, 26.9)
        .add_flow_sample(1.176, 32.9)
        .add_flow_sample(1.411, 38.5)
        .add_flow_sample(1.646, 43.9)
        .add_flow_sample(1.881, 48.9)
        .add_flow_sample(2.116, 53.8)
        .add_flow_sample(2.352, 58.5)
        .add_flow_sample(2.587, 63.0)
        .add_flow_sample(2.822, 67.4)
        .add_flow_sample(3.057, 71.6)
        .add_flow_sample(3.292, 75.8)
        .add_flow_sample(3.527, 79.8)
        .add_flow_sample(3.762, 83.7)
        .add_flow_sample(3.998, 87.6)
        .add_flow_sample(4.233, 91.3)
        .add_flow_sample(4.468, 95.0)
        .add_flow_sample(4.703, 98.6)
        .add_flow_sample(4.938, 102.1)
        .add_flow_sample(5.173, 105.6)
        .add_flow_sample(5.409, 109.0)
        .add_flow_sample(5.644, 112.3)
        .add_flow_sample(5.879, 115.6)
        .add_flow_sample(6.114, 118.8)
        .add_flow_sample(6.349, 122.0)
        .add_flow_sample(6.584, 125.1)
        .add_flow_sample(6.819, 128.2)
        .add_flow_sample(7.055, 131.3)
        .add_flow_sample(7.290, 134.3)
}

intake_lobe_profile intake_lobe_profile()
exhaust_lobe_profile exhaust_lobe_profile()

intake_flow intake_flow()
exhaust_flow exhaust_flow()

private node add_lobes {
    input base;
    input shift: 0.0;
    input var;
    input this;
    alias output __out: this;

    this.add_lobe(base + var.s1 * rot_base + shift)
    this.add_lobe(base + var.s2 * rot_base + shift)
    this.add_lobe(base + var.s3 * rot_base + shift)
    this.add_lobe(base + var.s4 * rot_base + shift)
}

public node bank_builder {
    input var;
    input crankshaft;
    input incline;
    input phasing;
    input ignition;
    input intake;
    input exhaust;
    input flip: false;

    output bank: _b0;

    ignition_wire _wire1()
    ignition_wire _wire2()
    ignition_wire _wire3()
    ignition_wire _wire4()

    piston_parameters piston_params(
        mass: piston_mass * units.g,
        blowby: k_28inH2O(0.031),
        compression_height: compression_height,
        wrist_pin_position: 0.0,
        displacement: 0.0
    )

    cylinder_bank_parameters bank_params(
        bore: bore * units.mm,
        deck_height: (con_rod + stroke / 2.0) * units.mm + compression_height
    )

    connecting_rod_parameters cr_params(
        mass: con_rod_mass * units.g,
        moment_of_inertia: rod_moment_of_inertia(
            mass: con_rod_mass * units.g,
            length: con_rod * units.mm
        ),
        center_of_mass: 0.0,
        length: con_rod * units.mm
    )

    camshaft _intake_cam_0(base_radius: 14.0 * units.mm, lobe_profile: intake_lobe_profile)
    camshaft _exhaust_cam_0(base_radius: 14.0 * units.mm, lobe_profile: exhaust_lobe_profile)

    _intake_cam_0.add_lobes(ILC, phasing, var)
    _exhaust_cam_0.add_lobes(ELC, phasing, var)

    ignition.connect_wire(_wire1, rot_base * var.s1 + phasing)
    ignition.connect_wire(_wire2, rot_base * var.s2 + phasing)
    ignition.connect_wire(_wire3, rot_base * var.s3 + phasing)
    ignition.connect_wire(_wire4, rot_base * var.s4 + phasing)

    cylinder_bank _b0(bank_params, angle: incline)

    _b0
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj0,
            intake: intake,
            exhaust_system: exhaust.es0,
            primary_length: exhaust.pl0,
            ignition_wire: _wire1
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj1,
            intake: intake,
            exhaust_system: exhaust.es1,
            primary_length: exhaust.pl1,
            ignition_wire: _wire2
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj2,
            intake: intake,
            exhaust_system: exhaust.es2,
            primary_length: exhaust.pl2,
            ignition_wire: _wire3
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj3,
            intake: intake,
            exhaust_system: exhaust.es3,
            primary_length: exhaust.pl3,
            ignition_wire: _wire4
        )

    _b0.set_cylinder_head(
        head(
            intake_camshaft: _intake_cam_0,
            exhaust_camshaft: _exhaust_cam_0,
            flip_display: flip
        )
    )
}

private node add_flow_sample {
    input lift;
    input flow;
    input this;
    alias output __out: this;

    this.add_sample(lift * units.mm, k_28inH2O(flow))
}

public node head {
    // port_flow.py v1.5
    // intake port area: 8.6 cm²; saturated lift: 8.04 mm
    // exhaust port area: 8.6 cm²; saturated lift: 8.04 mm
    // cylinder volume: 821.3 cm³ (50.1 CI); engine volume: 3.285 L (200.5 CI)
    // chamber volume: 256.7 cm³ (15.7 CI)
    // 32 harmonic intake runner length: 20.7 cm; diameter: 3.3 cm
    // primary length: 73.9 cm, area: 9.5 cm², diameter: 3.5 cm
    // collector diameter: 3.8 cm, area: 11.4 cm²

    input intake_camshaft;
    input exhaust_camshaft;
    input flip_display: false;
    
    alias output __out: head;
    generic_cylinder_head head(
        chamber_volume: 256.669 * units.cc,
        intake_runner_volume: 173.7 * units.cc,
        intake_runner_cross_section_area: 8.4 * units.cm2,
        exhaust_runner_volume: 57.9 * units.cc,
        exhaust_runner_cross_section_area: 9.5 * units.cm2,

        intake_port_flow: intake_flow,
        exhaust_port_flow: exhaust_flow,
        valvetrain: standard_valvetrain(
            intake_camshaft: intake_camshaft,
            exhaust_camshaft: exhaust_camshaft
        ),
        flip_display: flip_display
    )
}

public node crank {
    input phasing;
    input var;

    output c: _c;
    output rj0: _rj0;
    output rj1: _rj1;
    output rj2: _rj2;
    output rj3: _rj3;

    label crank_moment(disk_moment_of_inertia(mass: crank_mass * units.kg, radius: (stroke / 2.0) * units.mm))
    label flywheel_moment(disk_moment_of_inertia(mass: flywheel_mass * units.kg, radius: (stroke * 3.5) * units.mm))
    label other_moment(disk_moment_of_inertia(mass: 1 * units.kg, radius: 1.0 * units.cm))

    crankshaft _c(
        throw: (stroke / 2.0) * units.mm,
        flywheel_mass: flywheel_mass * units.kg,
        mass: crank_mass * units.kg,
        friction_torque: 4.9 * units.Nm,
        moment_of_inertia: crank_moment + flywheel_moment + other_moment,
        tdc: 0.0
    )

    rod_journal _rj0(angle: rot_base * var.s1 + rot90)
    rod_journal _rj1(angle: rot_base * var.s2 + rot90)
    rod_journal _rj2(angle: rot_base * var.s3 + rot90)
    rod_journal _rj3(angle: rot_base * var.s4 + rot90)

    _c
        .add_rod_journal(_rj0)
        .add_rod_journal(_rj1)
        .add_rod_journal(_rj2)
        .add_rod_journal(_rj3)
}

public node eng {
    alias output __out: engine;

    engine engine(
        name: "Ford A",
        starter_torque: 150 * units.Nm,
        starter_speed: 150 * units.rpm,
        redline: redline * units.rpm,
        fuel: s10(),
        throttle_gamma: 1.5,
        jitter: 0.7,
        noise: 0.2,
        hf_gain: 0.006,
        simulation_frequency: 12000,
        dyno_min_speed: 500 * units.rpm
    )

    variant_0 variant()
    es0 es()

    crank c0(rot90, variant)

    bank_builder bank0(
        var: variant,
        crankshaft: c0,
        incline: 0,
        phasing: 0,
        ignition: ignition,
        intake: intake,
        exhaust: es,
        flip: true
    )

    ignition_module ignition(timing_curve: timing_curve, rev_limit: redline * 1.2 * units.rpm, limiter_duration: 0.03)

    intake intake(
        plenum_volume: 1.1 * units.L,
        plenum_cross_section_area: 16.8 * units.cm2,
        runner_length: 20.7 * units.cm,
        intake_flow_rate: k_carb(138.0),
        idle_flow_rate: k_carb(0.02),
        idle_throttle_plate_position: 0.993,
        runner_flow_rate: k_carb(72.4),
        velocity_decay: 0.05
    )

    function timing_curve(160.0 * units.rpm)
    timing_curve
        .add_sample(0.0 * units.rpm, 5.0 * units.deg)
        .add_sample(160.0 * units.rpm, 5.0 * units.deg)
        .add_sample(320.0 * units.rpm, 5.0 * units.deg)
        .add_sample(480.0 * units.rpm, 6.7 * units.deg)
        .add_sample(640.0 * units.rpm, 9.8 * units.deg)
        .add_sample(800.0 * units.rpm, 12.5 * units.deg)
        .add_sample(960.0 * units.rpm, 14.8 * units.deg)
        .add_sample(1120.0 * units.rpm, 16.9 * units.deg)
        .add_sample(1280.0 * units.rpm, 18.6 * units.deg)
        .add_sample(1440.0 * units.rpm, 20.1 * units.deg)
        .add_sample(1600.0 * units.rpm, 21.5 * units.deg)
        .add_sample(1760.0 * units.rpm, 22.6 * units.deg)
        .add_sample(1920.0 * units.rpm, 23.6 * units.deg)
        .add_sample(2080.0 * units.rpm, 24.4 * units.deg)
        .add_sample(2240.0 * units.rpm, 25.0 * units.deg)
        .add_sample(2400.0 * units.rpm, 25.0 * units.deg)
        .add_sample(2560.0 * units.rpm, -15.0 * units.deg)
        .add_sample(2720.0 * units.rpm, -45.0 * units.deg)

    engine
        .add_crankshaft(c0.c)
        .add_cylinder_bank(bank0.bank)
        .add_ignition_module(ignition)
}

label spacing_factor(1.25)
label flange_density(1.0 * exhaust_delay_coeff)
label em_unit(bore * spacing_factor / flange_density)

// std1
private node es0{
    output es0: exhaust0; output es1: exhaust0; output es2: exhaust0;output es3: exhaust0;
    output pl0: _pl0; output pl1: _pl1; output pl2: _pl2; output pl3: _pl3;

    label collector_cross_section_area(11.4)
    label exhaust_pipe_length_0(354.0) // cm
    label exhaust_volume_0(collector_cross_section_area * exhaust_pipe_length_0 / 100.0) // Litres

    float _pl0 ((4.0 * em_unit) * units.mm)
    float _pl1 ((2.8 * em_unit) * units.mm)
    float _pl2 ((2.2 * em_unit) * units.mm)
    float _pl3 ((1.0 * em_unit) * units.mm)

    exhaust_system_parameters es_params0(
        outlet_flow_rate: k_carb(235.7),
        collector_cross_section_area: 11.4 * units.cm2,
        length: exhaust_pipe_length_0 * units.cm,
        primary_tube_length: 73.9 * units.cm,
        primary_flow_rate: k_carb(120.8),
        velocity_decay: 1.0,
        volume: (exhaust_volume_0 + 10.0) * units.L
    )

    exhaust_system exhaust0(es_params0, impulse_response: impulse_response(filename: "../../impulse_responses/smooth_51.wav", volume: 0.00025))
}

// Templar
public node veh {
    alias output __out: vehicle;
    label car_mass(1500) // ?
    vehicle vehicle(
        mass: car_mass * units.kg,
        drag_coefficient: 0.48,
        cross_sectional_area: (1700 * units.mm) * (1600 * units.mm), // ?
        diff_ratio: 5.10,
        tire_radius: (852.0 / 2) * units.mm, // 33x4.5
        rolling_resistance: 0.012 * car_mass * 9.81
        )
}

// Templar
private node trn {
    alias output __out:
        transmission(
            max_clutch_torque: 250 * units.Nm,
            max_clutch_flex: 30 * units.deg,
            limit_clutch_flex: true,
            clutch_stiffness: 20 * units.Nm / units.deg,
            clutch_damping: 1.0,
            simulate_flex: true
        )
        .add_gear(3.114)
        .add_gear(1.69)
        .add_gear(1.00);
}

run(
    engine: eng(),
    transmission: trn(),
    vehicle: veh()
)
