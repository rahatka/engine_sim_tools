// Engine Sim V0.1.14A
// 1925 Bugatti T37 49 @ 4200
// Created by oror 2024

/*
{
  "cam_cfg": {
    "equal_base_radius": false,
    "es_version": "0.1.14a",
    "exhaust_at_lift": 0.508,
    "exhaust_base_mult": 1.583,
    "exhaust_cos": false,
    "exhaust_sigma": 2.0,
    "exhaust_trim": 0.8,
    "exhaust_volume": 0.5,
    "intake_at_lift": 0.508,
    "intake_base_mult": 1.583,
    "intake_cos": false,
    "intake_sigma": 2.0,
    "intake_trim": 0.8,
    "intake_volume": 0.5,
    "lift_significant_fraction": 250.0,
    "ramp_position": 0.0,
    "ramp_steepness": 1.0,
    "resolution": 64,
    "roller_tappet_radius": 0.0
  },
  "flow_cfg": {
    "collector_area_coeff": 0.5,
    "exhaust_flange_dia": null,
    "exhaust_port_dia": null,
    "exhaust_stem_dia": null,
    "intake_port_dia": null,
    "intake_runner_dia_mult": 1.04,
    "intake_stem_dia": null,
    "ir_to_er_ratio": 3.0,
    "max_power_rpm": 4200.0,
    "max_torque_rpm": 2000.0,
    "port_to_valve_area": 0.85,
    "power_factor": 1.0,
    "primary_area_coeff": 1.1,
    "resolution": 32,
    "smoothness": 2.5,
    "valve_to_stem_dia": 3.3
  },
  "ign_cfg": {
    "end_deg": 20,
    "end_rpm": 3000,
    "exp_mode": 2,
    "flywheel": 0,
    "resolution": 16,
    "start_deg": 0,
    "start_rpm": 400
  }
}
*/

import "engine_sim.mr"
import "../../fuel/racing.mr"

units units()
constants constants()

label bore(69.0)
label stroke(100.0)
label compression_ratio(6.0)
label con_rod(200.0)
label compression_height(83.0 / 2 * units.mm) // PH 83
label intake_valve_diameter(26.0)
label exhaust_valve_diameter(37.0)
label intake_valves(2.0)
label exhaust_valves(1.0)

label redline(4500.0) // RPM

label cyl(4.0)
label row(1.0)
label cycle(720.0 * units.deg)
label rot(cycle / cyl)
label cyl_base(cyl / row)
label rot_base(cycle / cyl_base)
label rot90(90.0 * units.deg)
label rot180(180.0 * units.deg)
label rot360(360.0 * units.deg)

// 1925 T37
label IVL(7.5)
label EVL(8.5) // ?
label IVO(10.0 * units.deg) //BTDC
label IVC(35.0 * units.deg) //ABDC
label EVO(50.0 * units.deg) //BBDC
label EVC(20.0 * units.deg) //ATDC

label intake_duration(IVO + IVC + rot180)
label exhaust_duration(EVO + EVC + rot180)

label ILC((rot180 - IVO + IVC) / 2.0 + rot360)
label ELC((rot180 - EVO + EVC) / 2.0 + rot180)

label con_rod_mass(700.0) // ?
label piston_mass(500.0) // ?
label crank_mass(8.0) // ?
label flywheel_mass(3.0) // kg

label exhaust_delay_coeff(1.5)

public node variant_0 {
    output s1: 0;
    output s2: 1;
    output s4: 2;
    output s3: 3;
}

public node intake_lobe_profile {
    // lobes.py v1.0 | 225.0 250.0 LSA 103.8 ADV 1.2 OVL 30.0
    alias output __out: _lobe_profile;
    function _lobe_profile(2.252 * units.deg)
        _lobe_profile
        .add_sample(-74.307 * units.deg, 0.000 * units.mm)
        .add_sample(-72.055 * units.deg, 0.025 * units.mm)
        .add_sample(-69.803 * units.deg, 0.148 * units.mm)
        .add_sample(-67.551 * units.deg, 0.267 * units.mm)
        .add_sample(-65.300 * units.deg, 0.372 * units.mm)
        .add_sample(-63.048 * units.deg, 0.492 * units.mm)
        .add_sample(-60.796 * units.deg, 0.637 * units.mm)
        .add_sample(-58.545 * units.deg, 0.812 * units.mm)
        .add_sample(-56.293 * units.deg, 1.034 * units.mm)
        .add_sample(-54.041 * units.deg, 1.324 * units.mm)
        .add_sample(-51.789 * units.deg, 1.694 * units.mm)
        .add_sample(-49.538 * units.deg, 2.096 * units.mm)
        .add_sample(-47.286 * units.deg, 2.508 * units.mm)
        .add_sample(-45.034 * units.deg, 2.908 * units.mm)
        .add_sample(-42.783 * units.deg, 3.317 * units.mm)
        .add_sample(-40.531 * units.deg, 3.705 * units.mm)
        .add_sample(-38.279 * units.deg, 4.073 * units.mm)
        .add_sample(-36.027 * units.deg, 4.445 * units.mm)
        .add_sample(-33.776 * units.deg, 4.792 * units.mm)
        .add_sample(-31.524 * units.deg, 5.113 * units.mm)
        .add_sample(-29.272 * units.deg, 5.419 * units.mm)
        .add_sample(-27.021 * units.deg, 5.719 * units.mm)
        .add_sample(-24.769 * units.deg, 5.992 * units.mm)
        .add_sample(-22.517 * units.deg, 6.237 * units.mm)
        .add_sample(-20.265 * units.deg, 6.454 * units.mm)
        .add_sample(-18.014 * units.deg, 6.668 * units.mm)
        .add_sample(-15.762 * units.deg, 6.859 * units.mm)
        .add_sample(-13.510 * units.deg, 7.021 * units.mm)
        .add_sample(-11.259 * units.deg, 7.154 * units.mm)
        .add_sample(-9.007 * units.deg, 7.261 * units.mm)
        .add_sample(-6.755 * units.deg, 7.366 * units.mm)
        .add_sample(-4.503 * units.deg, 7.440 * units.mm)
        .add_sample(-2.252 * units.deg, 7.485 * units.mm)
        .add_sample(0.000 * units.deg, 7.500 * units.mm)
        .add_sample(2.252 * units.deg, 7.485 * units.mm)
        .add_sample(4.503 * units.deg, 7.440 * units.mm)
        .add_sample(6.755 * units.deg, 7.366 * units.mm)
        .add_sample(9.007 * units.deg, 7.261 * units.mm)
        .add_sample(11.259 * units.deg, 7.154 * units.mm)
        .add_sample(13.510 * units.deg, 7.021 * units.mm)
        .add_sample(15.762 * units.deg, 6.859 * units.mm)
        .add_sample(18.014 * units.deg, 6.668 * units.mm)
        .add_sample(20.265 * units.deg, 6.454 * units.mm)
        .add_sample(22.517 * units.deg, 6.237 * units.mm)
        .add_sample(24.769 * units.deg, 5.992 * units.mm)
        .add_sample(27.021 * units.deg, 5.719 * units.mm)
        .add_sample(29.272 * units.deg, 5.419 * units.mm)
        .add_sample(31.524 * units.deg, 5.113 * units.mm)
        .add_sample(33.776 * units.deg, 4.792 * units.mm)
        .add_sample(36.027 * units.deg, 4.445 * units.mm)
        .add_sample(38.279 * units.deg, 4.073 * units.mm)
        .add_sample(40.531 * units.deg, 3.705 * units.mm)
        .add_sample(42.783 * units.deg, 3.317 * units.mm)
        .add_sample(45.034 * units.deg, 2.908 * units.mm)
        .add_sample(47.286 * units.deg, 2.508 * units.mm)
        .add_sample(49.538 * units.deg, 2.096 * units.mm)
        .add_sample(51.789 * units.deg, 1.694 * units.mm)
        .add_sample(54.041 * units.deg, 1.324 * units.mm)
        .add_sample(56.293 * units.deg, 1.034 * units.mm)
        .add_sample(58.545 * units.deg, 0.812 * units.mm)
        .add_sample(60.796 * units.deg, 0.637 * units.mm)
        .add_sample(63.048 * units.deg, 0.492 * units.mm)
        .add_sample(65.300 * units.deg, 0.372 * units.mm)
        .add_sample(67.551 * units.deg, 0.267 * units.mm)
        .add_sample(69.803 * units.deg, 0.148 * units.mm)
        .add_sample(72.055 * units.deg, 0.025 * units.mm)
        .add_sample(74.307 * units.deg, 0.000 * units.mm)
}

public node exhaust_lobe_profile {
    // lobes.py v1.0 | 225.0 250.0 LSA 103.8 ADV 1.2 OVL 30.0
    alias output __out: _lobe_profile;
    function _lobe_profile(2.440 * units.deg)
        _lobe_profile
        .add_sample(-78.090 * units.deg, 0.000 * units.mm)
        .add_sample(-75.650 * units.deg, 0.101 * units.mm)
        .add_sample(-73.209 * units.deg, 0.239 * units.mm)
        .add_sample(-70.769 * units.deg, 0.374 * units.mm)
        .add_sample(-68.329 * units.deg, 0.492 * units.mm)
        .add_sample(-65.888 * units.deg, 0.632 * units.mm)
        .add_sample(-63.448 * units.deg, 0.795 * units.mm)
        .add_sample(-61.008 * units.deg, 0.988 * units.mm)
        .add_sample(-58.567 * units.deg, 1.219 * units.mm)
        .add_sample(-56.127 * units.deg, 1.500 * units.mm)
        .add_sample(-53.687 * units.deg, 1.849 * units.mm)
        .add_sample(-51.247 * units.deg, 2.275 * units.mm)
        .add_sample(-48.806 * units.deg, 2.741 * units.mm)
        .add_sample(-46.366 * units.deg, 3.220 * units.mm)
        .add_sample(-43.926 * units.deg, 3.686 * units.mm)
        .add_sample(-41.485 * units.deg, 4.161 * units.mm)
        .add_sample(-39.045 * units.deg, 4.608 * units.mm)
        .add_sample(-36.605 * units.deg, 5.036 * units.mm)
        .add_sample(-34.164 * units.deg, 5.461 * units.mm)
        .add_sample(-31.724 * units.deg, 5.851 * units.mm)
        .add_sample(-29.284 * units.deg, 6.207 * units.mm)
        .add_sample(-26.843 * units.deg, 6.560 * units.mm)
        .add_sample(-24.403 * units.deg, 6.886 * units.mm)
        .add_sample(-21.963 * units.deg, 7.174 * units.mm)
        .add_sample(-19.522 * units.deg, 7.426 * units.mm)
        .add_sample(-17.082 * units.deg, 7.668 * units.mm)
        .add_sample(-14.642 * units.deg, 7.884 * units.mm)
        .add_sample(-12.202 * units.deg, 8.061 * units.mm)
        .add_sample(-9.761 * units.deg, 8.200 * units.mm)
        .add_sample(-7.321 * units.deg, 8.321 * units.mm)
        .add_sample(-4.881 * units.deg, 8.420 * units.mm)
        .add_sample(-2.440 * units.deg, 8.480 * units.mm)
        .add_sample(0.000 * units.deg, 8.500 * units.mm)
        .add_sample(2.440 * units.deg, 8.480 * units.mm)
        .add_sample(4.881 * units.deg, 8.420 * units.mm)
        .add_sample(7.321 * units.deg, 8.321 * units.mm)
        .add_sample(9.761 * units.deg, 8.200 * units.mm)
        .add_sample(12.202 * units.deg, 8.061 * units.mm)
        .add_sample(14.642 * units.deg, 7.884 * units.mm)
        .add_sample(17.082 * units.deg, 7.668 * units.mm)
        .add_sample(19.522 * units.deg, 7.426 * units.mm)
        .add_sample(21.963 * units.deg, 7.174 * units.mm)
        .add_sample(24.403 * units.deg, 6.886 * units.mm)
        .add_sample(26.843 * units.deg, 6.560 * units.mm)
        .add_sample(29.284 * units.deg, 6.207 * units.mm)
        .add_sample(31.724 * units.deg, 5.851 * units.mm)
        .add_sample(34.164 * units.deg, 5.461 * units.mm)
        .add_sample(36.605 * units.deg, 5.036 * units.mm)
        .add_sample(39.045 * units.deg, 4.608 * units.mm)
        .add_sample(41.485 * units.deg, 4.161 * units.mm)
        .add_sample(43.926 * units.deg, 3.686 * units.mm)
        .add_sample(46.366 * units.deg, 3.220 * units.mm)
        .add_sample(48.806 * units.deg, 2.741 * units.mm)
        .add_sample(51.247 * units.deg, 2.275 * units.mm)
        .add_sample(53.687 * units.deg, 1.849 * units.mm)
        .add_sample(56.127 * units.deg, 1.500 * units.mm)
        .add_sample(58.567 * units.deg, 1.219 * units.mm)
        .add_sample(61.008 * units.deg, 0.988 * units.mm)
        .add_sample(63.448 * units.deg, 0.795 * units.mm)
        .add_sample(65.888 * units.deg, 0.632 * units.mm)
        .add_sample(68.329 * units.deg, 0.492 * units.mm)
        .add_sample(70.769 * units.deg, 0.374 * units.mm)
        .add_sample(73.209 * units.deg, 0.239 * units.mm)
        .add_sample(75.650 * units.deg, 0.101 * units.mm)
        .add_sample(78.090 * units.deg, 0.000 * units.mm)
}

public node intake_flow {
    alias output __out: _intake_flow;
    function _intake_flow(0.242 * units.mm)
    _intake_flow
        .add_flow_sample(0.000, 0.0)
        .add_flow_sample(0.242, 8.3)
        .add_flow_sample(0.484, 16.4)
        .add_flow_sample(0.726, 24.0)
        .add_flow_sample(0.968, 31.1)
        .add_flow_sample(1.210, 37.6)
        .add_flow_sample(1.452, 43.8)
        .add_flow_sample(1.694, 49.6)
        .add_flow_sample(1.935, 55.0)
        .add_flow_sample(2.177, 60.2)
        .add_flow_sample(2.419, 65.2)
        .add_flow_sample(2.661, 70.1)
        .add_flow_sample(2.903, 74.7)
        .add_flow_sample(3.145, 79.2)
        .add_flow_sample(3.387, 83.6)
        .add_flow_sample(3.629, 87.8)
        .add_flow_sample(3.871, 91.9)
        .add_flow_sample(4.113, 95.9)
        .add_flow_sample(4.355, 99.7)
        .add_flow_sample(4.597, 103.3)
        .add_flow_sample(4.839, 106.6)
        .add_flow_sample(5.081, 109.5)
        .add_flow_sample(5.323, 111.9)
        .add_flow_sample(5.565, 113.7)
        .add_flow_sample(5.806, 115.0)
        .add_flow_sample(6.048, 115.8)
        .add_flow_sample(6.290, 116.3)
        .add_flow_sample(6.532, 116.5)
        .add_flow_sample(6.774, 116.7)
        .add_flow_sample(7.016, 116.8)
        .add_flow_sample(7.258, 116.9)
        .add_flow_sample(7.500, 116.9)
}

public node exhaust_flow {
    alias output __out: _exhaust_flow;
    function _exhaust_flow(0.274 * units.mm)
    _exhaust_flow
        .add_flow_sample(0.000, 0.0)
        .add_flow_sample(0.274, 8.2)
        .add_flow_sample(0.548, 16.1)
        .add_flow_sample(0.823, 23.7)
        .add_flow_sample(1.097, 30.7)
        .add_flow_sample(1.371, 37.3)
        .add_flow_sample(1.645, 43.4)
        .add_flow_sample(1.919, 49.2)
        .add_flow_sample(2.194, 54.7)
        .add_flow_sample(2.468, 60.0)
        .add_flow_sample(2.742, 65.1)
        .add_flow_sample(3.016, 70.0)
        .add_flow_sample(3.290, 74.8)
        .add_flow_sample(3.565, 79.4)
        .add_flow_sample(3.839, 83.8)
        .add_flow_sample(4.113, 88.2)
        .add_flow_sample(4.387, 92.4)
        .add_flow_sample(4.661, 96.5)
        .add_flow_sample(4.935, 100.6)
        .add_flow_sample(5.210, 104.5)
        .add_flow_sample(5.484, 108.4)
        .add_flow_sample(5.758, 112.1)
        .add_flow_sample(6.032, 115.8)
        .add_flow_sample(6.306, 119.4)
        .add_flow_sample(6.581, 122.8)
        .add_flow_sample(6.855, 126.1)
        .add_flow_sample(7.129, 129.0)
        .add_flow_sample(7.403, 131.6)
        .add_flow_sample(7.677, 133.8)
        .add_flow_sample(7.952, 135.5)
        .add_flow_sample(8.226, 136.9)
        .add_flow_sample(8.500, 138.1)
}

intake_lobe_profile intake_lobe_profile()
exhaust_lobe_profile exhaust_lobe_profile()

intake_flow intake_flow()
exhaust_flow exhaust_flow()

private node add_lobes {
    input base;
    input shift: 0.0;
    input var;
    input this;
    alias output __out: this;

    this.add_lobe(base + var.s1 * rot_base + shift)
    this.add_lobe(base + var.s2 * rot_base + shift)
    this.add_lobe(base + var.s3 * rot_base + shift)
    this.add_lobe(base + var.s4 * rot_base + shift)
}

public node bank_builder {
    input var;
    input crankshaft;
    input incline;
    input phasing;
    input ignition;
    input intake;
    input exhaust;
    input flip: false;

    output bank: _b0;

    ignition_wire _wire1()
    ignition_wire _wire2()
    ignition_wire _wire3()
    ignition_wire _wire4()

    piston_parameters piston_params(
        mass: piston_mass * units.g,
        blowby: k_28inH2O(0.022),
        compression_height: compression_height,
        wrist_pin_position: 0.0,
        displacement: 0.0
    )

    cylinder_bank_parameters bank_params(
        bore: bore * units.mm,
        deck_height: (con_rod + stroke / 2.0) * units.mm + compression_height
    )

    connecting_rod_parameters cr_params(
        mass: con_rod_mass * units.g,
        moment_of_inertia: rod_moment_of_inertia(
            mass: con_rod_mass * units.g,
            length: con_rod * units.mm
        ),
        center_of_mass: 0.0,
        length: con_rod * units.mm
    )

    camshaft _intake_cam_0(base_radius: 11.9 * units.mm, lobe_profile: intake_lobe_profile)
    camshaft _exhaust_cam_0(base_radius: 13.5 * units.mm, lobe_profile: exhaust_lobe_profile)

    _intake_cam_0.add_lobes(ILC, phasing, var)
    _exhaust_cam_0.add_lobes(ELC, phasing, var)

    ignition.connect_wire(_wire1, rot_base * var.s1 + phasing)
    ignition.connect_wire(_wire2, rot_base * var.s2 + phasing)
    ignition.connect_wire(_wire3, rot_base * var.s3 + phasing)
    ignition.connect_wire(_wire4, rot_base * var.s4 + phasing)

    cylinder_bank _b0(bank_params, angle: incline)

    _b0
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj0,
            intake: intake,
            exhaust_system: exhaust.es0,
            primary_length: exhaust.pl0,
            ignition_wire: _wire1
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj1,
            intake: intake,
            exhaust_system: exhaust.es1,
            primary_length: exhaust.pl1,
            ignition_wire: _wire2
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj2,
            intake: intake,
            exhaust_system: exhaust.es2,
            primary_length: exhaust.pl2,
            ignition_wire: _wire3
        )
    .add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: crankshaft.rj3,
            intake: intake,
            exhaust_system: exhaust.es3,
            primary_length: exhaust.pl3,
            ignition_wire: _wire4
        )

    _b0.set_cylinder_head(
        head(
            intake_camshaft: _intake_cam_0,
            exhaust_camshaft: _exhaust_cam_0,
            flip_display: flip
        )
    )
}

private node add_flow_sample {
    input lift;
    input flow;
    input this;
    alias output __out: this;

    this.add_sample(lift * units.mm, k_28inH2O(flow))
}

public node head {
    // port_flow.py v1.7
    // intake port area: 8.1 cm²; saturated lift: 5.35 mm
    // exhaust port area: 8.2 cm²; saturated lift: 7.61 mm
    // cylinder volume: 373.9 cm³ (22.82 CI); engine volume: 1.496 L (91.27 CI)
    // chamber volume: 74.8 cm³ (4.56 CI); conrod/stroke 2.000; N.A.C.C. 11.81
    // 16 harmonic intake runner length: 22.2 cm; diameter: 3.1 cm
    // primary length: 29.9 cm, area: 9.0 cm², diameter: 3.4 cm
    // collector diameter: 4.8 cm, area: 17.9 cm²

    input intake_camshaft;
    input exhaust_camshaft;
    input flip_display: false;
    
    alias output __out: head;
    generic_cylinder_head head(
        chamber_volume: 74.786 * units.cc,
        intake_runner_volume: 168.0 * units.cc,
        intake_runner_cross_section_area: 7.6 * units.cm2,
        exhaust_runner_volume: 56.0 * units.cc,
        exhaust_runner_cross_section_area: 9.0 * units.cm2,

        intake_port_flow: intake_flow,
        exhaust_port_flow: exhaust_flow,
        valvetrain: standard_valvetrain(
            intake_camshaft: intake_camshaft,
            exhaust_camshaft: exhaust_camshaft
        ),
        flip_display: flip_display
    )
}

public node crank {
    input phasing;
    input var;

    output c: _c;
    output rj0: _rj0;
    output rj1: _rj1;
    output rj2: _rj2;
    output rj3: _rj3;

    label crank_moment(disk_moment_of_inertia(mass: crank_mass * units.kg, radius: (stroke / 2.0) * units.mm))
    label flywheel_moment(disk_moment_of_inertia(mass: flywheel_mass * units.kg, radius: (stroke * 3.5) * units.mm))
    label other_moment(disk_moment_of_inertia(mass: 1 * units.kg, radius: 1.0 * units.cm))

    crankshaft _c(
        throw: (stroke / 2.0) * units.mm,
        flywheel_mass: flywheel_mass * units.kg,
        mass: crank_mass * units.kg,
        friction_torque: 2.2 * units.Nm,
        moment_of_inertia: crank_moment + flywheel_moment + other_moment,
        tdc: 0.0
    )

    rod_journal _rj0(angle: rot_base * var.s1 + rot90)
    rod_journal _rj1(angle: rot_base * var.s2 + rot90)
    rod_journal _rj2(angle: rot_base * var.s3 + rot90)
    rod_journal _rj3(angle: rot_base * var.s4 + rot90)

    _c
        .add_rod_journal(_rj0)
        .add_rod_journal(_rj1)
        .add_rod_journal(_rj2)
        .add_rod_journal(_rj3)
}

public node eng {
    alias output __out: engine;

    engine engine(
        name: "Bugatti T37",
        starter_torque: 200 * units.Nm,
        starter_speed: 150 * units.rpm,
        redline: redline * units.rpm,
        fuel: r20(),
        throttle_gamma: 1.5,
        jitter: 0.7,
        noise: 0.2,
        hf_gain: 0.007,
        simulation_frequency: 12000,
        dyno_min_speed: 500 * units.rpm
    )

    variant_0 variant()
    es0 es()

    crank c0(rot90, variant)

    bank_builder bank0(
        var: variant,
        crankshaft: c0,
        incline: 0,
        phasing: 0,
        ignition: ignition,
        intake: intake,
        exhaust: es,
        flip: true
    )

    ignition_module ignition(timing_curve: timing_curve, rev_limit: redline * 1.5 * units.rpm, limiter_duration: 0.03)

    intake intake(
        plenum_volume: 0.9 * units.L,
        plenum_cross_section_area: 15.2 * units.cm2,
        runner_length: 22.2 * units.cm,
        intake_flow_rate: k_carb(112.2),
        idle_flow_rate: k_carb(0.02),
        idle_throttle_plate_position: 0.995,
        runner_flow_rate: k_carb(56.1),
        velocity_decay: 0.05
    )

    function timing_curve(300.0 * units.rpm)
    timing_curve
        .add_sample(0.0 * units.rpm, 0.0 * units.deg)
        .add_sample(300.0 * units.rpm, 0.0 * units.deg)
        .add_sample(600.0 * units.rpm, 5.1 * units.deg)
        .add_sample(900.0 * units.rpm, 9.8 * units.deg)
        .add_sample(1200.0 * units.rpm, 13.0 * units.deg)
        .add_sample(1500.0 * units.rpm, 15.4 * units.deg)
        .add_sample(1800.0 * units.rpm, 17.0 * units.deg)
        .add_sample(2100.0 * units.rpm, 18.2 * units.deg)
        .add_sample(2400.0 * units.rpm, 19.0 * units.deg)
        .add_sample(2700.0 * units.rpm, 19.6 * units.deg)
        .add_sample(3000.0 * units.rpm, 20.0 * units.deg)
        .add_sample(3300.0 * units.rpm, 20.0 * units.deg)
        .add_sample(3600.0 * units.rpm, 20.0 * units.deg)
        .add_sample(3900.0 * units.rpm, 20.0 * units.deg)
        .add_sample(4200.0 * units.rpm, 20.0 * units.deg)
        .add_sample(4500.0 * units.rpm, 20.0 * units.deg)
        .add_sample(4800.0 * units.rpm, -12.5 * units.deg)
        .add_sample(5100.0 * units.rpm, -25.0 * units.deg)

    engine
        .add_crankshaft(c0.c)
        .add_cylinder_bank(bank0.bank)
        .add_ignition_module(ignition)
}

label spacing_factor(1.25)
label flange_density(1.0 * exhaust_delay_coeff)
label em_unit(bore * spacing_factor / flange_density)

// std1
private node es0{
    output es0: exhaust0; output es1: exhaust0; output es2: exhaust0; output es3: exhaust0;
    output pl0: _pl0; output pl1: _pl1; output pl2: _pl2; output pl3: _pl3;

    label collector_cross_section_area(17.9)
    label exhaust_pipe_length_0(200.0) // cm
    label exhaust_volume_0(collector_cross_section_area * exhaust_pipe_length_0 / 100.0) // Litres

    float _pl0 ((1.5 * em_unit) * units.mm)
    float _pl1 ((1.0 * em_unit) * units.mm)
    float _pl2 ((1.0 * em_unit) * units.mm)
    float _pl3 ((1.5 * em_unit) * units.mm)

    exhaust_system_parameters es_params0(
        outlet_flow_rate: k_carb(187.0),
        collector_cross_section_area: 17.9 * units.cm2,
        length: exhaust_pipe_length_0 * units.cm,
        primary_tube_length: 29.9 * units.cm,
        primary_flow_rate: k_carb(93.5),
        velocity_decay: 0.8,
        volume: exhaust_volume_0 * units.L
    )

    exhaust_system exhaust0(es_params0, impulse_response: impulse_response(filename: "../../impulse_responses/smooth_56.wav", volume: 0.001))
}

// Templar
public node veh {
    alias output __out: vehicle;
    label car_mass(1500) // ?
    vehicle vehicle(
        mass: car_mass * units.kg,
        drag_coefficient: 0.48,
        cross_sectional_area: (1700 * units.mm) * (1600 * units.mm), // ?
        diff_ratio: 5.10,
        tire_radius: (852.0 / 2) * units.mm, // 33x4.5
        rolling_resistance: 0.012 * car_mass * 9.81
        )
}

// Templar
private node trn {
    alias output __out:
        transmission(
            max_clutch_torque: 250 * units.Nm,
            max_clutch_flex: 30 * units.deg,
            limit_clutch_flex: true,
            clutch_stiffness: 20 * units.Nm / units.deg,
            clutch_damping: 1.0,
            simulate_flex: true
        )
        .add_gear(3.114)
        .add_gear(1.69)
        .add_gear(1.00);
}

run(
    engine: eng(),
    transmission: trn(),
    vehicle: veh()
)
