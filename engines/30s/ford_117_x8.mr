// Engine Sim V0.1.14A
// Ford X8 Water Cooled #28
// ford_117_x8
// Created by oror 2023
// engine layout
// 1st row
// 1  2
//  \/
//  /\
// 4  3
//
// 2nd row
// 5  6
//  \/
//  /\
// 8  7
// 1-8-3-6-7-2-5-4

/*
{
  "cam_cfg": {
    "equal_base_radius": false,
    "es_version": "0.1.14a",
    "exhaust_at_lift": 0.508,
    "exhaust_base_mult": 1.75,
    "exhaust_cos": false,
    "exhaust_sigma": 1.5,
    "exhaust_trim": 0.9,
    "exhaust_volume": 0.5,
    "intake_at_lift": 0.508,
    "intake_base_mult": 1.75,
    "intake_cos": false,
    "intake_sigma": 2.0,
    "intake_trim": 0.9,
    "intake_volume": 0.5,
    "lift_significant_fraction": 250.0,
    "ramp_position": 0.0,
    "ramp_steepness": 0.0,
    "resolution": 64,
    "roller_tappet_radius": 0.0
  },
  "engine": {
    "catalog_id": 308,
    "description": [
      "This is an experimental Ford X8 engine (Sugar Mill experiment #28).",
      "I was able to found pictures and bore x stroke figures, the rest is my speculation,",
      "but it should be relatively accurate to what I have seen on pictures and the patent list."
    ],
    "enable_update": true,
    "es_version": "V0.1.14A",
    "name": "Ford Experimental 117CI X8 1927",
    "yt_handle": "kxygNqL-Vf8"
  },
  "flow_cfg": {
    "collector_area_coeff": 0.3,
    "exhaust_flange_dia": null,
    "exhaust_port_dia": null,
    "exhaust_stem_dia": null,
    "intake_port_dia": null,
    "intake_runner_dia_mult": 1.08,
    "intake_stem_dia": null,
    "ir_to_er_ratio": 3.0,
    "max_power_rpm": 3300,
    "max_torque_rpm": 2000,
    "port_to_valve_area": 0.82,
    "power_factor": 1.1,
    "primary_area_coeff": 1.1,
    "resolution": 32,
    "smoothness": 2.0,
    "valve_to_stem_dia": 4.8
  },
  "ign_cfg": {
    "end_deg": 25,
    "end_rpm": 3300,
    "exp_mode": 2,
    "flywheel": 5,
    "resolution": 16,
    "start_rpm": 200
  }
}
*/

import "engine_sim.mr"
import "../../fuel/30.mr"

units units()
constants constants()

label bore(54.7687) // 2 5/32
label stroke(101.6) // 4
label compression_ratio(6.5)
label con_rod(190.5) // 7 1/2

label compression_height(34.5 * units.mm) // ?
label intake_valve_diameter(27.7813) // 1 3/32
label exhaust_valve_diameter(25.0031) // 63/64
label intake_valves(1.0)
label exhaust_valves(1.0)

label redline(3500.0) // RPM

label cyl(8.0)
label row(4.0)
label cycle(720.0 * units.deg)
label vee(90.0 * units.deg)
label rot(cycle / cyl)
label rot90(90.0 * units.deg)
label rot180(180.0 * units.deg)
label rot360(360.0 * units.deg)

// 221 V8
label IVL(7.493) // .295
label EVL(7.493) // .295
label IVO(9.3 * units.deg) //BTDC
label IVC(54.3 * units.deg) //ABDC
label EVO(57.3 * units.deg) //BBDC
label EVC(6.3 * units.deg) //ATDC

label intake_duration(IVO + IVC + rot180)
label exhaust_duration(EVO + EVC + rot180)

label ILC((rot180 - IVO + IVC) / 2.0 + rot360)
label ELC((rot180 - EVO + EVC) / 2.0 + rot180)

label con_rod_mass(490.0) // g
label crank_mass(10.0) // kg
label flywheel_mass(5.0) // kg

label exhaust_delay_coeff(1.5)

public node distributor_0 {
    input wires;
    input timing_curve;
    input rev_limit: 4750 * units.rpm;
    input limiter_duration: 0.0001;
    alias output __out:
        ignition_module(timing_curve: timing_curve, rev_limit: rev_limit, limiter_duration: limiter_duration)
            .connect_wire(wires.wire1, rot * 0)
            .connect_wire(wires.wire8, rot * 1)
            .connect_wire(wires.wire3, rot * 2)
            .connect_wire(wires.wire6, rot * 3)
            .connect_wire(wires.wire7, rot * 4)
            .connect_wire(wires.wire2, rot * 5)
            .connect_wire(wires.wire5, rot * 6)
            .connect_wire(wires.wire4, rot * 7);
}

public node distributor_1 {
    input wires;
    input timing_curve;
    input rev_limit: 4750 * units.rpm;
    input limiter_duration: 0.0001;
    alias output __out:
        ignition_module(timing_curve: timing_curve, rev_limit: rev_limit, limiter_duration: limiter_duration)
            .connect_wire(wires.wire1, rot * 0)
            .connect_wire(wires.wire2, rot * 1)
            .connect_wire(wires.wire3, rot * 2)
            .connect_wire(wires.wire4, rot * 3)
            .connect_wire(wires.wire7, rot * 4)
            .connect_wire(wires.wire8, rot * 5)
            .connect_wire(wires.wire5, rot * 6)
            .connect_wire(wires.wire6, rot * 7);
}

private node wires {
    output wire1: ignition_wire();
    output wire2: ignition_wire();
    output wire3: ignition_wire();
    output wire4: ignition_wire();
    output wire5: ignition_wire();
    output wire6: ignition_wire();
    output wire7: ignition_wire();
    output wire8: ignition_wire();
}

public node intake_lobe_profile {
    // lobes.py v0.9f
    alias output __out: _lobe_profile;
    function _lobe_profile(2.655 * units.deg)
        _lobe_profile
        .add_sample(-84.968 * units.deg, 0.000 * units.mm)
        .add_sample(-82.313 * units.deg, 0.007 * units.mm)
        .add_sample(-79.658 * units.deg, 0.055 * units.mm)
        .add_sample(-77.002 * units.deg, 0.117 * units.mm)
        .add_sample(-74.347 * units.deg, 0.192 * units.mm)
        .add_sample(-71.692 * units.deg, 0.277 * units.mm)
        .add_sample(-69.037 * units.deg, 0.371 * units.mm)
        .add_sample(-66.381 * units.deg, 0.483 * units.mm)
        .add_sample(-63.726 * units.deg, 0.608 * units.mm)
        .add_sample(-61.071 * units.deg, 0.757 * units.mm)
        .add_sample(-58.416 * units.deg, 0.932 * units.mm)
        .add_sample(-55.760 * units.deg, 1.138 * units.mm)
        .add_sample(-53.105 * units.deg, 1.387 * units.mm)
        .add_sample(-50.450 * units.deg, 1.696 * units.mm)
        .add_sample(-47.795 * units.deg, 2.077 * units.mm)
        .add_sample(-45.139 * units.deg, 2.524 * units.mm)
        .add_sample(-42.484 * units.deg, 2.996 * units.mm)
        .add_sample(-39.829 * units.deg, 3.467 * units.mm)
        .add_sample(-37.174 * units.deg, 3.926 * units.mm)
        .add_sample(-34.518 * units.deg, 4.362 * units.mm)
        .add_sample(-31.863 * units.deg, 4.796 * units.mm)
        .add_sample(-29.208 * units.deg, 5.191 * units.mm)
        .add_sample(-26.553 * units.deg, 5.547 * units.mm)
        .add_sample(-23.897 * units.deg, 5.901 * units.mm)
        .add_sample(-21.242 * units.deg, 6.223 * units.mm)
        .add_sample(-18.587 * units.deg, 6.504 * units.mm)
        .add_sample(-15.932 * units.deg, 6.742 * units.mm)
        .add_sample(-13.276 * units.deg, 6.942 * units.mm)
        .add_sample(-10.621 * units.deg, 7.140 * units.mm)
        .add_sample(-7.966 * units.deg, 7.294 * units.mm)
        .add_sample(-5.311 * units.deg, 7.405 * units.mm)
        .add_sample(-2.655 * units.deg, 7.471 * units.mm)
        .add_sample(0.000 * units.deg, 7.493 * units.mm)
        .add_sample(2.655 * units.deg, 7.471 * units.mm)
        .add_sample(5.311 * units.deg, 7.405 * units.mm)
        .add_sample(7.966 * units.deg, 7.294 * units.mm)
        .add_sample(10.621 * units.deg, 7.140 * units.mm)
        .add_sample(13.276 * units.deg, 6.942 * units.mm)
        .add_sample(15.932 * units.deg, 6.742 * units.mm)
        .add_sample(18.587 * units.deg, 6.504 * units.mm)
        .add_sample(21.242 * units.deg, 6.223 * units.mm)
        .add_sample(23.897 * units.deg, 5.901 * units.mm)
        .add_sample(26.553 * units.deg, 5.547 * units.mm)
        .add_sample(29.208 * units.deg, 5.191 * units.mm)
        .add_sample(31.863 * units.deg, 4.796 * units.mm)
        .add_sample(34.518 * units.deg, 4.362 * units.mm)
        .add_sample(37.174 * units.deg, 3.926 * units.mm)
        .add_sample(39.829 * units.deg, 3.467 * units.mm)
        .add_sample(42.484 * units.deg, 2.996 * units.mm)
        .add_sample(45.139 * units.deg, 2.524 * units.mm)
        .add_sample(47.795 * units.deg, 2.077 * units.mm)
        .add_sample(50.450 * units.deg, 1.696 * units.mm)
        .add_sample(53.105 * units.deg, 1.387 * units.mm)
        .add_sample(55.760 * units.deg, 1.138 * units.mm)
        .add_sample(58.416 * units.deg, 0.932 * units.mm)
        .add_sample(61.071 * units.deg, 0.757 * units.mm)
        .add_sample(63.726 * units.deg, 0.608 * units.mm)
        .add_sample(66.381 * units.deg, 0.483 * units.mm)
        .add_sample(69.037 * units.deg, 0.371 * units.mm)
        .add_sample(71.692 * units.deg, 0.277 * units.mm)
        .add_sample(74.347 * units.deg, 0.192 * units.mm)
        .add_sample(77.002 * units.deg, 0.117 * units.mm)
        .add_sample(79.658 * units.deg, 0.055 * units.mm)
        .add_sample(82.313 * units.deg, 0.007 * units.mm)
        .add_sample(84.968 * units.deg, 0.000 * units.mm)
}

public node exhaust_lobe_profile {
    // lobes.py v0.9f
    alias output __out: _lobe_profile;
    function _lobe_profile(2.658 * units.deg)
        _lobe_profile
        .add_sample(-85.065 * units.deg, 0.000 * units.mm)
        .add_sample(-82.407 * units.deg, 0.010 * units.mm)
        .add_sample(-79.749 * units.deg, 0.064 * units.mm)
        .add_sample(-77.091 * units.deg, 0.125 * units.mm)
        .add_sample(-74.432 * units.deg, 0.196 * units.mm)
        .add_sample(-71.774 * units.deg, 0.278 * units.mm)
        .add_sample(-69.116 * units.deg, 0.368 * units.mm)
        .add_sample(-66.457 * units.deg, 0.473 * units.mm)
        .add_sample(-63.799 * units.deg, 0.592 * units.mm)
        .add_sample(-61.141 * units.deg, 0.732 * units.mm)
        .add_sample(-58.482 * units.deg, 0.894 * units.mm)
        .add_sample(-55.824 * units.deg, 1.086 * units.mm)
        .add_sample(-53.166 * units.deg, 1.318 * units.mm)
        .add_sample(-50.508 * units.deg, 1.599 * units.mm)
        .add_sample(-47.849 * units.deg, 1.950 * units.mm)
        .add_sample(-45.191 * units.deg, 2.381 * units.mm)
        .add_sample(-42.533 * units.deg, 2.865 * units.mm)
        .add_sample(-39.874 * units.deg, 3.358 * units.mm)
        .add_sample(-37.216 * units.deg, 3.828 * units.mm)
        .add_sample(-34.558 * units.deg, 4.299 * units.mm)
        .add_sample(-31.900 * units.deg, 4.732 * units.mm)
        .add_sample(-29.241 * units.deg, 5.127 * units.mm)
        .add_sample(-26.583 * units.deg, 5.517 * units.mm)
        .add_sample(-23.925 * units.deg, 5.880 * units.mm)
        .add_sample(-21.266 * units.deg, 6.202 * units.mm)
        .add_sample(-18.608 * units.deg, 6.483 * units.mm)
        .add_sample(-15.950 * units.deg, 6.722 * units.mm)
        .add_sample(-13.291 * units.deg, 6.941 * units.mm)
        .add_sample(-10.633 * units.deg, 7.139 * units.mm)
        .add_sample(-7.975 * units.deg, 7.294 * units.mm)
        .add_sample(-5.317 * units.deg, 7.404 * units.mm)
        .add_sample(-2.658 * units.deg, 7.471 * units.mm)
        .add_sample(0.000 * units.deg, 7.493 * units.mm)
        .add_sample(2.658 * units.deg, 7.471 * units.mm)
        .add_sample(5.317 * units.deg, 7.404 * units.mm)
        .add_sample(7.975 * units.deg, 7.294 * units.mm)
        .add_sample(10.633 * units.deg, 7.139 * units.mm)
        .add_sample(13.291 * units.deg, 6.941 * units.mm)
        .add_sample(15.950 * units.deg, 6.722 * units.mm)
        .add_sample(18.608 * units.deg, 6.483 * units.mm)
        .add_sample(21.266 * units.deg, 6.202 * units.mm)
        .add_sample(23.925 * units.deg, 5.880 * units.mm)
        .add_sample(26.583 * units.deg, 5.517 * units.mm)
        .add_sample(29.241 * units.deg, 5.127 * units.mm)
        .add_sample(31.900 * units.deg, 4.732 * units.mm)
        .add_sample(34.558 * units.deg, 4.299 * units.mm)
        .add_sample(37.216 * units.deg, 3.828 * units.mm)
        .add_sample(39.874 * units.deg, 3.358 * units.mm)
        .add_sample(42.533 * units.deg, 2.865 * units.mm)
        .add_sample(45.191 * units.deg, 2.381 * units.mm)
        .add_sample(47.849 * units.deg, 1.950 * units.mm)
        .add_sample(50.508 * units.deg, 1.599 * units.mm)
        .add_sample(53.166 * units.deg, 1.318 * units.mm)
        .add_sample(55.824 * units.deg, 1.086 * units.mm)
        .add_sample(58.482 * units.deg, 0.894 * units.mm)
        .add_sample(61.141 * units.deg, 0.732 * units.mm)
        .add_sample(63.799 * units.deg, 0.592 * units.mm)
        .add_sample(66.457 * units.deg, 0.473 * units.mm)
        .add_sample(69.116 * units.deg, 0.368 * units.mm)
        .add_sample(71.774 * units.deg, 0.278 * units.mm)
        .add_sample(74.432 * units.deg, 0.196 * units.mm)
        .add_sample(77.091 * units.deg, 0.125 * units.mm)
        .add_sample(79.749 * units.deg, 0.064 * units.mm)
        .add_sample(82.407 * units.deg, 0.010 * units.mm)
        .add_sample(85.065 * units.deg, 0.000 * units.mm)
}

public node intake_flow {
    alias output __out: _intake_flow;
    function _intake_flow(0.242 * units.mm)
    _intake_flow
        .add_flow_sample(0.000, 0.0)
        .add_flow_sample(0.242, 4.6)
        .add_flow_sample(0.483, 9.1)
        .add_flow_sample(0.725, 13.2)
        .add_flow_sample(0.967, 17.0)
        .add_flow_sample(1.209, 20.5)
        .add_flow_sample(1.450, 23.8)
        .add_flow_sample(1.692, 26.8)
        .add_flow_sample(1.934, 29.8)
        .add_flow_sample(2.175, 32.6)
        .add_flow_sample(2.417, 35.3)
        .add_flow_sample(2.659, 37.9)
        .add_flow_sample(2.901, 40.4)
        .add_flow_sample(3.142, 42.9)
        .add_flow_sample(3.384, 45.3)
        .add_flow_sample(3.626, 47.6)
        .add_flow_sample(3.867, 49.8)
        .add_flow_sample(4.109, 52.0)
        .add_flow_sample(4.351, 54.2)
        .add_flow_sample(4.592, 56.2)
        .add_flow_sample(4.834, 58.3)
        .add_flow_sample(5.076, 60.2)
        .add_flow_sample(5.318, 62.1)
        .add_flow_sample(5.559, 63.8)
        .add_flow_sample(5.801, 65.3)
        .add_flow_sample(6.043, 66.4)
        .add_flow_sample(6.284, 67.2)
        .add_flow_sample(6.526, 67.6)
        .add_flow_sample(6.768, 67.9)
        .add_flow_sample(7.010, 68.0)
        .add_flow_sample(7.251, 68.1)
        .add_flow_sample(7.493, 68.2)
}

public node exhaust_flow {
    alias output __out: _exhaust_flow;
    function _exhaust_flow(0.242 * units.mm)
    _exhaust_flow
        .add_flow_sample(0.000, 0.0)
        .add_flow_sample(0.242, 4.8)
        .add_flow_sample(0.483, 9.3)
        .add_flow_sample(0.725, 13.6)
        .add_flow_sample(0.967, 17.4)
        .add_flow_sample(1.209, 21.0)
        .add_flow_sample(1.450, 24.3)
        .add_flow_sample(1.692, 27.5)
        .add_flow_sample(1.934, 30.5)
        .add_flow_sample(2.175, 33.3)
        .add_flow_sample(2.417, 36.1)
        .add_flow_sample(2.659, 38.7)
        .add_flow_sample(2.901, 41.3)
        .add_flow_sample(3.142, 43.7)
        .add_flow_sample(3.384, 46.1)
        .add_flow_sample(3.626, 48.5)
        .add_flow_sample(3.867, 50.7)
        .add_flow_sample(4.109, 52.9)
        .add_flow_sample(4.351, 55.0)
        .add_flow_sample(4.592, 57.1)
        .add_flow_sample(4.834, 59.0)
        .add_flow_sample(5.076, 60.7)
        .add_flow_sample(5.318, 62.1)
        .add_flow_sample(5.559, 63.1)
        .add_flow_sample(5.801, 63.8)
        .add_flow_sample(6.043, 64.1)
        .add_flow_sample(6.284, 64.3)
        .add_flow_sample(6.526, 64.4)
        .add_flow_sample(6.768, 64.5)
        .add_flow_sample(7.010, 64.5)
        .add_flow_sample(7.251, 64.6)
        .add_flow_sample(7.493, 64.6)
}

intake_lobe_profile intake_lobe_profile()
exhaust_lobe_profile exhaust_lobe_profile()

intake_flow intake_flow()
exhaust_flow exhaust_flow()

public node camshaft_builder_0 {
    output intake_cam_0: _intake_cam_0;
    output exhaust_cam_0: _exhaust_cam_0;

    output intake_cam_1: _intake_cam_1;
    output exhaust_cam_1: _exhaust_cam_1;
    
    output intake_cam_2: _intake_cam_2;
    output exhaust_cam_2: _exhaust_cam_2;

    output intake_cam_3: _intake_cam_3;
    output exhaust_cam_3: _exhaust_cam_3;

    camshaft _intake_cam_0(base_radius: 13.1 * units.mm, lobe_profile: intake_lobe_profile)
    camshaft _exhaust_cam_0(base_radius: 13.1 * units.mm, lobe_profile: exhaust_lobe_profile)
    
    camshaft _intake_cam_1(base_radius: 13.1 * units.mm, lobe_profile: intake_lobe_profile)
    camshaft _exhaust_cam_1(base_radius: 13.1 * units.mm, lobe_profile: exhaust_lobe_profile)
    
    camshaft _intake_cam_2(base_radius: 13.1 * units.mm, lobe_profile: intake_lobe_profile)
    camshaft _exhaust_cam_2(base_radius: 13.1 * units.mm, lobe_profile: exhaust_lobe_profile)
    
    camshaft _intake_cam_3(base_radius: 13.1 * units.mm, lobe_profile: intake_lobe_profile)
    camshaft _exhaust_cam_3(base_radius: 13.1 * units.mm, lobe_profile: exhaust_lobe_profile)

    _intake_cam_0
        .add_lobe(ILC + 0 * rot) // 1
        .add_lobe(ILC + 6 * rot) // 5
    _exhaust_cam_0
        .add_lobe(ELC + 0 * rot) // 1
        .add_lobe(ELC + 6 * rot) // 5
        
    _intake_cam_1
        .add_lobe(ILC + 5 * rot) // 2
        .add_lobe(ILC + 3 * rot) // 6
    _exhaust_cam_1
        .add_lobe(ELC + 5 * rot) // 2
        .add_lobe(ELC + 3 * rot) // 6
        
    _intake_cam_2
        .add_lobe(ILC + 2 * rot) // 3
        .add_lobe(ILC + 4 * rot) // 7
    _exhaust_cam_2
        .add_lobe(ELC + 2 * rot) // 3
        .add_lobe(ELC + 4 * rot) // 7
        
    _intake_cam_3
        .add_lobe(ILC + 7 * rot) // 4
        .add_lobe(ILC + 1 * rot) // 8
    _exhaust_cam_3
        .add_lobe(ELC + 7 * rot) // 4
        .add_lobe(ELC + 1 * rot) // 8
}

public node camshaft_builder_1 {
    output intake_cam_0: _intake_cam_0;
    output exhaust_cam_0: _exhaust_cam_0;

    output intake_cam_1: _intake_cam_1;
    output exhaust_cam_1: _exhaust_cam_1;
    
    output intake_cam_2: _intake_cam_2;
    output exhaust_cam_2: _exhaust_cam_2;

    output intake_cam_3: _intake_cam_3;
    output exhaust_cam_3: _exhaust_cam_3;



    camshaft _intake_cam_0(base_radius: 13.1 * units.mm, lobe_profile: intake_lobe_profile)
    camshaft _exhaust_cam_0(base_radius: 13.1 * units.mm, lobe_profile: exhaust_lobe_profile)
    
    camshaft _intake_cam_1(base_radius: 13.1 * units.mm, lobe_profile: intake_lobe_profile)
    camshaft _exhaust_cam_1(base_radius: 13.1 * units.mm, lobe_profile: exhaust_lobe_profile)
    
    camshaft _intake_cam_2(base_radius: 13.1 * units.mm, lobe_profile: intake_lobe_profile)
    camshaft _exhaust_cam_2(base_radius: 13.1 * units.mm, lobe_profile: exhaust_lobe_profile)
    
    camshaft _intake_cam_3(base_radius: 13.1 * units.mm, lobe_profile: intake_lobe_profile)
    camshaft _exhaust_cam_3(base_radius: 13.1 * units.mm, lobe_profile: exhaust_lobe_profile)

    _intake_cam_0
        .add_lobe(ILC + 0 * rot) // 1
        .add_lobe(ILC + 6 * rot) // 5
    _exhaust_cam_0
        .add_lobe(ELC + 0 * rot) // 1
        .add_lobe(ELC + 6 * rot) // 5
        
    _intake_cam_1
        .add_lobe(ILC + 1 * rot) // 2
        .add_lobe(ILC + 7 * rot) // 6
    _exhaust_cam_1
        .add_lobe(ELC + 1 * rot) // 2
        .add_lobe(ELC + 7 * rot) // 6
        
    _intake_cam_2
        .add_lobe(ILC + 2 * rot) // 3
        .add_lobe(ILC + 4 * rot) // 7
    _exhaust_cam_2
        .add_lobe(ELC + 2 * rot) // 3
        .add_lobe(ELC + 4 * rot) // 7
        
    _intake_cam_3
        .add_lobe(ILC + 3 * rot) // 4
        .add_lobe(ILC + 5 * rot) // 8
    _exhaust_cam_3
        .add_lobe(ELC + 3 * rot) // 4
        .add_lobe(ELC + 5 * rot) // 8
}

private node add_flow_sample {
    input lift;
    input flow;
    input this;
    alias output __out: this;

    this.add_sample(lift * units.mm, k_28inH2O(flow))
}

public node head {
    // port_flow.py v1.7
    // intake port area: 4.7 cm²; saturated lift: 5.96 mm
    // exhaust port area: 3.8 cm²; saturated lift: 5.36 mm
    // cylinder volume: 239.4 cm³ (14.61 CI); engine volume: 1.915 L (116.85 CI)
    // chamber volume: 43.5 cm³ (2.66 CI); conrod/stroke 1.875; N.A.C.C. 14.88
    // 32 harmonic intake runner length: 13.6 cm; diameter: 2.3 cm
    // primary length: 37.5 cm, area: 4.2 cm², diameter: 2.3 cm
    // collector diameter: 3.6 cm, area: 10.1 cm²

    input intake_camshaft;
    input exhaust_camshaft;
    input flip_display: false;
    
    alias output __out: head;
    generic_cylinder_head head(
        chamber_volume: 43.520 * units.cc,
        intake_runner_volume: 55.7 * units.cc,
        intake_runner_cross_section_area: 4.1 * units.cm2,
        exhaust_runner_volume: 18.6 * units.cc,
        exhaust_runner_cross_section_area: 4.2 * units.cm2,

        intake_port_flow: intake_flow,
        exhaust_port_flow: exhaust_flow,
        valvetrain: standard_valvetrain(
            intake_camshaft: intake_camshaft,
            exhaust_camshaft: exhaust_camshaft
        ),
        flip_display: flip_display
    )
}

public node eng {
    alias output __out: engine;

    engine engine(
        name: "Ford 117 X8 Ex. #28",
        starter_torque: 50 * units.Nm,
        starter_speed: 400 * units.rpm,
        redline: redline * units.rpm,
        fuel: s30(),
        throttle_gamma: 1.5,
        jitter: 0.8,
        noise: 0.2,
        hf_gain: 0.005,
        simulation_frequency: 12000 
    )

    wires wires()

    connecting_rod_parameters cr_params(
        mass: con_rod_mass * units.g,
        moment_of_inertia: rod_moment_of_inertia(
            mass: con_rod_mass * units.g,
            length: con_rod * units.mm
        ),
        center_of_mass: 0.0,
        length: con_rod * units.mm
    )

    label crank_moment(disk_moment_of_inertia(mass: crank_mass * units.kg, radius: (stroke / 2.0) * units.mm))
    label flywheel_moment(disk_moment_of_inertia(mass: flywheel_mass * units.kg, radius: (stroke * 3.5) * units.mm))
    label other_moment(disk_moment_of_inertia(mass: 1 * units.kg, radius: 1.0 * units.cm))

    crankshaft c0(
        throw: (stroke / 2.0) * units.mm,
        flywheel_mass: flywheel_mass * units.kg,
        mass: crank_mass * units.kg,
        friction_torque: 2.9 * units.Nm,
        moment_of_inertia: crank_moment + flywheel_moment + other_moment,
        tdc: 0.0
    )

    rod_journal rj0(angle: rot90 + vee / 2)
    rod_journal rj1(angle: rot90 + vee / 2 + rot180)
    
    c0
        .add_rod_journal(rj0)
        .add_rod_journal(rj1)

    piston_parameters piston_params(
        mass: 350.0 * units.g,
        blowby: k_28inH2O(0.017),
        compression_height: compression_height,
        wrist_pin_position: 0.0,
        displacement: 0.0
    )

    cylinder_bank_parameters bank_params(
        bore: bore * units.mm,
        deck_height: (con_rod + stroke / 2.0) * units.mm + compression_height
    )

    label collector_cross_section_area(10.1)

    label exhaust_pipe_length_0(200.0) // cm
    label exhaust_volume_0(collector_cross_section_area * exhaust_pipe_length_0 / 100.0) // Litres
    
    label spacing_factor(1.1)
    label flange_density(1.0 * exhaust_delay_coeff)
    

    intake intake(
        plenum_volume: 1.0 * units.L,
        plenum_cross_section_area: 16.5 * units.cm2,
        runner_length: 13.6 * units.cm,
        intake_flow_rate: k_carb(117.3),
        idle_flow_rate: k_carb(0.01),
        idle_throttle_plate_position: 0.990,
        runner_flow_rate: k_carb(30.8),
        velocity_decay: 0.05
    )
    
    exhaust_system_parameters es_params0(
        outlet_flow_rate: k_carb(200.4),
        collector_cross_section_area: 10.1 * units.cm2,
        length: exhaust_pipe_length_0 * units.cm,
        primary_tube_length: 37.5 * units.cm,
        primary_flow_rate: k_carb(51.3),
        velocity_decay: 1.0,
        volume: (exhaust_volume_0 + 3.0) * units.L
    )

    exhaust_system exhaust0(es_params0, impulse_response: impulse_response(filename: "../../impulse_responses/smooth_53.wav", volume: 0.00025))
 
    cylinder_bank b0(bank_params, angle: vee / 2)
    cylinder_bank b1(bank_params, angle: -vee / 2)
    cylinder_bank b2(bank_params, angle: rot180 + vee / 2)
    cylinder_bank b3(bank_params, angle: rot180 - vee / 2)

    label sh(30.0 / flange_density * units.cm)
    label pl0((5.0 * bore * spacing_factor / flange_density) * units.mm)
    label pl1((2.0 * bore * spacing_factor / flange_density) * units.mm)

    b0.add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj0,
            intake: intake,
            exhaust_system: exhaust0,
            primary_length: pl0,
            ignition_wire: wires.wire1
        )
    b1.add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj0,
            intake: intake,
            exhaust_system: exhaust0,
            primary_length: pl0 + sh,
            ignition_wire: wires.wire2
        )
    b2.add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj0,
            intake: intake,
            exhaust_system: exhaust0,
            primary_length: pl0 + sh,
            ignition_wire: wires.wire3
        )
    b3.add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj0,
            intake: intake,
            exhaust_system: exhaust0,
            primary_length: pl0,
            ignition_wire: wires.wire4
        )

    b0.add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj1,
            intake: intake,
            exhaust_system: exhaust0,
            primary_length: pl1,
            ignition_wire: wires.wire5
        )
    b1.add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj1,
            intake: intake,
            exhaust_system: exhaust0,
            primary_length: pl1 + sh,
            ignition_wire: wires.wire6
        )
    b2.add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj1,
            intake: intake,
            exhaust_system: exhaust0,
            primary_length: pl1 + sh,
            ignition_wire: wires.wire7
        )
    b3.add_cylinder(
            piston: piston(piston_params),
            connecting_rod: connecting_rod(cr_params),
            rod_journal: rj1,
            intake: intake,
            exhaust_system: exhaust0,
            primary_length: pl1,
            ignition_wire: wires.wire8
        )

    engine
        .add_cylinder_bank(b0)
        .add_cylinder_bank(b1)
        .add_cylinder_bank(b2)
        .add_cylinder_bank(b3)

    engine.add_crankshaft(c0)

    camshaft_builder_0 camshaft()
    
    engine.add_ignition_module(
        distributor_0(
            wires: wires,
            timing_curve: timing_curve
        )
    )

    b0.set_cylinder_head (
        head(
            intake_camshaft: camshaft.intake_cam_0,
            exhaust_camshaft: camshaft.exhaust_cam_0,
            flip_display: true
        )
    )
    b1.set_cylinder_head (
        head(
            intake_camshaft: camshaft.intake_cam_1,
            exhaust_camshaft: camshaft.exhaust_cam_1
        )
    )
    b2.set_cylinder_head (
        head(
            intake_camshaft: camshaft.intake_cam_2,
            exhaust_camshaft: camshaft.exhaust_cam_2,
            flip_display: true
        )
    )
    b3.set_cylinder_head (
        head(
            intake_camshaft: camshaft.intake_cam_3,
            exhaust_camshaft: camshaft.exhaust_cam_3
        )
    )

    function timing_curve(233.3 * units.rpm)
    timing_curve
        .add_sample(0.0 * units.rpm, 5.0 * units.deg)
        .add_sample(233.3 * units.rpm, 5.6 * units.deg)
        .add_sample(466.7 * units.rpm, 9.3 * units.deg)
        .add_sample(700.0 * units.rpm, 12.5 * units.deg)
        .add_sample(933.3 * units.rpm, 15.4 * units.deg)
        .add_sample(1166.7 * units.rpm, 17.8 * units.deg)
        .add_sample(1400.0 * units.rpm, 20.0 * units.deg)
        .add_sample(1633.3 * units.rpm, 21.9 * units.deg)
        .add_sample(1866.7 * units.rpm, 23.6 * units.deg)
        .add_sample(2100.0 * units.rpm, 25.0 * units.deg)
        .add_sample(2333.3 * units.rpm, 26.3 * units.deg)
        .add_sample(2566.7 * units.rpm, 27.4 * units.deg)
        .add_sample(2800.0 * units.rpm, 28.3 * units.deg)
        .add_sample(3033.3 * units.rpm, 29.2 * units.deg)
        .add_sample(3266.7 * units.rpm, 29.9 * units.deg)
        .add_sample(3500.0 * units.rpm, 30.0 * units.deg)
        .add_sample(3733.3 * units.rpm, -15.0 * units.deg)
        .add_sample(3966.7 * units.rpm, -45.0 * units.deg)
}

// Ford A
public node veh {
    alias output __out: vehicle;
    vehicle vehicle(
        mass: 1027 * units.kg,
        drag_coefficient: 0.65,
        cross_sectional_area: (1702 * units.mm) * (1728 * units.mm),
        diff_ratio: 4.56, // AF-4209-B
        tire_radius: (807.0 / 2) * units.mm, // 6.00x19
        rolling_resistance: 20
        )
}

// Ford A
private node trn {
    alias output __out:
        transmission(
            max_clutch_torque: 200 * units.Nm,
            max_clutch_flex: 30 * units.deg,
            limit_clutch_flex: true,
            clutch_stiffness: 10 * units.Nm / units.deg,
            clutch_damping: 1.0,
            simulate_flex: true
        )
        .add_gear(3.12)
        .add_gear(1.85)
        .add_gear(1.0);
}

run(
    engine: eng(),
    transmission: trn(),
    vehicle: veh()
)
