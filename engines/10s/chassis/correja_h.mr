import "engine_sim.mr"

units units()

// 33.75 NACC 363.4 3 3/4 & 5 1/2 6c
public node veh {
    alias output __out: vehicle;
    label car_mass(1270.058) // 2800 fully equipped
    vehicle vehicle(
        mass: car_mass * units.kg,
        drag_coefficient: 0.95, // chatgpt
        cross_sectional_area: (1700 * units.mm) * (1800 * units.mm), // ?
        diff_ratio: 4.0,
        tire_radius: (921.0 / 2) * units.mm, // 34 x 4
        rolling_resistance: 0.013 * car_mass * 9.81
        )
}

public node trn {
    alias output __out:
        transmission(
            max_clutch_torque: 300 * units.Nm,
            max_clutch_flex: 10 * units.deg, // 5–12°
            limit_clutch_flex: true,
            clutch_stiffness: 80 * units.Nm / units.deg, // 80–200 Nm/°
            clutch_damping: 2.0,
            simulate_flex: true
        )
        .add_gear(2.625) // 42/16 = 2.625
        .add_gear(1.625) // 26/16 = 1.625
        .add_gear(1.00);
}
