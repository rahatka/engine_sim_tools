import "engine_sim.mr"

units units()

// 24.0 NACC 235.8 3 7/8 x 5 4c
public node veh {
    alias output __out: vehicle;
    label car_mass(1224.699) // 2700 fully equipped
    vehicle vehicle(
        mass: car_mass * units.kg,
        drag_coefficient: 0.95, // chatgpt
        cross_sectional_area: (1700 * units.mm) * (1800 * units.mm), // ?
        diff_ratio: 3.58, // 43/12 = 3.583333
        tire_radius: (876.0 / 2) * units.mm, // 33 × 4
        rolling_resistance: 0.013 * car_mass * 9.81
        )
}

public node trn {
    alias output __out:
        transmission(
            max_clutch_torque: 300 * units.Nm,
            max_clutch_flex: 10 * units.deg, // 5–12°
            limit_clutch_flex: true,
            clutch_stiffness: 80 * units.Nm / units.deg, // 80–200 Nm/°
            clutch_damping: 2.0,
            simulate_flex: true
        )
        .add_gear(3.200) // 48/15 = 3.2
        .add_gear(1.875) // 30/16 = 1.875
        .add_gear(1.00);
}
