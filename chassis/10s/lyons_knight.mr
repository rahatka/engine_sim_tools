import "engine_sim.mr"

units units()

// 32.4 NACC Knight 349.9 4 1/2 x 5 1/2 
public node veh {
    alias output __out: vehicle;
    label car_mass(1905.088) // 4200 fully equipped
    vehicle vehicle(
        mass: car_mass * units.kg,
        drag_coefficient: 0.95, // chatgpt
        cross_sectional_area: (1700 * units.mm) * (1800 * units.mm), // ?
        diff_ratio: 3.83, // 46/12 = 3.833333
        tire_radius: (935.0 / 2) * units.mm, // 36 x 5
        rolling_resistance: 0.013 * car_mass * 9.81
        )
}

public node trn {
    alias output __out:
        transmission(
            max_clutch_torque: 300 * units.Nm,
            max_clutch_flex: 10 * units.deg, // 5–12°
            limit_clutch_flex: true,
            clutch_stiffness: 80 * units.Nm / units.deg, // 80–200 Nm/°
            clutch_damping: 2.0,
            simulate_flex: true
        )
        .add_gear(3.329) // 40/12 = 3.333333
        .add_gear(1.632) // 31/19 = 1.631579
        .add_gear(1.00);
}
