import "engine_sim.mr"

units units()

// 38.4 NACC 414.7 4 x 5 1/2 6c
public node veh {
    alias output __out: vehicle;
    label car_mass(2041.165665) // 4500 lb fully equipped
    vehicle vehicle(
        mass: car_mass * units.kg,
        drag_coefficient: 0.95, // chatgpt
        cross_sectional_area: (1700 * units.mm) * (1800 * units.mm), // ?
        diff_ratio: 3.56, // 57/16 = 3.5625
        tire_radius: (935.0 / 2) * units.mm, // 36 x 4 1/2
        rolling_resistance: 0.013 * car_mass * 9.81
        )
}

public node trn {
    alias output __out:
        transmission(
            max_clutch_torque: 250 * units.Nm,
            max_clutch_flex: 10 * units.deg, // 5–12°
            limit_clutch_flex: true,
            clutch_stiffness: 80 * units.Nm / units.deg, // 80–200 Nm/°
            clutch_damping: 2.0,
            simulate_flex: true
        )
        .add_gear(3.652) // 51/14 = 3.642857
        .add_gear(2.022) // 59/29 = 2.034483
        .add_gear(1.478) // 34/23 = 1.478261
        .add_gear(1.00);
}
